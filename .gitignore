

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt
.aiox/handoffs/

# Next.js
.next/
out/

# Turborepo
.turbo/

# Vercel
.vercel/

# Drizzle
drizzle/meta/

# TypeScript build info
*.tsbuildinfo

# Test Coverage
coverage/

# Docker
*.pid

# Artefatos de sync do iCloud/Drive: o cliente duplica o arquivo com sufixo
# " 2" quando resolve conflito. Eles já foram commitados por engano uma vez
# (QA-4412-05, 8 arquivos e ~1.120 linhas), e um deles era um SERVIÇO dentro
# de `src/` — o tsc o compilava para `dist/`. Ignorar na origem é mais barato
# que revisar `git add`.
* 2.*
* 2/
*.d 2.ts
