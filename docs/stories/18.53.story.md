# Story 18.53: Captação Paga — Gráficos de Leads mostram Ingressos Únicos

**Epic:** 18 — Dashboard Improvements
**Status:** Done
**Created:** 2026-07-14 (@sm — River)
**Priority:** Medium
**Depends On:** 18.52 (`paidRows`/`overrideCplWithUniqueIngressos`)

<!-- clickup:task_id -->

---

## 📖 Contexto

Carry-forward do QA da 18.52: na Captação Paga, a tabela e o CPL já usam **ingressos únicos**, mas os gráficos de leads (Acumulados, Tendência+Meta, Projeção por custo) ainda mostravam **leads**, divergindo. Uniformizar para ingressos únicos na Paga (rótulos "Leads"→"Ingressos"). Gratuita intacta.

---

## ✅ Acceptance Criteria

**AC1** — `LeadsCumulativeChart`, `LeadsTrendAndGoalChart` e `LeadsProjectionCostBasedChart` recebem `paidRows` (já com leads=ingressos únicos na Paga, da 18.52) em vez de `metrics.rows`.

**AC2** — Cada gráfico ganha prop `isPaidCapture`; na Paga os rótulos (título, séries/legenda, tooltip) trocam "Leads" → "Ingressos".

**AC3** — Gratuita e demais etapas inalteradas (`isPaidCapture=false` → rótulos "Leads", `paidRows`=`metrics.rows`).

---

## 🧪 Definition of Done

- [x] 3 gráficos recebem `paidRows` + `isPaidCapture`.
- [x] Títulos/séries/tooltips com "Ingressos" na Paga.
- [x] Gratuita intacta.
- [x] `tsc --noEmit` verde + `next build` verde (27/27).

## Dev Agent Record

### File List
- `packages/web/components/funnels/leads-cumulative-chart.tsx`
- `packages/web/components/funnels/leads-trend-and-goal-chart.tsx`
- `packages/web/components/funnels/leads-projection-cost-based-chart.tsx`
- `packages/web/components/funnels/launch-dashboard.tsx` (passa `paidRows` + `isPaidCapture` aos 3)

### Notas
- Reusa `paidRows` da 18.52 (leads sobrescritos por ingressos únicos na Paga). `expandChartDataV2`/`buildLeadsCumulativeData`/`useLeadsProjection` somam `row.leadsPagos+leadsOrg+leadsSemTrack` → passam a somar ingressos únicos automaticamente.
- Rótulos via `name` nas séries + tooltip com prop `isPaidCapture` + título resolvido dinamicamente. `dataKey` internos preservados.

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-07-14 | @dev (Dex) | Criada e implementada (carry-forward QA 18.52). |
