# Story 29.15 — Funil Perpétuo: separar gráficos (Margem barras + Investimento linha) e colorir card Margem

**Epic:** 29
**Status:** InReview
**Points:** 2
**Tipo:** UX / Dataviz
**Prioridade:** P2

## Contexto

Ajustes pedidos por Danilo (2026-07-06) em cima da 29.14:
1. Separar os gráficos: manter **Margem no Tempo** (barras verde/vermelho) e transformar o Investimento num **gráfico de linha próprio** (só investimento), com valor numérico exibido a cada 7 dias.
2. Card **Margem**: colorir vermelho se ≤ 0, verde se > 0.

## Acceptance Criteria

- [x] **AC1:** O gráfico "Resultado no Tempo" (barras Receita−Investimento) foi **removido**. No lugar, dois gráficos empilhados full-width.
- [x] **AC2:** "Margem no Tempo" (barras verde/vermelho por sinal) fica **em cima**.
- [x] **AC3:** "Investimento no Tempo" vira **gráfico de linha** (só a série de investimento/spend com imposto), **abaixo**.
- [x] **AC4:** Na linha de investimento, o valor numérico aparece **a cada 7 dias** (`Spend7DayLabel` — renderiza label só quando `index % 7 === 0`), evitando poluição.
- [x] **AC5:** Card **Margem** colorido: borda/fundo + número **verde** se `margin > 0`, **vermelho** se `margin ≤ 0`. Quando `margin` é null (sem fonte de vendas, 29.10), fica neutro.
- [x] **AC6:** Typecheck OK. *(Build completo antes do push.)*

## Tasks

- [x] 1. Re-adicionar imports `LineChart`, `Line`, `LabelList`; criar `Spend7DayLabel` (label a cada 7 dias).
- [x] 2. Trocar o layout: grid-cols-1, Margem (barras) em cima, Investimento (linha) embaixo. Remover o gráfico "Resultado no Tempo".
- [x] 3. `KpiCard`: nova prop `signValue?: number | null` → colore verde (>0) / vermelho (≤0) borda, fundo e número. Neutro quando null.
- [x] 4. Card Margem passa `signValue={m.margin}`.
- [x] 5. Typecheck.

## Dev Notes

### Interpretação do layout (confirmar visualmente)
Entendi "manter apenas o gráfico Margem no Tempo na mesma linha e acima" como **Margem em cima** e **Investimento (linha) embaixo**, ambos full-width. Se o Danilo quiser invertido ou lado a lado, é ajuste de 1 linha (grid).

### Investimento agora é linha simples
O gráfico de investimento voltou a ser linha (não barra), pois é uma série só e o pedido foi valor numérico a cada 7 dias — melhor lido em linha. Tooltip usa o `FormulaChartTooltip` padrão (fórmula do Investimento).

### Código residual
O campo `resultado` do `dailyChartData` e a variante `bruto` do `DailyResultTooltip` ficaram sem uso após remover o gráfico "Resultado no Tempo" — inofensivos, removíveis num cleanup futuro se o layout estabilizar.

## File List

- `packages/web/components/funnels/perpetual-dashboard.tsx` *(modified)* — layout dos gráficos (Margem barras em cima, Investimento linha embaixo com label a cada 7 dias); `Spend7DayLabel`; `KpiCard` ganha `signValue` e card Margem colore por sinal.

## Change Log

| Data | Quem | Mudança |
|---|---|---|
| 2026-07-06 | @dev Dex | Gráficos separados (Margem barras / Investimento linha 7-dias); card Margem colorido por sinal via `signValue`. Typecheck OK. Status: InReview. |
