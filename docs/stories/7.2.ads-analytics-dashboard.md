# Story 7.2: Ads Analytics Dashboard — Metricas de Midia

---

## Status

**Draft**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, ui_validation]
priority: P0
estimate: L
depends_on: ["6.3"]
epic: "EPIC-7-TRAFFIC-ANALYTICS"
```

---

## Story

**As a** gestor de trafego da Loyola Digital,
**I want** a dashboard with charts showing campaign performance (spend, CTR, CPC, CPM over time) and a drill-down table by campaign > ad set > ad,
**so that** I can analyze media metrics and identify top-performing campaigns and creatives.

---

## Acceptance Criteria

1. [ ] Dashboard `/traffic` refatorado: seletor de conta Meta Ads no topo e filtro de periodo (7d, 14d, 30d, 90d)
2. [ ] Cards de resumo: Total Spend, Total Impressions, Total Clicks, CTR medio, CPC medio, CPM medio
3. [ ] Grafico de linha: spend diario + clicks diarios no periodo selecionado (eixo duplo Y)
4. [ ] Tabela de campanhas com colunas: Nome | Status | Spend | Impressions | Clicks | CTR | CPC | CPM
5. [ ] Metricas calculadas: CTR = clicks/impressions × 100, CPC = spend/clicks, CPM = (spend×1000)/impressions
6. [ ] Drill-down: clicar em campanha expande e mostra ad sets da campanha; clicar em ad set expande e mostra ads
7. [ ] `GET /api/meta-ads/accounts/:id/adsets?campaignId=X` — nova rota: ad sets de uma campanha com metricas
8. [ ] `GET /api/meta-ads/accounts/:id/ads?adsetId=X` — nova rota: ads de um ad set com metricas
9. [ ] `GET /api/meta-ads/accounts/:id/insights/daily?days=30` — nova rota: insights com breakdown por dia
10. [ ] Hooks: `useMetaAdsAdSets(accountId, campaignId)`, `useMetaAdsAds(accountId, adsetId)`, `useMetaAdsDailyInsights(accountId, days)`
11. [ ] Loading skeletons para graficos e tabelas
12. [ ] Empty state quando conta nao tem campanhas ativas
13. [ ] Formatacao de valores: spend em R$ (ou currency da conta), percentuais com 2 casas, numeros grandes com K/M
14. [ ] `pnpm typecheck` e `pnpm lint` passam sem erros

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **Frontend:** Next.js 15 App Router, Tailwind CSS, shadcn/ui [Source: architecture/fullstack-architecture]
- **State:** React Query (`@tanstack/react-query`) via `useApiClient()` [Source: padrao em lib/hooks/use-meta-ads.ts]
- **Charts:** Instalar Recharts (`recharts`) — library de graficos mais popular para React, leve e declarativa
- **Dashboard existente:** `packages/web/app/(app)/traffic/page.tsx` — atualmente so mostra cards de contas (shell do 6.3)
- **Hooks existentes:** `packages/web/lib/hooks/use-meta-ads.ts` — `useMetaAdsAccounts()`, `useCreateMetaAdsAccount()`, etc.
- **Meta Ads service:** `packages/api/src/services/meta-ads.ts` — ja tem `fetchCampaigns()`, `fetchInsights()`, `fetchMeta()`
- **Meta Ads routes:** `packages/api/src/routes/meta-ads.ts` — ja tem campaigns e insights basicos

### Meta Marketing API — Novas chamadas necessarias

```typescript
// Ad Sets de uma campanha:
// GET /act_{ID}/adsets?fields=id,name,status,targeting,bid_amount,daily_budget&filtering=[{"field":"campaign_id","operator":"EQUAL","value":"{CAMPAIGN_ID}"}]

// Ads de um ad set:
// GET /act_{ID}/ads?fields=id,name,status,creative&filtering=[{"field":"adset_id","operator":"EQUAL","value":"{ADSET_ID}"}]

// Insights diarios (breakdown por dia):
// GET /act_{ID}/insights?fields=impressions,reach,clicks,spend,ctr,cpc,cpm&time_increment=1&date_preset=last_30d&level=account

// Insights por campanha:
// GET /act_{ID}/insights?fields=impressions,reach,clicks,spend,ctr,cpc,cpm&date_preset=last_30d&level=campaign

// Insights por ad set:
// GET /act_{ID}/insights?fields=impressions,reach,clicks,spend,ctr,cpc,cpm&date_preset=last_30d&level=adset&filtering=[{"field":"campaign_id","operator":"EQUAL","value":"{CAMPAIGN_ID}"}]

// Insights por ad:
// GET /act_{ID}/insights?fields=impressions,reach,clicks,spend,ctr,cpc,cpm&date_preset=last_30d&level=ad&filtering=[{"field":"adset_id","operator":"EQUAL","value":"{ADSET_ID}"}]
```

### Novas funcoes no service meta-ads.ts

```typescript
// Adicionar ao packages/api/src/services/meta-ads.ts:

export async function fetchAdSets(
  metaAccountId: string,
  accessToken: string,
  campaignId: string
): Promise<MetaAdSet[]> { ... }

export async function fetchAds(
  metaAccountId: string,
  accessToken: string,
  adsetId: string
): Promise<MetaAd[]> { ... }

export async function fetchDailyInsights(
  metaAccountId: string,
  accessToken: string,
  days: number = 30
): Promise<MetaDailyInsight[]> { ... }

export async function fetchCampaignInsights(
  metaAccountId: string,
  accessToken: string,
  days: number = 30
): Promise<MetaCampaignInsight[]> { ... }

// Tipos adicionais:
interface MetaAdSet {
  id: string; name: string; status: string;
  targeting?: object; bid_amount?: string; daily_budget?: string;
}

interface MetaAd {
  id: string; name: string; status: string;
  creative?: { id: string };
}

interface MetaDailyInsight {
  date_start: string; date_stop: string;
  impressions: string; reach: string; clicks: string;
  spend: string; ctr: string; cpc: string; cpm: string;
}

interface MetaCampaignInsight extends MetaDailyInsight {
  campaign_id: string; campaign_name: string;
}
```

### Dashboard UI Structure

```
/traffic (refatorado)
├── AccountSelector (dropdown de contas Meta Ads)
├── PeriodFilter (7d | 14d | 30d | 90d)
├── SummaryCards (spend, impressions, clicks, CTR, CPC, CPM)
├── DailyChart (Recharts LineChart — spend + clicks dual Y axis)
└── CampaignTable (expandable rows)
    ├── Campaign row (nome, status, spend, impressions, clicks, CTR, CPC, CPM)
    │   └── [expand] AdSet rows
    │       └── [expand] Ad rows
```

### Key Files

| File | Change |
|------|--------|
| `packages/api/src/services/meta-ads.ts` | Adicionar `fetchAdSets()`, `fetchAds()`, `fetchDailyInsights()`, `fetchCampaignInsights()`, tipos |
| `packages/api/src/routes/meta-ads.ts` | Adicionar 3 novas rotas: adsets, ads, daily insights |
| `packages/web/app/(app)/traffic/page.tsx` | Refatorar: dashboard completo com charts e tabela |
| `packages/web/lib/hooks/use-meta-ads.ts` | Adicionar hooks: `useMetaAdsAdSets`, `useMetaAdsAds`, `useMetaAdsDailyInsights`, `useMetaAdsCampaignInsights` |
| `packages/web/package.json` | Adicionar `recharts` dependency |

### Previous Story Insights (6.3)

- Dashboard atual e shell simples com cards de contas — refatorar para dashboard real
- Hooks com React Query funcionam bem — `queryKey` com params para cache por conta/periodo
- Loading skeletons ja tem padrao no projeto (Skeleton component do shadcn)
- `useApiClient()` retorna funcao tipada para fetch

---

## Tasks

- [ ] **Task 1:** Instalar `recharts` no `packages/web` (`pnpm add recharts`)
- [ ] **Task 2 (AC: 7, 8, 9):** Adicionar funcoes `fetchAdSets()`, `fetchAds()`, `fetchDailyInsights()`, `fetchCampaignInsights()` no service `meta-ads.ts` com tipos TypeScript
- [ ] **Task 3 (AC: 7):** Implementar rota `GET /api/meta-ads/accounts/:id/adsets?campaignId=X` — decrypt token, fetch ad sets, retornar com insights
- [ ] **Task 4 (AC: 8):** Implementar rota `GET /api/meta-ads/accounts/:id/ads?adsetId=X` — decrypt token, fetch ads, retornar com insights
- [ ] **Task 5 (AC: 9):** Implementar rota `GET /api/meta-ads/accounts/:id/insights/daily?days=30` — insights com `time_increment=1`
- [ ] **Task 6 (AC: 10):** Criar hooks `useMetaAdsAdSets`, `useMetaAdsAds`, `useMetaAdsDailyInsights`, `useMetaAdsCampaignInsights` no `use-meta-ads.ts`
- [ ] **Task 7 (AC: 1):** Refatorar `/traffic/page.tsx` — adicionar AccountSelector (dropdown) e PeriodFilter (7d/14d/30d/90d) no topo
- [ ] **Task 8 (AC: 2, 5, 13):** Implementar SummaryCards — 6 cards com metricas calculadas e formatacao (R$, %, K/M)
- [ ] **Task 9 (AC: 3):** Implementar DailyChart — Recharts LineChart com eixo duplo Y (spend esquerda, clicks direita)
- [ ] **Task 10 (AC: 4, 5, 6):** Implementar CampaignTable — tabela expandivel com drill-down por ad set e ad
- [ ] **Task 11 (AC: 11, 12):** Loading skeletons para charts/tabela + empty state
- [ ] **Task 12 (AC: 14):** Executar `pnpm typecheck` e `pnpm lint`

---

## Testing

- Seletor de conta funciona — altera dados do dashboard
- Filtro de periodo funciona — altera range de dados
- Cards mostram metricas calculadas corretamente (CTR, CPC, CPM)
- Grafico de linha renderiza spend + clicks por dia
- Tabela de campanhas carrega e mostra metricas
- Drill-down funciona: campanha → ad sets → ads
- Loading skeletons exibidos durante fetch
- Empty state quando sem campanhas
- Formatacao: R$ para valores monetarios, % para taxas, K/M para numeros grandes
- typecheck e lint passam

---

## Dev Agent Record

### File List

| File | Status |
|------|--------|
| | |

### Debug Log

_(empty)_

### Completion Notes

_(empty)_

### Change Log

_(empty)_
