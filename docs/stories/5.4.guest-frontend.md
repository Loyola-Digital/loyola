# Story 5.4: Guest Frontend — Sidebar Simplificada + Fluxo de Convite

---

## Status

**Ready for Review**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools: [ux_review, a11y_check]
priority: P1
estimate: M
depends_on: ["5.3", "4.3"]
epic: "EPIC-5-GUEST-ACCESS"
```

---

## Story

**As a** guest user (client),
**I want** to see only the projects and modules I was invited to,
**so that** I have a focused experience without access to internal Loyola data.

---

## Acceptance Criteria

**Fluxo de convite (admin):**
1. [ ] Dialog "Convidar pessoa" dentro da overview do projeto (`/projects/[id]`)
   - Campo: e-mail do convidado
   - Toggles por módulo: Instagram, Conversas, Mind (todos on por default)
   - Botão "Enviar convite" → `POST /api/projects/:id/invitations`
   - Exibe URL do convite para admin copiar
2. [ ] Tela de membros do projeto exibe cada guest com suas permissões atuais
3. [ ] Admin pode editar permissões inline (toggles) via `PATCH .../permissions`
4. [ ] Admin pode remover guest ("Remover acesso") via `DELETE .../members/:userId`

**Fluxo de aceite (guest):**
5. [ ] Página `/invite/[token]` exibe: nome do projeto, quem convidou, e-mail, módulos liberados
6. [ ] Botão "Aceitar convite" → `POST /api/invitations/:token/accept` → redirect para `/projects/[id]`
7. [ ] Token expirado → mensagem "Este convite expirou. Peça um novo ao seu gestor."
8. [ ] Token inválido → mensagem de erro

**Experiência do guest:**
9. [ ] Sidebar do guest (`GuestSidebar`) renderiza apenas projetos onde é membro
10. [ ] Dentro de cada projeto, exibe apenas módulos com `permissions[module] = true`
11. [ ] Sem Settings, sem Tasks, sem navegação global, sem outros projetos
12. [ ] Guest tentando acessar módulo bloqueado via URL → redirect com toast "Você não tem acesso a este módulo"
13. [ ] Layout não exibe botões de admin (ex: "+ Novo Projeto", "+ Convidar", "+ Adicionar conta")
14. [ ] `useUserRole()` hook retorna o role do usuário autenticado
15. [ ] `app-sidebar.tsx` renderiza `GuestSidebar` se `role === 'guest'`, `AppSidebar` se admin
16. [ ] `middleware.ts` do Next.js: guest que tenta acessar rota global → redirect para `/projects` (primeiro projeto)

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **Auth:** Usar `useUser()` do `@clerk/nextjs` para acessar `publicMetadata.role`
- **GuestSidebar:** componente completamente separado do `AppSidebar` — não reutilizar diretamente para evitar complexidade condicional
- **InviteMemberDialog:** Dialog com campos e toggles — padrão dos dialogs existentes (`add-account-dialog.tsx`)
- **Middleware Next.js:** `packages/web/middleware.ts` — adicionar lógica de redirect para guest

### GuestSidebar Data

```typescript
// Dados necessários:
// - useProjects() filtrado: API retorna apenas projetos do guest (Story 5.3 filtra no backend)
// - Para cada projeto: project_members.permissions do guest (retornado junto com projeto ou endpoint separado)
```

### useUserRole Hook

```typescript
// packages/web/lib/hooks/use-user-role.ts
import { useUser } from "@clerk/nextjs";
export function useUserRole() {
  const { user } = useUser();
  return user?.publicMetadata?.role as string ?? "admin";
}
```

### Middleware Redirect

```typescript
// middleware.ts — adicionar após auth check:
// Se role === 'guest' e pathname não começa com /projects ou /invite:
// → redirect para /projects
```

---

## Testing

### Validation Commands

```bash
cd packages/web
pnpm lint
pnpm typecheck
```

### Test Scenarios

1. Admin navega para `/projects/[id]` → botão "Convidar" visível, dialog abre
2. Admin cria convite → URL exibida
3. Guest acessa `/invite/[token]` → exibe dados do convite
4. Guest clica "Aceitar" → redirect para projeto
5. Token expirado → mensagem de erro clara
6. Guest faz login → GuestSidebar exibida (sem navegação global)
7. Guest vê apenas módulos com permission=true
8. Guest tenta acessar `/tasks` → redirect para `/projects`
9. Admin vê AppSidebar normal (não afetado)

---

## Tasks / Subtasks

- [x] **Task 1: Hook useUserRole + useProjectMembers** (AC: 14)
  - [x] 1.1 Criar `packages/web/lib/hooks/use-user-role.ts`
  - [x] 1.2 Criar `useProjectMembers(projectId)` em `use-projects.ts` (GET /api/projects/:id/members)
  - [x] 1.3 Criar mutations: `useInviteMember()`, `useRemoveMember()`, `useUpdateMemberPermissions()`

- [x] **Task 2: Dialog de convite (admin)** (AC: 1, 2, 3, 4)
  - [x] 2.1 Criar `packages/web/components/projects/invite-member-dialog.tsx`
  - [x] 2.2 Campos: email + toggles Instagram/Conversas/Mind
  - [x] 2.3 Exibir URL do convite após criação (copy button)
  - [x] 2.4 Criar `packages/web/components/projects/member-permissions-editor.tsx`
  - [x] 2.5 Integrar na página `/projects/[id]`

- [x] **Task 3: Página de aceite** (AC: 5, 6, 7, 8)
  - [x] 3.1 Criar `packages/web/app/invite/[token]/page.tsx` (rota pública, fora do layout `(app)`)
  - [x] 3.2 Fetch `GET /api/invitations/:token` ao carregar
  - [x] 3.3 Exibir dados do convite + lista de módulos
  - [x] 3.4 Botão "Aceitar" → `POST /api/invitations/:token/accept` → tela de sucesso com link para sign-in

- [x] **Task 4: GuestSidebar** (AC: 9, 10, 11, 13)
  - [x] 4.1 Criar `packages/web/components/layout/guest-sidebar.tsx`
  - [x] 4.2 Renderizar apenas projetos do guest
  - [x] 4.3 Dentro de cada projeto: apenas módulos com permission=true (via `useMyMembership`)
  - [x] 4.4 Sem botões de admin

- [x] **Task 5: Atualizar app-sidebar.tsx** (AC: 15)
  - [x] 5.1 Usar `useUserRole()` → se `role === 'guest'`: renderizar `<GuestSidebar>`

- [x] **Task 6: Middleware Next.js** (AC: 12, 16)
  - [x] 6.1 Atualizar `packages/web/middleware.ts`
  - [x] 6.2 Guest acessando rota global → redirect para `/projects`
  - [x] 6.3 `/invite/(.*)` adicionado às rotas públicas (sem auth)

- [x] **Task 7: Lint e typecheck** (AC: todos)
  - [x] 7.1 `pnpm lint` e `pnpm typecheck` — zero erros

---

## File List

> Atualizado pelo @dev durante implementacao

| File | Status |
|------|--------|
| `packages/web/lib/hooks/use-user-role.ts` | Created |
| `packages/web/lib/hooks/use-projects.ts` | Modified |
| `packages/web/components/projects/invite-member-dialog.tsx` | Created |
| `packages/web/components/projects/member-permissions-editor.tsx` | Created |
| `packages/web/components/layout/guest-sidebar.tsx` | Created |
| `packages/web/components/layout/app-sidebar.tsx` | Modified |
| `packages/web/components/ui/switch.tsx` | Created (shadcn) |
| `packages/web/app/(app)/projects/[id]/page.tsx` | Modified |
| `packages/web/app/invite/[token]/page.tsx` | Created |
| `packages/web/middleware.ts` | Modified |
| `packages/api/src/routes/projects.ts` | Modified |

---

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log

- `Switch` não estava presente em `components/ui/` — adicionado via `npx shadcn@latest add switch`
- `/invite/[token]` é rota pública (fora do layout `(app)`) — usa fetch nativo em vez de `useApiClient` para evitar dependência de auth
- `useMyMembership` usa `retry: false` para não fazer loop em caso de 404 (guest em projeto sem membership)
- AC 6 (redirect após aceite): MVP redireciona para tela de sucesso com link sign-in, pois o guest ainda não está autenticado no Clerk
- `GET /api/projects/:id/my-membership` adicionado em `projects.ts` (backend) para que GuestSidebar possa ler permissões sem ser owner do projeto

### Completion Notes

- `use-user-role.ts` — lê `publicMetadata.role` do Clerk, fallback `"admin"`
- `use-projects.ts` — adicionados `useProjectMembers`, `useMyMembership`, `useInviteMember`, `useRemoveMember`, `useUpdateMemberPermissions`
- `invite-member-dialog.tsx` — dialog com email + toggles de módulo + copy URL após criação
- `member-permissions-editor.tsx` — lista membros com toggles inline + botão remover
- `guest-sidebar.tsx` — sidebar simplificada: apenas projetos do guest + módulos com permission=true via `useMyMembership`
- `app-sidebar.tsx` — renderiza `GuestSidebar` se `role === 'guest'`
- `middleware.ts` — `/invite/(.*)` público; guests em rota global → redirect `/projects`
- `projects/[id]/page.tsx` — botão "Convidar" + seção de membros visíveis apenas para admin
- `projects.ts` (API) — novo endpoint `GET /api/projects/:id/my-membership`

### Change Log

- `packages/web/lib/hooks/use-user-role.ts`: criado — retorna role do Clerk publicMetadata
- `packages/web/lib/hooks/use-projects.ts`: adicionados tipos `ProjectPermissions`, `ProjectMember`, hooks e mutations de membros
- `packages/web/components/projects/invite-member-dialog.tsx`: criado — dialog de convite com copy URL
- `packages/web/components/projects/member-permissions-editor.tsx`: criado — editor inline de permissões
- `packages/web/components/layout/guest-sidebar.tsx`: criado — sidebar para guests
- `packages/web/components/layout/app-sidebar.tsx`: adicionado `useUserRole()` + conditional render `GuestSidebar`
- `packages/web/components/ui/switch.tsx`: criado via shadcn
- `packages/web/app/(app)/projects/[id]/page.tsx`: botão "Convidar" + seção membros (admin only)
- `packages/web/app/invite/[token]/page.tsx`: criado — página pública de aceite de convite
- `packages/web/middleware.ts`: `/invite/(.*)` público, redirect guest em rotas globais
- `packages/api/src/routes/projects.ts`: endpoint `GET /api/projects/:id/my-membership`
