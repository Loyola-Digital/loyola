# Story 7.6: Sales Funnel Analysis — Vendas por UTM + ROAS Real

---

## Status

**Draft**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation]
priority: P0
estimate: M
depends_on: ["7.1", "7.4"]
epic: "EPIC-7-TRAFFIC-ANALYTICS"
```

---

## Story

**As a** gestor de trafego da Loyola Digital,
**I want** the system to cross-reference sales data from Google Sheets with Meta Ads campaigns via UTM,
**so that** I can see real sales count, revenue, cost per sale, ROAS, and conversion rate per campaign/ad set/ad.

---

## Acceptance Criteria

1. [ ] Service `sales-analytics.ts`: le aba "sales" do Sheets via `getTabData()` com column mapping (utmCampaign, utmMedium, utmContent, valor)
2. [ ] Contagem de vendas por UTM — reusar padrao `countLeadsByUtm()` do traffic-analytics
3. [ ] Calculo de receita: somar coluna "valor" agrupada por UTM Campaign/Medium/Content
4. [ ] `GET /api/traffic/analytics/:projectId/campaigns` expandido — campos adicionais: `sales`, `revenue`, `costPerSale`, `roas`, `conversionRate`
5. [ ] `costPerSale` = spend / sales (null se sales = 0)
6. [ ] `roas` = revenue / spend (null se spend = 0)
7. [ ] `conversionRate` = sales / leads × 100 (null se leads = 0 ou null)
8. [ ] Mesma logica para rotas adsets e ads
9. [ ] Vendas sem UTM atribuivel mostradas em `unattributedSales` no response
10. [ ] Dashboard: colunas Vendas, Custo/Venda, ROAS visiveis quando aba "sales" mapeada
11. [ ] Cache compartilhado com traffic-analytics (mesma TTL 15min, mesmo `invalidateProjectCache`)
12. [ ] `pnpm typecheck` e `pnpm lint` passam sem erros

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **traffic-analytics.ts:** Ja tem `getLeadsForProject()`, `getMetaAccountForProject()`, `countLeadsByUtm()`, `normalizeUtm()`, `matchUtm()` — tudo reutilizavel
- **google-sheets.ts:** `getTabData(spreadsheetId, tabName, columnMapping)` — retorna rows com campos mapeados e UTMs normalizados
- **Tab mapping:** Aba "sales" mapeada com `{ utmCampaign, utmMedium, utmContent, valor }` — column mapping JSONB
- **CampaignAnalytics interface:** Atualmente tem `leads` e `cpl` — expandir com `sales`, `revenue`, `costPerSale`, `roas`, `conversionRate`
- **Cache:** `getCached()`, `setCache()`, `invalidateProjectCache()` ja existem

### Sales Data Flow

```
1. getTabData(spreadsheetId, "Vendas", columnMapping)
   → Returns: [{ utmCampaign: "campanha_1", utmMedium: "publico_frio", utmContent: "criativo_1", valor: "297" }, ...]

2. Group by UTM Campaign:
   → campanha_1: 15 vendas, receita R$ 4.455
   → campanha_2: 8 vendas, receita R$ 2.376

3. Cross with Meta Ads:
   → campanha_1: spend R$ 2.000, 15 vendas, R$ 4.455 receita
     → costPerSale: R$ 133.33
     → ROAS: 2.23 (receita / spend)
     → conversionRate: 15/50 × 100 = 30% (vendas / leads)
```

### Expanded CampaignAnalytics Interface

```typescript
// Expand existing CampaignAnalytics in traffic-analytics.ts
export interface CampaignAnalytics {
  // ... existing fields (campaignId, campaignName, spend, impressions, clicks, ctr, cpc, cpm, leads, cpl)

  // Qualification (Story 7.5)
  qualifiedLeads: number | null;
  cplQualified: number | null;
  qualificationRate: number | null;

  // Sales (Story 7.6)
  sales: number | null;
  revenue: number | null;
  costPerSale: number | null;
  roas: number | null;
  conversionRate: number | null;
}
```

### getSalesForProject() Pattern

```typescript
// Follow same pattern as getLeadsForProject()
async function getSalesForProject(
  db: Database,
  projectId: string
): Promise<{ rows: Record<string, string>[]; valorField: string } | null> {
  // 1. Find connection
  // 2. Find tab mapping with tabType === "sales"
  // 3. getTabData() with column mapping
  // 4. Return rows + identify which field has "valor"
}

// Count sales + sum revenue
function countSalesByUtm(
  sales: Record<string, string>[],
  utmField: string,
  valorField: string,
  entities: { id: string; name: string }[]
): { matched: Map<string, { count: number; revenue: number }>; unattributed: { count: number; revenue: number } }
```

### Key Files

| File | Change |
|------|--------|
| `packages/api/src/services/traffic-analytics.ts` | Expandir — add `getSalesForProject()`, integrate sales into analytics responses |
| `packages/api/src/services/traffic-analytics.ts` | Expandir `CampaignAnalytics` interface com sales fields |
| `packages/web/lib/hooks/use-traffic-analytics.ts` | Expandir types com sales fields |
| `packages/web/app/(app)/traffic/page.tsx` | Expandir tabela — colunas Vendas, Custo/Venda, ROAS |

### Previous Story Insights (7.4)

- `getLeadsForProject()` fetches connection → finds tab by type → reads data — same pattern for sales
- `countLeadsByUtm()` returns `{ matched: Map<id, count>, unattributed }` — extend to include revenue
- Cache key pattern: `analytics:${projectId}:campaigns:${days}` — same invalidation works

---

## Tasks

- [ ] **Task 1 (AC: 1, 2, 3):** Add `getSalesForProject()` and `countSalesByUtm()` to traffic-analytics.ts — reads "sales" tab, groups by UTM, sums valor
- [ ] **Task 2 (AC: 4, 5, 6, 7, 8, 9, 11):** Expand `getProjectCampaignAnalytics()`, `getProjectAdSetAnalytics()`, `getProjectAdAnalytics()` — integrate sales data, calculate costPerSale/roas/conversionRate
- [ ] **Task 3:** Update `CampaignAnalytics` interface + frontend types in `use-traffic-analytics.ts`
- [ ] **Task 4 (AC: 10):** Dashboard: add conditional Vendas/Custo-Venda/ROAS columns in campaign table + drill-down
- [ ] **Task 5 (AC: 12):** Executar `pnpm typecheck` e `pnpm lint`

---

## Testing

- Sales contadas corretamente por UTM Campaign/Medium/Content
- Revenue somada corretamente (campo valor)
- costPerSale = spend / sales
- ROAS = revenue / spend
- conversionRate = sales / leads × 100
- Null values quando sales = 0 ou leads = null
- Vendas sem UTM em unattributedSales
- Dashboard mostra colunas apenas quando aba "sales" mapeada
- Drill-down adsets/ads inclui metricas de vendas
- Cache funciona (mesma invalidation)
- typecheck e lint passam

---

## Dev Agent Record

### File List

| File | Status |
|------|--------|
| | |

### Debug Log

_(empty)_

### Completion Notes

_(empty)_

### Change Log

_(empty)_
