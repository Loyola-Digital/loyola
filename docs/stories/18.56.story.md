# Story 18.56: Testes de LPs — Link manual por LP com nome hiperlinkado

**Epic:** 18 — Dashboard Improvements
**Status:** Done
**Created:** 2026-07-16 (@sm — River)
**Priority:** Low
**Depends On:** 18.44/18.46 (tabela Desempenho de Testes de LPs), 18.50 (Vendas/ROAS por LP — apenas contexto, sem acoplamento)

<!-- clickup:86ajjfn3j -->

---

## 📖 Contexto

A tabela **Desempenho de Testes de LPs** (`lp-performance-table.tsx`, renderizada pela `LpPerformanceSection` do `launch-dashboard.tsx`) lista as LPs do stage — mas as LPs são **derivadas dinamicamente** do Campaign Name (Story 18.46: "lpX" no nome da campanha → "LPA", "LPB"...). Não existe entidade LP no banco, portanto não há onde guardar a URL real de cada página.

O usuário quer **cadastrar manualmente o link de cada LP direto na tabela** e, uma vez cadastrado, o **nome da LP vira hiperlink** — um clique abre a página real para conferir o criativo/copy sem caçar a URL no Notion/planilha.

## 🗳️ Decisões de escopo (elicitação com o usuário, 2026-07-16)

| Dúvida | Decisão |
|--------|---------|
| UX de edição | **Lápis na linha:** ícone de editar ao lado do nome da LP; clica → popover com input de URL → salvar |
| Escopo do link | **Por etapa:** cada stage tem seus próprios links (LPA da Paga ≠ LPA da Gratuita). Persistência em `funnel_stages` (JSONB), seguindo o padrão de `campaigns`/`switchyLinkedLinks` |

---

## ✅ Acceptance Criteria

**AC1 — Backend: persistência `lpLinks` por stage**
- Nova coluna `lp_links` (JSONB, default `{}`) em `funnel_stages`, tipada `Record<string, string>` — chave = `lpName` normalizado (`trim().toLowerCase()`, ex.: `"lpa"`), valor = URL.
- Migration nova (seguir numeração `packages/api/src/db/migrations/`, próxima após `0079`).
- **Given** o PUT `/api/projects/:projectId/funnels/:funnelId/stages/:stageId` existente, **when** o body inclui `lpLinks`, **then** o campo é validado (Zod: record de strings; cada valor deve ser URL `http(s)://` válida OU string vazia — vazia remove o link) e persistido; `stageShape` passa a retornar `lpLinks`. Demais campos do PUT inalterados.
- Guest continua bloqueado pelo endpoint (comportamento atual, linha 361).

**AC2 — Edição inline na tabela (lápis por linha)**
- **Given** a tabela Desempenho de Testes de LPs, **when** renderiza cada linha, **then** um ícone de lápis aparece ao lado do nome da LP (visível em hover da linha ou sempre — seguir padrão visual das tabelas existentes).
- **When** clico no lápis, **then** abre um popover com input de URL (pré-preenchido se já houver link), botões Salvar/Cancelar.
- **When** salvo, **then** o PUT do stage é chamado com o `lpLinks` atualizado (merge: só a chave da LP editada muda) e a UI reflete sem reload (invalidation/optimistic do React Query).
- Salvar com o campo **vazio remove** o link da LP (nome volta a texto puro).

**AC3 — Nome hiperlinkado**
- **Given** uma LP com link cadastrado, **when** a tabela renderiza, **then** o nome (`row.lpName`) vira `<a href={url} target="_blank" rel="noopener noreferrer">` com affordance visual de link (cor/underline discreto e/ou ícone de link externo).
- LP **sem** link cadastrado: nome continua texto puro, idêntico a hoje.
- O match usa a mesma normalização do agregado (`lpName.trim().toLowerCase()`, como `lpTotals` no `useLpPerformanceData.ts:94`).

**AC4 — Validação de URL**
- **Given** o popover, **when** digito algo que não é URL `http(s)://` válida (e não-vazio), **then** o Salvar é bloqueado com mensagem de erro inline ("URL inválida — use http:// ou https://"). Trim aplicado antes de validar.

**AC5 — Escopo por etapa**
- **Given** o mesmo funil, **when** cadastro o link da LPA na Captação Paga, **then** a LPA da Captação Gratuita continua sem link (e vice-versa) — cada stage lê/escreve seu próprio `lpLinks`.

**AC6 — Regressão zero**
- Stage sem nenhum link cadastrado: tabela byte-a-byte como hoje (exceto o lápis). Métricas, ordenação, filtro de público (Hot/Cold/Todos) e colunas paid/free intactos.
- O PUT de stage sem `lpLinks` no body não toca o campo (partial update, padrão das linhas 385-393 de `funnel-stages.ts`).

---

## 📊 Complexidade & Valor

- **Estimativa:** S (small) — 1 coluna JSONB + extensão de endpoint existente + popover de edição + render condicional do nome. Sem endpoint novo, sem cruzamento de dados.
- **Valor de negócio:** o gestor confere a página real de cada LP em 1 clique durante a análise de testes — hoje a URL vive fora do dashboard (Notion/planilha/memória), quebrando o fluxo de análise.

## ⚠️ Riscos

- **R1 (baixo)** — LPs somem da tabela quando o período/campanhas mudam (são derivadas dos Campaign Names do range): o link cadastrado fica órfão no JSONB, invisível mas persistido. **Mitigação:** aceitável — se a LP reaparecer, o link volta; órfãos não têm efeito colateral.
- **R2 (baixo)** — URL maliciosa/typo (`javascript:` etc.): **Mitigação:** validação AC4 restringe a `http(s)://` no frontend E no Zod do backend; `rel="noopener noreferrer"` no anchor.
- **R3 (baixo)** — Corrida entre dois usuários editando links do mesmo stage (PUT com objeto inteiro): último ganha. **Mitigação:** aceitável no tamanho do time; merge por chave no client reduz janela.

## 📦 Escopo

**IN:**
- `packages/api/src/db/schema.ts` — coluna `lpLinks` (JSONB) em `funnelStages`.
- `packages/api/src/db/migrations/00XX_stage_lp_links.sql` — migration.
- `packages/api/src/routes/funnel-stages.ts` — `updateStageSchema` + `updates` + `stageShape` com `lpLinks`.
- `packages/web/lib/components/funnels/lp-performance-table.tsx` — lápis + popover + nome hiperlinkado (novas props `lpLinks`/`onSaveLpLink` ou equivalente).
- `packages/web/components/funnels/launch-dashboard.tsx` — `LpPerformanceSection` fornece `lpLinks` do stage e a mutation do PUT.
- Hook/mutation de update do stage (reusar o existente de edição de stages, se houver; senão mutation local com invalidation das queries do stage).

**OUT:**
- Auto-detecção de URL da LP (via GA4/Switchy/planilha) — fica para story futura se necessário.
- Links por funil (decidido por etapa) e links em outras tabelas (Criativos, Dados Diários).
- Preview/screenshot da LP no hover.
- Gestão de LPs como entidade (CRUD de LPs) — as LPs continuam derivadas do Campaign Name.

---

## 🧪 Definition of Done

- [x] Migration aplicada; coluna `lp_links` default `{}` em `funnel_stages`.
- [x] PUT de stage aceita/valida/persiste/retorna `lpLinks` (URL http(s) ou vazio p/ remover); demais campos intactos.
- [x] Lápis por linha + popover salvar/cancelar/remover funcionando com update sem reload.
- [x] Nome da LP hiperlinkado (nova aba, `noopener noreferrer`) quando há link; texto puro quando não.
- [x] Escopo por etapa verificado (Paga e Gratuita independentes — `lpLinks` lido/gravado por `stageId`).
- [x] Regressão zero na tabela sem links; `tsc --noEmit` API+web verdes; `next build` verde.

---

## 📝 Dev Notes

- **Persistência (IDS/REUSE):** seguir o padrão das colunas JSONB de config de `funnelStages` (`campaigns` `:659`, `switchyLinkedLinks` `:675`). O PUT `funnel-stages.ts:357-413` já faz partial update campo-a-campo — adicionar `lpLinks` nas linhas ~385-393 e no `stageShape`.
- **Migrations:** conferir o runner usado pelas migrations recentes (`0078_stage_sales_order_bumps.sql`, `0079_stage_operational_costs.sql`) e replicar o processo.
- **Frontend:** `LpPerformanceSection` (`launch-dashboard.tsx:1330`) já recebe `projectId`/`funnelId`/`stageId` — buscar o `lpLinks` do stage (ver de onde o dashboard já carrega os stages para não criar fetch novo) e passar à tabela junto com callback de save. A tabela é client component puro (recebe `rows` via props).
- **Popover:** usar os primitives de UI existentes (`components/ui/` — popover/input/button, mesmos das outras tabelas). Ícone `Pencil`/`ExternalLink` do lucide-react (padrão do repo).
- **Chave normalizada:** `lpTotals` usa `entry.lpName.toLowerCase()` (`useLpPerformanceData.ts:94`) — usar a MESMA normalização para gravar/ler o link, senão "LPA" ≠ "lpa".
- **Não** aplicar imposto/métrica nova — story é só de metadado + UI.
- ⚠️ Verificar build local antes do PR (Vercel só mostra o 1º erro) — ver `feedback_verify_build_before_merge`.

---

## 📁 File List

| Arquivo | Mudança |
|---------|---------|
| `packages/api/src/db/schema.ts` | Coluna `lpLinks` (JSONB, default `{}`) em `funnelStages`, tipada `Record<string, string>` |
| `packages/api/src/db/migrations/0080_stage_lp_links.sql` | **Novo** — `ADD COLUMN IF NOT EXISTS lp_links` (additive + idempotente, aplicada em prod) |
| `packages/api/src/routes/funnel-stages.ts` | `updateStageSchema.lpLinks` (Zod: URL http(s) ou `""` p/ remover), handler normaliza chaves e descarta vazios, `stageShape` retorna `lpLinks` |
| `packages/shared/src/types/funnel.ts` | `FunnelStage.lpLinks: Record<string, string>` |
| `packages/web/lib/hooks/use-funnel-stages.ts` | `CreateStageInput.lpLinks?` (o PUT do `useUpdateStage` já repassa) |
| `packages/web/components/funnels/launch-dashboard.tsx` | `LpPerformanceSection`: `useFunnelStage` + `useUpdateStage` (REUSE, invalidation já embutida), merge por chave, props novas p/ tabela |
| `packages/web/lib/components/funnels/lp-performance-table.tsx` | `LpNameCell`: nome hiperlinkado (nova aba, `noopener noreferrer`) + lápis com popover (Salvar/Cancelar, vazio remove, validação http(s) inline) |

---

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-07-16 | @sm (River) | Story criada (6 ACs) após elicitação com usuário: edição por lápis inline na linha, escopo por etapa (JSONB em funnel_stages). |
| 2026-07-16 | @dev (Dex) | Status Ready → **InProgress**. Branch `feat/18.56-lp-links` (base `ae02fd3`, local sincronizado com origin/main pós-mudanças do Lucas). |
| 2026-07-16 | @dev (Dex) | Implementação completa (7 arquivos, ver File List). Migration 0080 aplicada em prod (idempotente). Gates: `tsc --noEmit` API+web verdes, `next build` verde. Decisões: (1) REUSE de `useFunnelStage`/`useUpdateStage` — invalidation do React Query já cobre o AC2 "sem reload"; (2) validação de URL dupla (frontend `isValidLpUrl` + Zod com regex `^https?://`) — protege contra `javascript:` (R2); (3) handler normaliza chave (trim+lowercase) e descarta valores vazios no gravado, então "vazio remove" sem estado especial; (4) props da tabela opcionais — outros usos (não há) e testes existentes não quebram (AC6). CodeRabbit pré-commit pulado — CLI não disponível neste ambiente (macOS; config é WSL). Status InProgress → **InReview**. |
| 2026-07-16 | @qa (Quinn) | QA gate `*qa-gate`: **PASS**. 6/6 ACs atendidos, validação de URL dupla verificada (27/27 asserts node script — bloqueia javascript:/data:/vbscript:/ftp:/protocol-relative), guest 403, `rel="noopener noreferrer"`, zero chamada Meta API (sync só em `body.campaigns`), tsc API+web verdes. 2 concerns low não-bloqueantes (PERF-001 fetch extra cacheado, TEST-001 sem vitest). Status InReview → **Done**. Gate: `docs/qa/gates/18.56-lp-links-manual.yml`. |
| 2026-07-16 | @po (Pax) | Validação `*validate-story-draft`: **GO — score 10/10**. Anti-alucinação (Artigo IV) OK: todas as referências de código conferidas e reais (funnel-stages.ts PUT:357, updateStageSchema:51, stageShape:98, partial update:385-393, guest:361; schema funnelStages campaigns:659/switchyLinkedLinks:675; lp-performance-table rows via props + TableCell lpName:116; launch-dashboard LpPerformanceSection:1322-1336 recebe projectId/funnelId/stageId; useLpPerformanceData toLowerCase:94; migrations 0078/0079 existem, padrão additive/idempotente). Status Draft → Ready. |

---

## QA Results

### Review Date: 2026-07-16

### Reviewed By: Quinn (Test Architect & Quality Advisor)

**Escopo revisado:** `git diff ae02fd3..5ddff6f` (commit `5ddff6f`, único da story). Diff uncommitted em `mind-registry.ts` e untracked `18.54.story 2.md` ignorados (fora de escopo).

#### 7 Quality Checks

1. **Code review — PASS.** Backend REUSE limpo do padrão partial-update; frontend `LpNameCell` isola nome+lápis+popover com os UI primitives do repo (`popover`/`input`/`button`), estado local (`open`/`draft`/`saving`/`error`) bem contido, `useCallback` no handler de save. Comentários referenciam os ACs. Sem `dangerouslySetInnerHTML`, sem lógica duplicada.
2. **Tests — PASS (com débito low).** Sem runner no `packages/web` (TEST-001, herdado). Validação de URL verificada estaticamente (tsc) e via script node avulso: **27/27 asserts verdes** (13 backend Zod + 12 frontend `isValidLpUrl` + cap de chave 100 chars).
3. **Acceptance Criteria — PASS.** AC1 (coluna JSONB default `{}`, Zod record, `stageShape` retorna, guest 403), AC2 (Pencil+Popover, PUT via `useUpdateStage`, invalidation cobre "sem reload", vazio remove), AC3 (`<a target=_blank rel=noopener noreferrer>` + `ExternalLink`, match por `trim().toLowerCase()`), AC4 (bloqueio inline + Salvar disabled), AC5 (por `stageId`), AC6 (props opcionais). Todos conferidos contra o código.
4. **No regressions — PASS.** Props `lpLinks`/`onSaveLpLink` opcionais; sem `onSaveLpLink` a célula degrada para texto puro (pencil não renderiza). PUT partial update inalterado — body sem `lpLinks` não toca o campo. `stageShape` só adiciona campo (não quebra consumidores do `FunnelStage`).
5. **Performance — CONCERN (low).** `useFunnelStage` adiciona 1 GET por seção só para ler `lpLinks`, não deduplicado contra o cache plural (`funnel-stages`). Cacheado 2min, payload de 1 linha — impacto baixo (PERF-001).
6. **Security — PASS.** Validação http(s) em dupla camada (frontend + Zod `.url()`+regex `^https?://`) bloqueia `javascript:`/`data:`/`vbscript:`/`ftp:`/protocol-relative/schemeless. `rel="noopener noreferrer"` presente. Guest bloqueado (403) antes da validação. Zod server-side obrigatório. **Zero chamada à Meta API** — `fireBackgroundMetaSync` só dispara em `body.campaigns !== undefined`; PUT lpLinks-only é rate-limit safe.
7. **Documentation — PASS.** File List, Change Log e DoD completos e consistentes com o diff.

**Concerns (ambos low, não-bloqueantes):** PERF-001 (fetch extra cacheado), TEST-001 (sem vitest no monorepo).

### Gate Status

Gate: PASS → docs/qa/gates/18.56-lp-links-manual.yml
