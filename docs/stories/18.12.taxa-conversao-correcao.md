---
id: 18.12
epic: 18
title: "Corrigir Fórmula de Taxa de Conversão (Tx Conv.)"
description: |
  Corrigir o cálculo de Taxa de Conversão na tabela Dados Diários.
  Fórmula correta: Tx Conv. = Leads pagos / Link Clicks (não LP Views / Link Clicks)
  E atualizar o tooltip/hover com a explicação corrigida.
status: InProgress
story_type: Bug
complexity: Small (2 pontos)

---

## Story

### Problema
Atualmente a Taxa de Conversão (Tx Conv.) está sendo calculada como:
- `Tx Conv = Total Leads / LP Views × 100`

Mas a métrica correta deve ser:
- `Tx Conv = Leads Pagos / Link Clicks × 100`

Isso reflete melhor a taxa real de conversão de cliques em leads de qualidade (pagos).

### Solução Proposta
1. Alterar a fórmula de cálculo de Tx Conv. em `buildDailyRows()` e `computeTotals()`
2. Atualizar o tooltip/hover na tabela para explicar: "Leads Pagos ÷ Link Clicks × 100"
3. Garantir que a fórmula seja consistente em todas as visualizações

### User Story
**Como** gestor de tráfego,  
**Eu quero** ver a Taxa de Conversão corrigida (Leads Pagos / Cliques),  
**Para que** entenda melhor qual % de cliques resulta em leads de qualidade.

---

## Acceptance Criteria

### AC-1: Fórmula Corrigida
**Given** dados de Leads Pagos e Link Clicks disponíveis  
**When** calcula Taxa de Conversão  
**Then** usa fórmula `(Leads Pagos / Link Clicks) × 100`

### AC-2: Divisão por Zero
**Given** Link Clicks = 0  
**When** calcula Taxa de Conversão  
**Then** retorna null (exibe "—" na tabela)

### AC-3: Tooltip Atualizado
**Given** usuário passa mouse sobre coluna "Tx Conv."  
**When** hover aparece  
**Then** exibe tooltip: "Leads Pagos ÷ Link Clicks × 100"

### AC-4: Acumulado Correto
**Given** múltiplos dias com dados  
**When** calcula acumulado do período  
**Then** usa fórmula `(Total Leads Pagos / Total Link Clicks) × 100`

---

## Scope

### IN
- Correção do cálculo de Taxa de Conversão
- Atualização do tooltip/hover
- Aplicação em `buildDailyRows()` e `computeTotals()`
- Consistência em todas as tabelas

### OUT
- Mudanças em outras métricas
- Histórico de dados antigos
- Sincronização com API

---

## Dependencies

**Prerequisite:**
- Dados de Leads Pagos e Link Clicks (já disponíveis)

---

## Dev Notes

**Contexto:** Epic 18 — Dashboard Improvements / Bug Fix  
**Branch:** `fix/18.12-taxa-conversao` (será criado a partir de main)  
**Model:** Claude Haiku 4.5

**Notas:**
- Bug similar ao da Story 18.9 (Connect Rate também estava com fórmula errada)
- Revisar se outras métricas na tabela estão corretas também

---

## Criteria of Done

- [x] Fórmula corrigida em buildDailyRows()
- [x] Fórmula corrigida em computeTotals()
- [x] Divisão por zero tratada
- [x] Tooltip/hover atualizado
- [x] Typecheck/Lint PASS
- [ ] Visualmente testado

---

## Change Log

| Data | Autor | Alteração |
|------|-------|-----------|
| 2026-04-21 | Dex (@dev) | 🔨 Implementada — Fórmula corrigida de Tx Conv. em `buildDailyRows()` e `computeTotals()`: agora usa `(leadsPagos / linkClicks) × 100` ao invés de `(totalLeads / lpView) × 100`. Tooltip adicionado ao header com explicação. Typecheck/Lint PASS. Aguardando teste visual. |
| 2026-04-21 | River (@sm) | 🌊 Story criada em Draft |
