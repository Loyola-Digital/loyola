# Story 29.20 — Detalhamento: subcontagem por nome repetido (M1) + CTR/CPC link em linha mesclada (M2)

**Epic:** 29
**Status:** InReview
**Points:** 5
**Tipo:** Bug / Data Correctness
**Prioridade:** P2
**Origem:** QA Gate `docs/qa/29.10-29.19-perpetual-gate.md` (findings M1, M2)

## Contexto

Durante o QA gate do batch 29.10–29.19, o review adversarial encontrou 2 problemas reais de contabilização na tabela Detalhamento, diferidos pra não travar o ship das melhorias já validadas.

## Problema M1 — Subcontagem quando adset/ad têm nome igual (Média)

O backend (`packages/api/src/services/traffic-analytics.ts:719-756` adsets, ~789 ads) agrega **por nome** (`key = adset_name.trim()` / `ad_name.trim()`) e fixa `id: a.adset_id`/`a.ad_id` **do primeiro visto**. No frontend, `overlaySpreadsheetMetrics` (`perpetual-dashboard.tsx:151`) casa `byId.get(r.campaignId)` — só o id do primeiro. Vendas da planilha cujo `utm_medium`/`utm_content` apontam pra **outro id de mesmo nome** somem da linha do Detalhamento.

**Sintoma:** a mesma entidade mostra Receita/Vendas menor no Detalhamento do que no gráfico "Receita por Público/Criativo" ao lado (que re-agrupa **todos** os `porUtmMedium`/`porUtmContent` por nome). Dois números diferentes na mesma tela.

## Problema M2 — CTR/CPC "(link)" errado em linha mesclada (Baixa)

O merge do 29.19 (`perpetual-dashboard.tsx` ~585) recalcula `ctr = clicks/impressions` e `cpc = spend/clicks` de **cliques totais**; linhas não-mescladas mantêm o valor do backend (que usa **link clicks**). O header diz "(link)". Então linha com "— Cópia" mesclada mostra CTR/CPC de base diferente sob rótulo que promete link.

## Acceptance Criteria

- [x] **AC1 (M1):** No filtro Público/Criativo, vendas da planilha de **todos** os ids que compartilham o mesmo nome são somadas na linha correspondente. **Solução (b):** overlay soma por nome resolvido (`salesByAdsetName`/`salesByAdName`), cruzando `adsetNamesMap`/`adNamesMap`.
- [x] **AC2 (M1):** A linha do Detalhamento bate com o gráfico "Receita por Público/Criativo" (ambos agrupam por nome resolvido agora).
- [x] **AC3 (M2):** Linha mesclada re-deriva CTR/CPC de **link clicks** somados (fallback pra cliques totais), replicando exatamente `buildAnalyticsRow` do backend.
- [x] **AC4:** Campanhas seguem por id (não sofrem — não são agregadas por nome). Sem regressão em id único; typecheck + build OK.

## Tasks

- [x] 1. Decidir estratégia M1 → overlay por nome no front (sem mexer no backend).
- [x] 2. Implementar M1: `overlaySpreadsheetMetrics` generalizado com `keyOf`; mapas `salesByAdsetName`/`salesByAdName`; `funnelAdSets`/`funnelAds` casam por `campaignName`.
- [x] 3. Implementar M2: soma `linkClicks` no merge e deriva CTR/CPC deles (fallback total).
- [x] 4. Typecheck + build OK.

## Dev Notes

- Referência do agregador por nome: `packages/api/src/services/traffic-analytics.ts:719` (adsets), `:789` (ads).
- `overlaySpreadsheetMetrics`: `packages/web/components/funnels/perpetual-dashboard.tsx:151`.
- Merge/`detailRows`: mesmo arquivo, ~565-596.
- Campanha (utm_campaign) provavelmente não sofre tanto porque ids de campanha raramente colidem de nome — confirmar.

## File List

- `packages/web/components/funnels/perpetual-dashboard.tsx` *(modified)* — `overlaySpreadsheetMetrics` com `keyOf`; `salesByAdsetName`/`salesByAdName` (soma por nome resolvido); `funnelAdSets`/`funnelAds` casam por nome (M1); merge do `detailRows` soma `linkClicks` e deriva CTR/CPC deles (M2).

## Change Log

| Data | Quem | Mudança |
|---|---|---|
| 2026-07-06 | @qa Quinn | Story criada a partir dos findings M1/M2 do QA gate do batch 29.10–29.19. Status: Draft. |
| 2026-07-06 | @dev Dex | M1 resolvido (overlay adset/ad por nome resolvido, soma ids de mesmo nome → bate com o gráfico de barras). M2 resolvido (merge deriva CTR/CPC de link clicks igual buildAnalyticsRow). Campanhas seguem por id. Typecheck + build OK. Status: Draft → InReview. |
