# Story 9.6: Creative Metadata — CTA Type + Landing Page URL

---

## Status

**Ready for Dev**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [code_review, ui_validation]
priority: P2
estimate: S
depends_on: ["9.5"]
epic: "EPIC-9-ADS-DASHBOARD-2.0"
wave: 2
moscow: MUST
```

---

## Story

**As a** gestor de tráfego da Loyola Digital,
**I want** to see the CTA button type and landing page URL for each ad creative,
**so that** I can understand the full ad context (where users are directed and what action is requested) without opening the Meta Ads Manager.

---

## Acceptance Criteria

### CTA Type Display

1. [ ] Show CTA type as a styled badge in the Creative Gallery cards (e.g., "Saiba Mais", "Compre Agora")
2. [ ] Show CTA type in the lightbox metadata section
3. [ ] Map Meta API `ctaType` values to PT-BR labels: `LEARN_MORE` → "Saiba Mais", `SHOP_NOW` → "Compre Agora", `SIGN_UP` → "Cadastre-se", `WATCH_MORE` → "Assistir Mais", `CONTACT_US` → "Fale Conosco", `APPLY_NOW` → "Inscreva-se", `BOOK_TRAVEL` → "Reserve", `DOWNLOAD` → "Baixar", `GET_OFFER` → "Ver Oferta", `GET_QUOTE` → "Pedir Orçamento", `SUBSCRIBE` → "Assinar", `BUY_NOW` → "Comprar", `ORDER_NOW` → "Pedir Agora", others → show raw value capitalized

### Landing Page URL

4. [ ] In the lightbox, show the landing page URL below the CTA badge as a clickable link with external icon
5. [ ] Truncate URL display to domain + first path segment (e.g., "loyoladigital.com.br/lp-ingles") with full URL on hover/title
6. [ ] Open in new tab with `rel="noopener noreferrer"`
7. [ ] If `linkUrl` is null, don't show the URL section (some lead form ads have no external URL)

### Gallery Enhancement

8. [ ] In Creative Gallery cards, show CTA badge in the info section below the thumbnail (small, subtle)
9. [ ] In drill-down ad rows, show CTA type as a small badge next to the creative type badge

---

## Dev Notes

- Both `ctaType` and `linkUrl` are already in `MetaAdCreative` interface and already fetched from the Meta API — this is purely a frontend display story
- No API changes needed
- The CTA mapping should be a simple constant object, not a service call
- Some `ctaType` values from Meta: `LEARN_MORE`, `SHOP_NOW`, `SIGN_UP`, `WATCH_MORE`, `CONTACT_US`, `APPLY_NOW`, `BOOK_TRAVEL`, `DOWNLOAD`, `GET_OFFER`, `GET_QUOTE`, `SUBSCRIBE`, `BUY_NOW`, `ORDER_NOW`, `NO_BUTTON`, `MESSAGE_PAGE`, `WHATSAPP_MESSAGE`
- If `ctaType` is `NO_BUTTON` or null, don't show any badge

---

## Tasks

- [ ] 1. Create CTA_LABELS constant mapping Meta ctaType → PT-BR display labels
- [ ] 2. Add CTA badge to Creative Gallery cards
- [ ] 3. Add CTA badge + landing page URL to lightbox (from Story 9.5)
- [ ] 4. Add CTA badge to drill-down ad rows (next to creative type badge)

---

## File List

| File | Action | Description |
|------|--------|-------------|
| `packages/web/app/(app)/traffic/page.tsx` | Modified | Add CTA labels, badges, URL display |

---

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-03-23 | Story drafted | @sm (River) |

---

## Dev Agent Record

### Debug Log

_(to be filled by @dev)_

### Completion Notes

_(to be filled by @dev)_

### Agent Model Used

_(to be filled by @dev)_
