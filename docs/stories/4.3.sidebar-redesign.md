# Story 4.3: Sidebar Redesign — Project Folders (ClickUp-style)

---

## Status

**Ready for Review**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools: [ux_review, a11y_check]
priority: P1
estimate: M
depends_on: ["4.2"]
epic: "EPIC-4-MULTI-PROJECT-STRUCTURE"
```

---

## Story

**As a** Loyola Digital employee,
**I want** a sidebar with collapsible project folders,
**so that** I can navigate per-client resources quickly without losing access to global views.

---

## Acceptance Criteria

1. [ ] Seção "Global" mantida no topo: Dashboard (`/`), Conversas (`/conversations`), Tasks (`/tasks`), Instagram (`/instagram`), Settings (`/settings`)
2. [ ] Separador visual entre seção Global e seção Projetos
3. [ ] Seção "Projetos": cada projeto é um folder colapsável com chevron toggle
4. [ ] Folder exibe cor do projeto como dot indicator + nome do projeto
5. [ ] Estado de colapso de cada projeto persistido em `localStorage` (key: `project-folder-{id}`)
6. [ ] Subitens visíveis quando expandido: 📊 Instagram → `/projects/[id]/instagram`, 💬 Conversas → `/projects/[id]/conversations`
7. [ ] Item ativo destacado (variant `secondary`)
8. [ ] Botão "+ Novo Projeto" ao fim da lista → abre `CreateProjectDialog`
9. [ ] `CreateProjectDialog`: campos `name`, `clientName`, `color` (color picker simples — 6 cores predefinidas) + botão Criar
10. [ ] Loading skeleton enquanto projetos carregam
11. [ ] Empty state inline quando sem projetos: "Nenhum projeto. Crie o primeiro."
12. [ ] Responsivo: comportamento de sheet no mobile mantido
13. [ ] Rota `/projects/[id]` criada — overview do projeto (nome, clientName, cor, lista de contas vinculadas)
14. [ ] `useProjects()` hook TanStack Query para listar projetos

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **Sidebar atual:** `packages/web/components/layout/app-sidebar.tsx` — refatorar mantendo comportamento desktop/mobile [Source: packages/web/components/layout/app-sidebar.tsx]
- **State:** Zustand `ui-store` para sidebarOpen — manter; adicionar localStorage para collapse de folders
- **Components:** shadcn/ui `Collapsible` para folders (verificar se já instalado, se não: `npx shadcn@latest add collapsible`)
- **API Client:** padrão `useApiClient()` + TanStack Query — igual a `use-instagram-accounts.ts`

### UI Design Reference

```
[Global]              ← label de seção
  🏠 Dashboard
  💬 Conversas
  ✅ Tasks
  📊 Instagram
  ⚙️  Settings
──────────────        ← Separator
[Projetos]            ← label de seção
  ▶ ● Cliente XYZ    ← chevron + dot colorido + nome (colapsado)
  ▼ ● Cliente ABC    ← expandido
    📊 Instagram
    💬 Conversas
  + Novo Projeto
```

### Key Notes

- `Collapsible` do shadcn/ui já é dependência do projeto (instalado em EPIC-1) — verificar
- Color picker: 6 cores predefinidas como swatches (não input hex livre) para simplicidade
- `project-folder-{id}` no localStorage: `"open"` ou `"closed"`

---

## Testing

### Validation Commands

```bash
cd packages/web
pnpm lint
pnpm typecheck
```

### Test Scenarios

1. Sidebar carrega com projetos → exibe folders
2. Clicar chevron → colapsa/expande folder
3. Recarregar página → estado de colapso preservado (localStorage)
4. Clicar subitem Instagram → navega para `/projects/[id]/instagram`
5. "+ Novo Projeto" → dialog abre, preencher → projeto aparece na lista
6. Sem projetos → empty state exibido
7. Loading → skeleton visível

---

## Tasks / Subtasks

- [x] **Task 1: Hook useProjects** (AC: 14)
  - [x] 1.1 Criar `packages/web/lib/hooks/use-projects.ts` — `useProjects()`, `useCreateProject()`, `useDeleteProject()`

- [x] **Task 2: Componente ProjectFolder** (AC: 3, 4, 5, 6, 7)
  - [x] 2.1 Criar `packages/web/components/layout/project-folder.tsx`
  - [x] 2.2 Collapsible com chevron e dot de cor
  - [x] 2.3 Persistir estado no localStorage

- [x] **Task 3: CreateProjectDialog** (AC: 8, 9)
  - [x] 3.1 Criar `packages/web/components/layout/create-project-dialog.tsx`
  - [x] 3.2 Campos: name, clientName, color (6 swatches predefinidos)
  - [x] 3.3 Chamar `useCreateProject()` mutation

- [x] **Task 4: Refatorar app-sidebar.tsx** (AC: 1, 2, 10, 11, 12)
  - [x] 4.1 Adicionar seção "Projetos" abaixo do Separator
  - [x] 4.2 Loading skeleton e empty state
  - [x] 4.3 Renderizar `<ProjectFolder>` para cada projeto
  - [x] 4.4 Botão "+ Novo Projeto"
  - [x] 4.5 Manter comportamento mobile (Sheet)

- [x] **Task 5: Project overview page** (AC: 13)
  - [x] 5.1 Criar `packages/web/app/(app)/projects/[id]/page.tsx` — exibe nome, clientName, cor, e lista de contas Instagram do projeto
  - [x] 5.2 Criar `packages/web/app/(app)/projects/[id]/layout.tsx` — layout base

- [x] **Task 6: Lint e typecheck** (AC: todos)
  - [x] 6.1 `pnpm lint` — zero warnings/erros
  - [x] 6.2 `pnpm typecheck` — zero erros

---

## File List

> Atualizado pelo @dev durante implementacao

| File | Status |
|------|--------|
| `packages/web/lib/hooks/use-projects.ts` | Created |
| `packages/web/components/layout/project-folder.tsx` | Created |
| `packages/web/components/layout/create-project-dialog.tsx` | Created |
| `packages/web/components/layout/app-sidebar.tsx` | Modified |
| `packages/web/app/(app)/projects/[id]/page.tsx` | Created |
| `packages/web/app/(app)/projects/[id]/layout.tsx` | Created |

---

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log

- `Skeleton` e `Dialog`/`Input`/`Label` confirmados como já instalados no projeto antes de criar os componentes
- `Collapsible` do shadcn/ui confirmado como já instalado (dependência do EPIC-1)
- `pnpm typecheck` passou com zero erros

### Completion Notes

- `use-projects.ts` criado com `useProjects`, `useCreateProject`, `useDeleteProject` — padrão idêntico a `use-instagram-accounts.ts`
- `project-folder.tsx` Collapsible com chevron animado, dot colorido, subitems Instagram/Conversas, localStorage persistence por `project-folder-{id}`
- `create-project-dialog.tsx` com 6 swatches de cor predefinidos, campos name + clientName obrigatórios
- `app-sidebar.tsx` refatorado: seção Global mantida, separator, seção Projetos com skeleton/empty state/folders/botão "+ Novo Projeto"
- `projects/[id]/page.tsx` exibe nome, clientName, cor e lista de contas Instagram via `GET /api/projects/:id/instagram/accounts`
- `pnpm lint` e `pnpm typecheck` — zero erros

### Change Log

- `packages/web/lib/hooks/use-projects.ts`: criado com 3 hooks TanStack Query
- `packages/web/components/layout/project-folder.tsx`: componente Collapsible com localStorage
- `packages/web/components/layout/create-project-dialog.tsx`: dialog com color swatches
- `packages/web/components/layout/app-sidebar.tsx`: adicionada seção Projetos
- `packages/web/app/(app)/projects/[id]/page.tsx`: overview do projeto
- `packages/web/app/(app)/projects/[id]/layout.tsx`: layout base
