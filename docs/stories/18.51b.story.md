# Story 18.51b: Captação Paga — Dados Diários com Ingressos Únicos/Totais, Faturamento Único/Total, Ticket Médio e Tooltips

**Epic:** 18 — Dashboard Improvements
**Status:** Ready
**Created:** 2026-07-13 (@po — Pax, split de [18.51](18.51.story.md))
**Priority:** High
**Parent:** [18.51](18.51.story.md)
**Depends On:** **18.51a** (consome os campos do backend)

<!-- clickup:task_id -->

---

## 📖 Escopo desta fatia

Refatorar a tabela **Dados Diários** (`crossed-funnel-daily-table.tsx`) da etapa Paga: renomear/adicionar colunas de ingressos e faturamento, colunas de ticket médio, tooltips em todos os cabeçalhos, e corrigir o bug de rótulo "Ingressos" sobre leads.

Ler a **story-pai [18.51](18.51.story.md)** para glossário e definições de fórmula.

---

## ✅ Acceptance Criteria

**AC1b.1** — Renomear coluna **"Total Ingressos" → "Ingressos únicos"** (`crossed-funnel-daily-table.tsx:181`).

**AC1b.2** — Nova coluna **"Ingressos totais"** imediatamente à direita de "Ingressos únicos" (todos os produtos do dia, sem dedup).

**AC1b.3** — Tooltip do cabeçalho "Ingressos totais" detalha **ingressos por produto** (ex.: `Imersão: 120 · ChatGPT+Gravação: 45 …`), vindo de `ingressosPorProduto`.

**AC1b.4** — Footer soma ambas as colunas corretamente.

**AC1c.1** — Substituir a coluna "Faturamento" (linha 307) por **"Faturamento único"** (captação, dedup e-mail, por dia) + **"Faturamento Total"** (todos produtos, sem dedup, por dia).

**AC1c.2** — Footer soma ambas corretamente.

**AC1d.1** — À direita de **"CPL Geral"** (linha 315): **"Ticket médio (únicos)"** = `Fat. único ÷ Ingressos únicos` e **"Ticket médio (total)"** = `Fat. Total ÷ Ingressos totais` (por dia; footer = agregados).

**AC1d.2** — Divisão por zero → `—` (nunca `NaN`/`Infinity`).

**AC1e.1** — **Todos** os cabeçalhos da tabela têm tooltip explicando o cálculo (fórmula + fonte). Reusar padrão `cursor-help` + `title` (linhas 308-311, 323). Lista completa na pai (AC1e.1).

**AC-BUG.1** — Etapa Paga **sem** planilha de vendas conectada (`usesIngressos === false`, linha 169): **Opção A (decisão @po)** — manter rótulos "Ingressos …" mas células com `—` e hint "Conecte a planilha de vendas para ver ingressos". **Não** reverter para "Leads …".

---

## 🧪 Definition of Done

- [ ] Colunas na ordem: … Faturamento único · Faturamento Total · Ingressos únicos · Ingressos totais · CPL Pg · CPL Geral · Ticket médio (únicos) · Ticket médio (total) · …
- [ ] Todos os cabeçalhos com tooltip de cálculo.
- [ ] Footer soma correta em todas as novas colunas.
- [ ] Tabela horizontalmente scrollável sem quebrar layout (já é `overflow-x-auto`, linha 301).
- [ ] Bug: sem planilha → nenhum número de leads sob rótulo "Ingressos".
- [ ] `tsc --noEmit` + `next build` verdes ([[feedback_verify_build_before_merge]]).

---

## 🛠️ Dev Notes

`crossed-funnel-daily-table.tsx`: labels 180-189, header 305-326, body 357-378, footer 384-411. Adicionar props para `ingressosUnicosByDay`, `ingressosTotaisByDay`, `faturamentoUnicoByDay`, `faturamentoTotalByDay`, `ingressosPorProduto` (vindos de `salesData` via `launch-dashboard.tsx:471-483`). Reusar `fmtCurrency`/`fmtInt` (60-77). Ver pai para detalhes.

⚠️ Muitas colunas — validar legibilidade/scroll com @ux se necessário.

---

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-07-13 | @po (Pax) | Criada Ready (split de 18.51). |
