# Story 29.24: Auditoria e correção do cálculo de ROAS em todas as etapas (denominador com imposto, consistência de fonte)

**Epic:** 29 — Perpétuo (melhorias) / transversal
**Status:** Ready
**Created:** 2026-07-21 (@sm — River)
**Priority:** Medium-High
**Depends On:**
- 29.9 (imposto 12,15% no spend — `applyMetaTax`)
- 29.20 (ROAS usa receita BRUTA; Margem usa receita LÍQUIDA — divergência intencional a preservar)
- 18.x / Meta Ads (fórmulas de ROAS do launch e dos serviços de traffic-analytics)

<!-- clickup:pending — registrar task "[Story 29.24] Auditoria de ROAS em todas as etapas" na list APP - Loyola X -->

---

## 📖 Contexto

O usuário pediu **conferência do cálculo de ROAS em todas as etapas**. A investigação de código encontrou a fórmula **conceitualmente consistente em todo lugar** — `ROAS = Receita (bruta) ÷ Investimento`, sempre com guarda `spend > 0 → null` (renderiza "—") — **mas com uma divergência potencial no denominador**: o agregado do perpétuo usa `effectiveSpend` (**com imposto 12,15%**), enquanto alguns pontos por linha/serviço usam `spend` **sem imposto**. Como o card de Investimento e a Margem usam spend **com** imposto, um ROAS calculado sobre spend **sem** imposto fica **superestimado** e inconsistente com o resto do dashboard.

Esta é uma story de **auditoria + correção**: mapear cada ponto onde ROAS é calculado, definir a fórmula canônica e alinhar todos os pontos divergentes (ou documentar formalmente as divergências intencionais).

### 🔬 Pontos de cálculo de ROAS mapeados (no código)

| # | Local | Fórmula atual | Denominador tem imposto? |
|---|-------|---------------|--------------------------|
| P1 | `perpetual-dashboard.tsx:706` (agregado perpétuo) | `revenue / effectiveSpend` | ✅ Sim (`effectiveSpend`) |
| P2 | `perpetual-dashboard.tsx:883` (por linha da tabela Detalhamento) | `revenue / spend` | ❓ **Verificar** se `spend` já tem tax |
| P3 | `perpetual-dashboard.tsx:175` (override por planilha, match UTM) | `match.bruto / r.spend` | ❓ **Verificar** |
| P4 | `traffic-analytics.ts:412` (agregado geral, backend) | `totalRevenue / totalSpend` | ❓ **Verificar** (`totalSpend` do Meta é sem tax) |
| P5 | `traffic-analytics.ts:877` (por campanha, backend) | `saleData.revenue / spend` | ❓ **Verificar** |
| P6 | `sales-daily-sync.ts:78` (ROAS por temperatura hot/cold) | `bruto / spend(hot/cold)` | ❓ **Verificar** |
| P7 | `meta-ads.ts:208-215` (memorial Meta Ads) | `revenue / spend` | ❓ **Verificar** |
| — | `formulas/funnels.ts:131-133` (`buildFunnelRoasFormula`) | texto "Revenue ÷ Spend" | (rótulo/memorial) |

**Divergência intencional a preservar (29.20):** ROAS usa receita **bruta**; Margem usa receita **líquida** (após fees). Isso **não** é bug — a auditoria deve manter e documentar.

---

## 🗳️ Decisões de escopo

| Dúvida | Decisão |
|--------|---------|
| Fórmula canônica de ROAS | **ROAS = Receita Bruta ÷ Investimento (spend COM imposto 12,15%)** em todas as etapas onde o imposto se aplica. O denominador do ROAS deve ser o **mesmo** Investimento exibido nos cards/tabelas. |
| Receita bruta vs líquida no numerador | **Bruta** (mantém 29.20). ROAS ≠ Margem por design. |
| Guarda de divisão | Manter `spend > 0 ? ... : null` (renderiza "—"), padrão já existente. |
| Divergências que forem intencionais | **Documentar** (comentário no código + nota na story), não "corrigir". |

---

## ✅ Acceptance Criteria

**AC1 — Auditoria documentada de todos os pontos**
**Given** os pontos P1–P7 (e quaisquer outros encontrados via busca por `roas`/`ROAS`), **when** a auditoria roda, **then** cada ponto é classificado como **✅ correto**, **⚠️ divergente-a-corrigir** ou **🔵 divergente-intencional-a-documentar**, com evidência (arquivo:linha, valor do denominador — com/sem imposto).
- Resultado registrado numa tabela nesta story (seção QA/Dev Notes) ou em `docs/qa/` — rastreável.

**AC2 — Denominador com imposto onde aplicável**
**Given** cada ponto classificado como ⚠️ divergente, **when** corrigido, **then** o ROAS passa a usar Investimento **com imposto 12,15%** (mesma base do card de Investimento e da fórmula do agregado `perpetual-dashboard.tsx:706`), respeitando a data de vigência do imposto (`META_TAX_EFFECTIVE_DATE`, 29.9).
- **Foco confirmado:** P2 (`:883`) e P3 (`:175`) por linha da tabela Detalhamento do perpétuo, se estiverem usando `spend` sem tax enquanto o agregado usa `effectiveSpend`.

**AC3 — Consistência agregado × soma das linhas**
**Given** a etapa de vendas do perpétuo, **when** o ROAS agregado (card) e os ROAS por linha (tabela Detalhamento) são exibidos, **then** são coerentes entre si: o agregado corresponde a `Σreceita ÷ Σinvestimento(c/ tax)`, não à média dos ROAS por linha, e nenhuma linha usa base de spend diferente do card.

**AC4 — Numerador bruto preservado (não virar líquido)**
**Given** a divergência intencional 29.20, **when** a auditoria corrige denominadores, **then** o **numerador continua sendo receita bruta** em todos os pontos de ROAS (a correção é só no denominador/imposto; ROAS **não** deve passar a usar receita líquida).

**AC5 — Memorial/tooltip reflete a fórmula real**
**Given** `buildFunnelRoasFormula` (`formulas/funnels.ts:131-133`) e o memorial Meta Ads (`meta-ads.ts:208-215`), **when** a base muda, **then** o texto do memorial/tooltip reflete "Receita bruta ÷ Investimento (com imposto 12,15%)" onde aplicável — sem mentir sobre a fonte.

**AC6 — Regressão zero**
**Given** as demais métricas (Margem, CAC, cards, gráficos, tabelas de launch), **when** o ROAS é alinhado, **then** só o ROAS dos pontos divergentes muda (para menor, por incluir imposto); Margem/CAC/Investimento/Faturamento permanecem idênticos.
- Gates: `pnpm --filter @loyola-x/api exec tsc --noEmit` e `pnpm --filter @loyola-x/web exec tsc --noEmit` verdes; `next build` (web) verde.

---

## 📊 Complexidade & Valor

- **Estimativa:** **S-M**. O grosso é auditoria/leitura (baixo risco) + correções cirúrgicas no denominador em poucos pontos. Risco de regressão baixo porque a mudança é localizada e para-menor (imposto no denominador).
- **Valor:** Alto em **confiança do número** — ROAS é a métrica de decisão nº1 do gestor. Um ROAS superestimado (sem imposto) leva a decisão errada de escala. Consistência agregado×linha evita "por que o card não bate com a soma?".

---

## ⚠️ Riscos

- **R1 (médio)** — Corrigir o denominador **abaixa** o ROAS exibido; pode assustar quem comparava com o número antigo. **Mitigação:** comunicar no PR/changelog que é correção de base (imposto), não queda de performance real.
- **R2 (baixo)** — Alterar `traffic-analytics.ts` (P4/P5) afeta **outras telas além do perpétuo** (launch, etc.). **Mitigação:** validar impacto por consumidor antes de mexer; se a etapa não aplica imposto, deixar como está e documentar (não forçar imposto onde não há).
- **R3 (baixo)** — Data de vigência do imposto (`META_TAX_EFFECTIVE_DATE`) precisa ser respeitada por dia. **Mitigação:** reusar `applyMetaTax`/`effectiveSpend` existente, nunca hardcodar 1.1215.

---

## 📦 Escopo

**IN:**
- `packages/web/components/funnels/perpetual-dashboard.tsx` — alinhar denominador do ROAS por linha (`:883`) e override planilha (`:175`) ao `effectiveSpend` (com imposto), se divergentes.
- `packages/api/src/services/traffic-analytics.ts` — auditar/corrigir P4 (`:412`) e P5 (`:877`) **apenas se** a etapa aplica imposto e diverge.
- `packages/api/src/services/sales-daily-sync.ts` — auditar P6 (`:78`, ROAS por temperatura).
- `packages/web/lib/formulas/funnels.ts` / `packages/web/lib/formulas/meta-ads.ts` — ajustar texto do memorial se a base mudar.
- Documento de auditoria (tabela P1–Pn com veredito) nesta story / `docs/qa/`.

**OUT:**
- Mudar o numerador de ROAS para receita líquida (proibido — 29.20).
- Redesenhar cards/tabelas de ROAS (só o cálculo/base, não a UI).
- Alterar a fórmula de Margem, CAC ou qualquer outra métrica.
- Aplicar imposto em etapas/contas onde ele comprovadamente não incide.

---

## 🧪 Definition of Done

- [ ] AC1 — Auditoria dos pontos P1–Pn documentada com veredito por ponto
- [ ] AC2 — Denominador com imposto nos pontos ⚠️ divergentes (respeitando vigência)
- [ ] AC3 — Agregado × soma das linhas coerentes no perpétuo
- [ ] AC4 — Numerador bruto preservado em todos os pontos
- [ ] AC5 — Memorial/tooltip reflete a fórmula real
- [ ] AC6 — Regressão zero (só ROAS divergente muda); `tsc --noEmit` (api+web) + `next build` verdes
- [ ] Validação visual: conferir ROAS de 1 campanha (card × linha) com imposto
- [ ] `File List` e `Change Log` atualizados
- [ ] CodeRabbit self-healing (se ambiente permitir)

## 🤖 CodeRabbit Integration

- **Story type:** Bug Fix / Refactor (correção de fórmula transversal)
- **Focus:** consistência do denominador (imposto), não introduzir divisão por zero, não alterar numerador, impacto cross-tela em `traffic-analytics.ts`.
- **Quality gate:** @dev self-healing (max 2) → @qa gate.

## 📝 Dev Notes

- **Comece pela auditoria (AC1) antes de mudar código.** Rode uma busca por `roas`/`ROAS` no repo inteiro (backend + frontend + formulas) e complete a tabela P1–Pn com o valor real do denominador em cada ponto (grep + leitura das linhas). Só então corrija os ⚠️.
- **Fonte da verdade do denominador:** `effectiveSpend` no perpétuo (`perpetual-dashboard.tsx`, agregado `:700`) já é `spend` com imposto via `applyMetaTax`. Por linha, verificar se `r.spend`/`spend` já passou por `applyMetaTax` — se não, aplicar a mesma transformação (nunca `* 1.1215` hardcoded; usar o helper e a data de vigência).
- **Não confundir etapas:** `traffic-analytics.ts` serve várias telas. Antes de forçar imposto lá, confirmar se aquela etapa exibe Investimento com imposto (se sim, ROAS deve casar; se não, não mexer).
- **Preservar 29.20:** ROAS = receita **bruta** ÷ investimento; Margem = receita **líquida** − investimento. A auditoria toca só o **denominador** do ROAS. Deixar um comentário no código reafirmando isso onde corrigir.
- ⚠️ **Verificar build antes de mergear** (memória `feedback_verify_build_before_merge`).

## 📁 File List

| Arquivo | Mudança | Status |
|---------|---------|--------|
| `packages/web/components/funnels/perpetual-dashboard.tsx` | ROAS por linha/override com denominador c/ imposto (se divergente) | ⏳ pendente |
| `packages/api/src/services/traffic-analytics.ts` | Auditoria P4/P5; correção só se etapa aplica imposto | ⏳ pendente |
| `packages/api/src/services/sales-daily-sync.ts` | Auditoria P6 (ROAS por temperatura) | ⏳ pendente |
| `packages/web/lib/formulas/funnels.ts` / `meta-ads.ts` | Texto do memorial de ROAS (se base mudar) | ⏳ pendente |
| `docs/qa/` (auditoria) | Tabela P1–Pn com veredito por ponto | ⏳ pendente |

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-07-21 | @sm (River) | Story criada (Draft) após mapeamento dos 7 pontos de cálculo de ROAS (perpétuo agregado/linha/override, traffic-analytics agregado/campanha, sales-daily-sync temperatura, memorial Meta Ads) e identificação da divergência de imposto no denominador |
| 2026-07-21 | @po (Pax) | Validação `validate-story-draft`: **GO — 10/10**. Anti-alucinação (Art. IV) OK — divergência **confirmada** no código: `:706` usa `revenue / effectiveSpend` (com imposto) mas `:883` usa `revenue / spend` e `:175` usa `match.bruto / r.spend` (a verificar se têm imposto). AC1 (auditoria-primeiro) mitiga o risco de correção às cegas. AC4 preserva numerador bruto (29.20). R2 (impacto cross-tela em `traffic-analytics.ts`) bem sinalizado. Status **Draft → Ready**. |
