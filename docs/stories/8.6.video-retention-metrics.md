# Story 8.6: Video Retention Metrics

---

## Status

**Draft**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [code_review, pattern_validation]
priority: P1
estimate: M
depends_on: ["8.1"]
epic: "EPIC-8-CREATIVE-INTELLIGENCE"
wave: 3
moscow: SHOULD
```

---

## Story

**As a** gestor de trafego da Loyola Digital,
**I want** to see video retention metrics (25%, 50%, 75%, 100% watched) as mini sparkline charts for video ads,
**so that** I can understand where viewers drop off and optimize video creative production.

---

## Acceptance Criteria

1. [ ] `fetchAdInsights()` atualizado — campos adicionais: `video_p25_watched_actions`, `video_p50_watched_actions`, `video_p75_watched_actions`, `video_p100_watched_actions`, `video_thruplay_watched_actions`
2. [ ] Novo tipo `VideoMetrics`: `{ views3s, p25, p50, p75, p100, thruplay }`
3. [ ] `MetaAdInsight` expandido com campo opcional `videoMetrics: VideoMetrics | null`
4. [ ] Componente `VideoRetentionChart`: mini area chart (Recharts) mostrando curva 25% → 50% → 75% → 100% — sparkline inline
5. [ ] No drill-down de ads: se ad e video, mostrar sparkline de retencao na linha (ao lado do thumbnail)
6. [ ] No Top Performers: se ad e video, mostrar sparkline dentro do card
7. [ ] Tooltip no sparkline: "25%: 5.2K views → 50%: 3.1K → 75%: 1.8K → 100%: 900"
8. [ ] Video metrics so sao puxadas para ads com `objectType === 'VIDEO'` (nao desperdicar requests)
9. [ ] `pnpm typecheck` e `pnpm lint` passam

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **Meta Ads service:** `packages/api/src/services/meta-ads.ts` — `fetchAdInsights()` precisa de campos extras
- **Traffic analytics:** `packages/api/src/services/traffic-analytics.ts` — propagar videoMetrics nos resultados
- **Dashboard:** Top Performers (8.2) e DrillDown (8.4) ja terao thumbnails — sparkline e adicionado ao lado

### Meta API — Video Fields

```
GET /act_{id}/insights?level=ad&fields=...,video_p25_watched_actions,video_p50_watched_actions,video_p75_watched_actions,video_p100_watched_actions,video_thruplay_watched_actions
```

Estes campos retornam arrays de `[{ action_type: "video_view", value: "5200" }]`. Parsear o `value` do primeiro item.

### Sparkline Component

```typescript
// Mini area chart — inline, sem eixos, apenas a curva
function VideoRetentionChart({ metrics }: { metrics: VideoMetrics }) {
  const data = [
    { label: "25%", value: metrics.p25 },
    { label: "50%", value: metrics.p50 },
    { label: "75%", value: metrics.p75 },
    { label: "100%", value: metrics.p100 },
  ];

  return (
    <ResponsiveContainer width={80} height={24}>
      <AreaChart data={data}>
        <Area type="monotone" dataKey="value" stroke="hsl(200 80% 60%)" fill="hsl(200 80% 60% / 0.2)" strokeWidth={1.5} />
        <Tooltip formatter={(v) => fmtNumber(Number(v))} />
      </AreaChart>
    </ResponsiveContainer>
  );
}
```

### Conditional Fetch

```typescript
// Apenas puxar video metrics para ads que sao VIDEO
// No fetchAdInsights: sempre incluir os campos de video
// No buildAnalyticsRow: parsear apenas se objectType === 'VIDEO'
// Se nao e video: videoMetrics = null
```

### Key Files

| File | Change |
|------|--------|
| `packages/api/src/services/meta-ads.ts` | Expand `fetchAdInsights()` fields com video metrics |
| `packages/api/src/services/traffic-analytics.ts` | Propagar videoMetrics nos resultados de ads/top performers |
| `packages/web/lib/hooks/use-traffic-analytics.ts` | +tipo `VideoMetrics`, expand tipos de ad |
| `packages/web/app/(app)/traffic/page.tsx` | +`VideoRetentionChart` sparkline, integrar em DrillDown e TopPerformers |

---

## Tasks

- [ ] **Task 1 (AC: 1, 2, 3, 8):** Backend — expandir `fetchAdInsights()` com campos de video, parsear para `VideoMetrics`, condicional por objectType
- [ ] **Task 2 (AC: 3):** Propagar `videoMetrics` em `getTopPerformers()` e `getProjectAdAnalytics()`
- [ ] **Task 3 (AC: 4, 7):** Componente `VideoRetentionChart` — sparkline com tooltip
- [ ] **Task 4 (AC: 5):** Integrar sparkline no drill-down de ads (ao lado do thumbnail)
- [ ] **Task 5 (AC: 6):** Integrar sparkline no Top Performers card (dentro do card)
- [ ] **Task 6 (AC: 9):** `pnpm typecheck` e `pnpm lint` passando

---

## Testing

- Video metrics retornados corretamente para ads de video
- Ads nao-video retornam `videoMetrics: null`
- Sparkline renderiza curva de retencao correta
- Tooltip mostra valores formatados por percentil
- Sparkline aparece no drill-down ao lado do thumbnail de video ads
- Sparkline aparece no card de Top Performers para video ads
- Nao aparece para ads de imagem/carousel
- typecheck e lint passam

---

## Dev Agent Record

### File List

| File | Status |
|------|--------|
| — | — |

### Debug Log

_(empty)_

### Change Log

- 2026-03-20: Story 8.6 criada por @sm (River) a partir do EPIC-8
