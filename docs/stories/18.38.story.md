# Story 18.38: Faturamento da etapa de vendas inclui TMB + cache Meta 1h/permanente

**Epic:** 18  
**Status:** Ready for Review  
**Assignee:** @dev  
**Branch:** feature/18.38-faturamento-tmb-cache

---

## Story

Dois ajustes na **etapa de Vendas**:

1. **Card de Faturamento/Vendas do topo não contava TMB.** O `SalesMetaKpis` (topo) mostrava `R$ 207.500 / 55 vendas` (só Produto Principal + PIX), enquanto a tabela unificada abaixo mostrava `R$ 208.643,61 / 58` (Produto Principal + TMB + PIX). Diferença = 3 vendas de TMB (R$ 1.143,61).
2. **Cache Meta do dash de vendas reconsultava demais.** Deve consultar a Meta só se não tiver info do dia OU se a info salva do dia passou de 1h; qualquer dia passado já no banco não reconsulta.

## Acceptance Criteria

### AC1: Faturamento/Vendas do topo = Produto Principal + TMB
- [x] Endpoint `sales-data` aceita `subtype` CSV (ex.: `main_product,tmb`) e agrega via `inArray`.
- [x] Venda manual (PIX) continua somada **uma única vez** (independe de subtype) — sem duplicar.
- [x] `SalesMetaKpis` passa a pedir `"main_product,tmb"`.
- [x] TMB entra em Faturamento, Vendas **e** ROAS/CPV/margem (decisão do usuário: receita real do mesmo comprador pago).

### AC2: Cache Meta — hoje=1h, dias passados=permanente
- [x] `ttlCutoffForDate` (meta-insights-cache.ts): dia atual reconsulta só se a linha salva passou de 1h; qualquer dia passado já gravado serve do banco (sem reconsultar).
- [x] Removido o TTL de 24h pra dias 1-7 (Meta não muda spend de dia fechado; minimiza chamadas no tier development).

### AC3: Validação
- [x] Typecheck ✅ (api + web), Lint ✅ (api + web)
- [x] Sem regressão (8 falhas de teste são pré-existentes: instagram/projects/schema, não tocam nesta mudança)

## Dev Notes

**Decisões do usuário (confirmadas):**
- TMB conta em tudo, inclusive ROAS/CPV.
- Cache: hoje=1h, dias passados permanentes uma vez no banco.

**Por que o fix do #1 é no backend:** as vendas manuais (PIX) são somadas em qualquer subtype no `sales-data`; somar duas respostas (`main_product` + `tmb`) no front duplicaria o PIX. Agregar via `inArray` no backend soma o PIX uma vez só.

## File List

- `packages/api/src/routes/stage-sales-data.ts` (querySchema CSV + parseSubtypes + inArray)
- `packages/api/src/services/meta-insights-cache.ts` (TTL hoje=1h, passado=permanente)
- `packages/web/lib/hooks/use-stage-sales-data.ts` (aceita subtype CSV)
- `packages/web/components/funnels/sales-meta-kpis.tsx` (pede main_product,tmb)

## Change Log

- Created + Implemented: 2026-06-09 (@dev)
