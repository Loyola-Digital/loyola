---
title: "Painel de conexão Kiwify + hooks (substitui o KiwifyStub)"
epic: 35
story: 4
status: Ready for Review

clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

## 📖 Story

**Como** administrador de um projeto na Loyola Digital X,
**Quero** conectar a credencial da Kiwify (client_id + client_secret + account_id) na aba "Assinaturas" e ver o status da conexão,
**Para** habilitar o dashboard de recorrência da Kiwify do meu projeto — substituindo o placeholder "em breve" por integração real.

---

## ✅ Acceptance Criteria

1. **Hooks `lib/hooks/use-kiwify.ts`** (espelha `use-hotmart.ts`, React Query + `useApiClient`):
   - `useKiwifyConnection(projectId)` → GET connection.
   - `useSetKiwifyConnection(projectId)` → PUT `{ clientId, clientSecret, accountId }`; invalida `kiwify-connection` + `kiwify-products`.
   - `useDeleteKiwifyConnection(projectId)` → DELETE; invalida os mesmos.
   - `useKiwifyProducts(projectId, { months, enabled })` e `useKiwifyDashboard(projectId, { productId, months, enabled })`.
   - Tipos `KiwifyConnectionResponse`, `KiwifyProduct`, `KiwifyDashboard` (espelha o contrato do backend; campos null para gaps).

2. **`KiwifyConnectionPanel`** (`components/subscriptions/kiwify-connection-panel.tsx`, espelha `hotmart-connection-panel.tsx`):
   - Header "Kiwify — Assinaturas" + Badge "Conectado" quando conectado.
   - Estados loading/error/connected/form (admin) / aviso (não-admin).
   - **Form de conexão** (admin, não conectado): inputs `client_id` (text), `client_secret` (`type=password`), `account_id` (text). Validação não-vazio; toast sucesso/erro; **limpa o secret do estado local após sucesso**.
   - **Header conectado** com botão "Desconectar" (admin).
   - Texto de ajuda: onde gerar a credencial na Kiwify (Apps/API) e o que é o `account_id` (store-id).

3. **Substituição do stub:** o `KiwifyStub` em `hotmart-connection-panel.tsx` (ou na página) é **removido/substituído** pela renderização real de `KiwifyConnectionPanel`. A aba "Assinaturas" passa a mostrar Hotmart **e** Kiwify reais.

4. **Dashboard montado quando conectado:** quando `connected`, renderiza `<KiwifySubscriptionsDashboard />` (entregue na 35.5 — nesta story pode ser um placeholder importável que a 35.5 preenche, OU a 35.5 depende deste ponto de montagem). Documentar o ponto de integração.

5. **Gating de admin:** `isAdmin = role !== null && role !== "guest"` (mesmo padrão da página subscriptions). Não-admin vê dashboard (se conectado) mas não o form.

6. **Qualidade:** `npm run typecheck` + `npm run lint` (web) passam; sem erros de hidratação.

---

## 🔍 Scope

**IN:** `use-kiwify.ts`; `kiwify-connection-panel.tsx`; substituição do `KiwifyStub`; ponto de montagem do dashboard.
**OUT:** Dashboard UI completo (35.5); endpoints (35.3, consumidos aqui); webhooks (fase 2).

---

## 📊 Complexity Estimate

**Story Points:** 4 (Medium) · ~1 dia. Espelha `use-hotmart.ts` + `hotmart-connection-panel.tsx` quase 1:1, +1 campo (`accountId`). Risco baixo.

---

## ⚠️ Risks & Mitigations

| Risk | Severity | Mitigation |
|------|----------|-----------|
| Secret persistir no estado do client | MEDIUM | Limpar `clientSecret` do state após sucesso (espelha Hotmart). Nunca exibir de volta. |
| account_id confundir o usuário | LOW | Texto de ajuda explica o que é (store-id) e onde achar. |
| Acoplamento com a 35.5 (dashboard) | LOW | Ponto de montagem documentado; placeholder importável até a 35.5. |

---

## 🔧 Dev Notes

### Espelhar `use-hotmart.ts` e `hotmart-connection-panel.tsx` (Story 34.4)

Base path `/api/projects/${projectId}/kiwify`. Estrutura idêntica de hooks; mutation de set recebe 3 campos. Componente: copiar layout (sections, Badge, Skeleton, estados), trocar copy Hotmart→Kiwify, adicionar o input `account_id`.

### Onde está o stub hoje

`packages/web/components/subscriptions/hotmart-connection-panel.tsx` → função `KiwifyStub` (placeholder decorativo). Substituir a renderização do stub pela montagem real de `KiwifyConnectionPanel`. Verificar a página `app/(app)/projects/[id]/subscriptions/page.tsx` para o layout das duas plataformas.

### Reuso

shadcn/ui (Button, Input, Label, Skeleton, Badge), lucide (CreditCard, Plug, Unlink, Loader2, RefreshCw), sonner (toast), React Query.

---

## ✔️ Criteria of Done

1. ✅ Tasks [x]. 2. ✅ 6 ACs. 3. ✅ Hooks + painel funcionando contra o backend. 4. ✅ Stub substituído. 5. ✅ Secret limpo após sucesso. 6. ✅ typecheck+lint web. 7. ✅ File List + Change Log. 8. ✅ Status → Ready for Review.

---

## 📋 Tasks / Subtasks

- [x] **Task 1:** `use-kiwify.ts` (5 hooks + tipos).
- [x] **Task 2:** `kiwify-connection-panel.tsx` (estados + form 3 campos + header conectado).
- [x] **Task 3:** Substituir `KiwifyStub` pela montagem real + ponto de montagem do dashboard.
- [x] **Task 4:** Copy de ajuda (onde gerar credencial / o que é account_id).
- [x] **Task 5:** typecheck + lint web; corrigir.
- [x] **Task 6:** File List + Change Log; status → Ready for Review.

---

## 🤖 CodeRabbit Integration

- **Primary Type:** Feature (frontend) · **Complexity:** Medium.
- **Primary Agents:** @dev (hooks+UI), @ux-design-expert (consistência com Hotmart). **Supporting:** @qa (segurança do secret no client).
- **Focus:** secret não persistido/exibido; gating admin correto; sem erro de hidratação; reuso do padrão Hotmart.
- **Self-Healing:** @dev light, max 2, CRITICAL/HIGH.

---

## 📝 File List

- [x] `packages/web/lib/hooks/use-kiwify.ts` (novo)
- [x] `packages/web/components/subscriptions/kiwify-connection-panel.tsx` (novo)
- [x] `packages/web/components/subscriptions/kiwify-subscriptions-dashboard.tsx` (novo — placeholder importável; a 35.5 substitui o corpo, mantendo a assinatura `KiwifySubscriptionsDashboard({ projectId })`)
- [x] `packages/web/components/subscriptions/hotmart-connection-panel.tsx` (modificado — remove `KiwifyStub`, monta `KiwifyConnectionPanel` real)

---

## 🔄 Change Log

- **[Draft]** Story criada pelo @sm. Painel de conexão Kiwify real (client_id+secret+account_id) + hooks `use-kiwify.ts`, substituindo o `KiwifyStub`. Espelha `use-hotmart.ts`/`hotmart-connection-panel.tsx` com +1 campo.
- **[Ready]** @po validação: GO (8/10) — transição Draft → Ready (2026-06-12). Sólida; ponto de integração com a 35.5 (montagem do dashboard) a confirmar na implementação — risco baixo, documentado.
- **[Implemented]** @dev (2026-06-12): hooks `use-kiwify.ts` (useKiwifyConnection/useSetKiwifyConnection PUT {clientId,clientSecret,accountId}/useDeleteKiwifyConnection/useKiwifyProducts/useKiwifyDashboard) + tipos espelhando o contrato do backend (KiwifyDashboard com MoneyByCurrency e gaps null para activeSubscriptions/churnRate). `KiwifyConnectionPanel` com estados loading/error/connected/form(admin)/aviso, form de 3 campos (client_id text / client_secret password / account_id text), secret limpo do state após sucesso, header conectado com Desconectar, copy de ajuda (Apps→API + explicação do account_id/store-id). `KiwifyStub` removido de `hotmart-connection-panel.tsx` e substituído por `<KiwifyConnectionPanel projectId isAdmin />`. Ponto de montagem do dashboard via `KiwifySubscriptionsDashboard` (placeholder importável — a 35.5 substitui o corpo). Imports absolutos `@/...`. Gates web: typecheck 0 erros; lint 0 erros (apenas 3 warnings `no-explicit-any` pré-existentes em `components/funnels/*`, não tocados). Status → Ready for Review.

---

## Dev Agent Record

### Status
Ready for Review

### Debug Log
_(vazio)_

### Completion Notes

Implementado por @dev (Dex) em 2026-06-12.

- **Hooks** (`packages/web/lib/hooks/use-kiwify.ts`): espelha `use-hotmart.ts` 1:1 com +1 campo. `useKiwifyConnection` (GET), `useSetKiwifyConnection` (PUT `{ clientId, clientSecret, accountId }`, invalida `kiwify-connection` + `kiwify-products`), `useDeleteKiwifyConnection` (DELETE, mesmas invalidações), `useKiwifyProducts`, `useKiwifyDashboard`. Tipos `KiwifyConnectionResponse`, `KiwifyProduct`, `KiwifyDashboard` (+ `KiwifyMoneyByCurrency`/`KiwifyChargeBucket`/`KiwifyStatusCount`) espelhando o contrato do backend (`services/kiwify.ts`): valores em centavos via `MoneyByCurrency` (`{ currency, value }`), buckets de cobrança, `newVsRenewal`, e gaps honestos `activeSubscriptions: null` / `churnRate: null`. Base `/api/projects/${projectId}/kiwify`.
- **Painel** (`packages/web/components/subscriptions/kiwify-connection-panel.tsx`): espelha `hotmart-connection-panel.tsx`. Estados loading (Skeleton) / error (retry) / connected (header + Desconectar p/ admin) / form (admin) / aviso (não-admin). Form de 3 campos: `client_id` (text), `client_secret` (`type=password`), `account_id` (text); validação não-vazio nos 3; toast sucesso/erro; **`clientSecret` limpo do state após sucesso** (nunca exibido de volta). Copy de ajuda: gerar credencial em Apps → API e o que é o `account_id` (store-id). Monta `<KiwifySubscriptionsDashboard projectId={projectId} />` quando conectado.
- **Ponto de montagem 35.5** (`packages/web/components/subscriptions/kiwify-subscriptions-dashboard.tsx`): placeholder importável com a assinatura pública final `KiwifySubscriptionsDashboard({ projectId })`. A 35.5 substitui o corpo (dropdown + período + KPIs honestos + gráfico) mantendo a assinatura — sem mudança no ponto de montagem.
- **Substituição do stub**: removida a função `KiwifyStub` de `hotmart-connection-panel.tsx`; renderização trocada por `<KiwifyConnectionPanel projectId={projectId} isAdmin={isAdmin} />`. A aba "Assinaturas" agora mostra Hotmart e Kiwify reais. `isAdmin` continua vindo da `page.tsx` (`role !== null && role !== "guest"`).
- **Gating de admin**: não-admin vê dashboard (se conectado) mas não o form (mensagem para pedir a um admin). Header conectado só mostra "Desconectar" para admin.
- **Imports absolutos** (`@/...`) em todos os arquivos novos.

**Gates (web):** `npm run typecheck` → 0 erros. `npm run lint` → 0 erros (3 warnings pré-existentes de `@typescript-eslint/no-explicit-any` em `components/funnels/leads-projection-cost-based-chart.tsx` e `components/funnels/stage-creative-performance-table.tsx`, fora do escopo desta story e não tocados). Nenhum warning/erro introduzido pelos arquivos novos ou modificados.

**Segurança:** GET de conexão devolve só `{ connected }`; o secret nunca é renderizado nem re-exibido e é limpo do state local após sucesso; nenhum log de credencial.
