---
title: "Dashboard de recorrência Kiwify (UI) — dropdown de produto + KPIs honestos"
epic: 35
story: 5
status: Ready

clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

## 📖 Story

**Como** usuário com acesso a um projeto da Loyola Digital X,
**Quero** um dashboard de recorrência da Kiwify por produto recorrente escolhido num dropdown, com filtro de período,
**Para** acompanhar receita recorrente, cobranças (pagas/reembolsadas/chargeback/pendentes), MRR aproximado e novos vs renovações — com clareza sobre o que a API da Kiwify **não** entrega.

---

## ✅ Acceptance Criteria

1. **`KiwifySubscriptionsDashboard`** (`components/subscriptions/kiwify-subscriptions-dashboard.tsx`) montado quando a conexão Kiwify está ativa (ponto de montagem da 35.4).

2. **Dropdown de produto recorrente** (reusa `product-picker.tsx`) populado por `useKiwifyProducts`. **Filtro de período** (reusa `period-picker.tsx`, default 12 meses). Dashboard só busca quando há `productId` selecionado.

3. **KPIs honestos** (reusa `KpiCard`/`subscription-kpis.tsx` + `format.ts` para moeda em centavos→BRL):
   - **Receita recorrente** (período, por moeda) · **MRR aproximado** (últimos 30d, com tooltip "aproximação: receita do mês, não MRR contratual") ·
   - **Cobranças pagas** (count + valor) · **Reembolsadas** (count + valor) · **Chargeback** (count + valor) · **Pendentes** (count) ·
   - **Taxa de reembolso** e **Taxa de chargeback** (de `/stats`) ·
   - **Novos vs renovações** (via `parent_order_id`).

4. **Cards de gap honestos:** "Assinaturas vigentes", "Churn" e afins aparecem **desabilitados/com tooltip** "Requer integração de webhooks (fase 2) — a API pública da Kiwify não expõe estado de assinatura". **Nunca** mostrar número inventado.

5. **Gráfico de distribuição por status de venda** (reusa `status-distribution-chart.tsx`, recharts) com os buckets/contagens de `statusDistribution`.

6. **Estados:** loading (skeleton), erro (com retry), vazio (produto sem vendas no período) tratados.

7. **Qualidade:** `npm run typecheck` + `npm run lint` (web) passam; sem warning de recharts/hidratação.

---

## 🔍 Scope

**IN:** `kiwify-subscriptions-dashboard.tsx` + reuso de `product-picker`/`period-picker`/`subscription-kpis`/`status-distribution-chart`/`format.ts`; KPIs honestos + cards de gap; gráfico.
**OUT:** Endpoints (35.3); painel de conexão (35.4); métricas que exigem estado de assinatura (fase 2).

---

## 📊 Complexity Estimate

**Story Points:** 5 (Medium) · ~1.25 dia. Reaproveita ~90% dos componentes `subscriptions/*`. O esforço real é **adaptar os KPIs ao contrato Kiwify** (centavos, buckets de venda) e os **cards de gap honestos**.

---

## ⚠️ Risks & Mitigations

| Risk | Severity | Mitigation |
|------|----------|-----------|
| Mostrar métrica de assinatura inexistente como número | HIGH | AC4: cards de gap desabilitados com tooltip explícito; QA verifica que nenhum número é inventado. |
| Centavos exibidos como reais (valor 100× maior) | HIGH | `format.ts` converte centavos→moeda na borda; teste visual com valor conhecido. |
| Período > 90d confundir (API janela) | LOW | Janelamento é no backend (35.2); UI só passa `months`. |
| Produto recurring sem vendas | LOW | Estado vazio tratado (AC6). |

---

## 🔧 Dev Notes

### Espelhar `subscriptions-dashboard.tsx` (Story 34.5)

Mesma casca (dropdown + período + grid de KpiCards + gráfico). Trocar hooks Hotmart→Kiwify e o set de cards. **Diferença conceitual importante:** o dashboard Hotmart mostra estado de assinatura (vigentes/canceladas); o Kiwify mostra **fluxo de vendas recorrentes** + cards de gap honestos. Não copiar os cards de "vigentes/churn" como se tivessem dado — usar o tratamento de gap (AC4).

### Componentes reutilizáveis (`components/subscriptions/`)

`product-picker.tsx`, `period-picker.tsx`, `subscription-kpis.tsx` (KpiCard), `status-distribution-chart.tsx`, `format.ts`. Verificar assinaturas/props de cada um antes de reusar.

### Formatação

`net_amount`/valores vêm em **centavos** do backend → `format.ts` divide por 100 e formata BRL. Multi-moeda: exibir por moeda (primária BRL).

### Reuso

shadcn/ui, recharts, lucide, `MetricTooltip` (se existir no padrão sales/traffic) para os tooltips de aproximação/gap.

---

## ✔️ Criteria of Done

1. ✅ Tasks [x]. 2. ✅ 7 ACs. 3. ✅ Dashboard renderiza com dados reais. 4. ✅ Cards de gap honestos (sem número inventado). 5. ✅ Centavos formatados corretamente. 6. ✅ typecheck+lint web. 7. ✅ File List + Change Log. 8. ✅ Status → Ready for Review.

---

## 📋 Tasks / Subtasks

- [ ] **Task 1:** `kiwify-subscriptions-dashboard.tsx` (casca: dropdown + período + grid).
- [ ] **Task 2:** Wire `useKiwifyProducts` + `useKiwifyDashboard` (busca só com productId).
- [ ] **Task 3:** KPIs honestos (receita, MRR aprox, buckets de cobrança, taxas, novos vs renovação).
- [ ] **Task 4:** Cards de gap (vigentes/churn) desabilitados + tooltip "fase 2 / webhooks".
- [ ] **Task 5:** Gráfico de distribuição por status de venda.
- [ ] **Task 6:** Estados loading/erro/vazio.
- [ ] **Task 7:** typecheck + lint web; corrigir.
- [ ] **Task 8:** File List + Change Log; status → Ready for Review.

---

## 🤖 CodeRabbit Integration

- **Primary Type:** Feature (frontend/dataviz) · **Complexity:** Medium.
- **Primary Agents:** @dev (UI), @ux-design-expert (consistência + honestidade dos cards). **Supporting:** @qa (nenhum número inventado, centavos corretos).
- **Focus:** integridade dos dados exibidos (gaps honestos), formatação de moeda (centavos), reuso dos componentes `subscriptions/*`, acessibilidade dos tooltips.
- **Self-Healing:** @dev light, max 2, CRITICAL/HIGH.

---

## 📝 File List

- [ ] `packages/web/components/subscriptions/kiwify-subscriptions-dashboard.tsx` (novo)
- [ ] componentes `subscriptions/*` reusados (sem modificação, ou ajustes mínimos de props)

---

## 🔄 Change Log

- **[Draft]** Story criada pelo @sm. Dashboard de recorrência Kiwify: dropdown de produto recorrente + período + KPIs honestos (receita, MRR aprox, cobranças por bucket, taxas, novos vs renovação) + gráfico de status + cards de gap honestos (vigentes/churn → fase 2). Reusa `subscriptions/*`.
- **[Ready]** @po validação: GO (9/10) — transição Draft → Ready (2026-06-12). Honestidade dos dados (cards de gap) é diferencial de qualidade; reuso de `subscriptions/*` confirmado.

---

## Dev Agent Record

### Status
Ready

### Debug Log
_(vazio)_

### Completion Notes
_(vazio)_
