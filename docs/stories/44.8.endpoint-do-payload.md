# Story 44.8: Endpoint do payload da aba + schema versionado

**Epic:** 44 — Aba "Inácio"
**Status:** Ready for Review *(2ª passada — QA fixes aplicados)*
**Created:** 2026-08-19 (@sm — River)
**Priority:** 🔴 Alta — é a ponte entre o módulo e qualquer consumidor; sem ela a 44.9 não tem o que desenhar
**Complexity:** L (10 pontos) — a composição já existe pronta; o custo está nas entradas que o módulo pede e que hoje ninguém produz, **mais o benchmark de mediana que a decisão 3 do @po traz para cá** (AC11)
**Depends On:** 44.6 (primitivas, `Done`) · 44.7 (composição, `Done`)
**Origem:** spec mestre **v1.1** §2, §4, §7 · gate `docs/qa/gates/44.7-composicao-do-teto.yml`

> **Fonte única:** `docs/stories/epics/epic-44-aba-inacio.md` **v1.1**. Onde esta story e a spec divergirem, a spec vence.

---

## 💼 Por que esta story existe

O Epic 44 tem, hoje, um módulo puro completo em `packages/shared/src/cadeia-cac.ts` — 24 funções, 72 testes, os 12 valores dourados da §8 batendo — e **nenhum consumidor**. Nenhum arquivo em `routes/`, `services/` ou `utils/` importa `cadeia-cac`.

Esta story é o primeiro consumidor de produção. Ela não calcula nada: ela **monta as entradas**, chama o shared e serve o resultado.

### ⚠️ A regra que define o desenho inteiro

Regra **7.6** da spec: *"o cálculo mora em `@loyola-x/shared` como função pura; a aba consome, a API expõe o mesmo resultado."*

Se qualquer número for recalculado aqui — uma taxa, um teto, uma queda — a aba e o Inácio passam a poder divergir. **Foi exatamente assim que o `connectRate` ficou 18 a 35 pontos percentuais errado por mais de um ano** (Story 44.2): não por bug, por duas implementações da mesma conta.

O teste desta AC é mecânico: se aparecer uma divisão nova no arquivo da rota, ela está no lugar errado.

---

## 🗺️ O terreno (levantado em 2026-08-19)

Isto não é contexto opcional — cada linha abaixo elimina uma decisão que o @dev teria que inventar.

| o que | onde | estado |
|---|---|---|
| Rotas públicas | `packages/api/src/routes/`, registradas em `app.ts:155-233` | plugin Fastify por arquivo |
| Gate de API key | `middleware/api-key-auth.ts:65` (prefixo `/api/public/`) + `requireScope(PUBLIC_READ_SCOPE)` por rota | pronto |
| Rota irmã mais próxima | `public-meta.ts:715-903` — `.../stages/:stageId/campaigns/daily` (Story 44.1) | **é o molde** |
| `GROUP BY campanha, dia` | `public-meta.ts:846-897`, **inline no handler** | existe, **não é reutilizável** |
| `funnel_stages.campaigns` | `db/schema.ts:667` — JSONB `{id,name}[]` | é assim que a campanha se liga à etapa |
| `funnel_stages.stageType` | `db/schema.ts:689` | 9 valores; `classificarFamilia` (`cadeia-cac.ts:172`) traduz |
| Atribuição de venda/lead | `services/campaign-attribution.ts:135` — função **pura** | pronta, **sem nenhum consumidor ainda** |
| Vendas por dia | `services/sales-daily-sync.ts:186` → `SalesDailyPayload.byDay` | pronto |
| **Cobertura de lead por dia** | — | ❌ **NÃO EXISTE** — ver AC5 |
| Imposto Meta | `utils/meta-tax.ts:22`, aplicado em `public-meta.ts:161` | ⚠️ ver AC6 |
| Contrato painel↔API | `packages/shared/src/contract.ts` — `API_CONTRACT_VERSION = 2` | ver AC9 |
| Template de teste de rota | `__tests__/public-meta-campaigns-daily.test.ts` | banco **mockado**, nunca real |

**Import:** a API usa **bare import** (`from "@loyola-x/shared"`). O subpath é o padrão do web e **derrota o boot em runtime passando por `tsc`, `vitest` e `next build`** (Story 19.14).

---

## ✅ Critérios de aceite

### AC1 — Uma rota, pública, por etapa

`GET /api/public/meta/v1/projects/:projectId/stages/:stageId/cadeia-cac`

Mesmo padrão da rota irmã: `requireScope(PUBLIC_READ_SCOPE)`, range opcional `from`/`to`.

⚠️ **O `innerJoin` `funnel_stages` ↔ `funnels` é obrigatório**, provando `funnels.projectId = :projectId` antes de devolver qualquer coisa (`public-meta.ts:775-777`). Sem ele a rota é IDOR: qualquer chave de API lê a etapa de qualquer projeto. **A 43.7 introduziu exatamente esse buraco** porque o guest-guard global não cobre rota project-scoped. Etapa que não pertence ao projeto da URL → **404**, nunca 403 (não confirmar existência).

### AC2 — A série de entrada é **uma linha por (campanha, dia)**

O módulo recebe `SerieDeCampanha[]` = `{ campaignId, fonte, dias: DiaBruto[] }`.

⚠️ **`dias` precisa ter no máximo uma entrada por data, e isto é contrato, não detalhe.** O gate da 44.7 (QA-447-01) mediu o que acontece quando não é: com duas linhas na mesma data, o teto de CPC saiu **0,20 em vez de 0,92** — 4,6× inflado, vindo de um anúncio isolado. O `ultimoDaData` de `janelasDe7Dias` hoje protege contra isso, mas proteção não é licença: uma série ad-level crua também infla a base do piso de confiança, e aí nenhuma guarda alcança.

**O caminho seguro já existe.** O handler de `campaigns/daily` agrupa em `Map<campanha, Map<data, agregado>>` (`public-meta.ts:866-882`) — o `Map` por data garante unicidade por construção. **Reusar esse agrupamento, não reescrever.**

**Entregar:** extrair o bloco `public-meta.ts:846-897` para um serviço (sugestão: `services/meta-campaign-daily.ts`), com `campaigns/daily` passando a chamá-lo. Duas rotas montando a mesma série de dois jeitos é a semente da próxima divergência do `connectRate`.

Se a extração se mostrar mais cara que o previsto, **parar e trazer ao @po** — repetir a query é aceitável como decisão consciente, não como acidente.

### AC3 — A composição chama o shared, e só

Na ordem, sem nenhuma conta própria:

1. `classificarFamilia(stage.stageType)` → `paga` | `gratuita` | `null`. **Família `null` → a etapa está fora da aba**: `lyrio`, `comercial`, `debriefing` (spec §1). Devolver 200 com o motivo, não erro.
2. `calcularMetricas(agregar(todosOsDias), familia)` → as métricas **atuais** do período.
3. `calcularTetos(series, familia, { coberturaDaEtapa })` → `TetosDoGrupo`.
4. `montarRanking(tetos, atuais)` → `ItemRanking[]` — já ordenado, já sem CPM/CTR (QA-447-02), já sem as métricas no teto.
5. `metricasDoTeto(tetos)` + `compostoNoTeto(atuais, metricasDoTeto)` → o composto, **rotulado cenário teórico** no payload.
6. `decomporCPC(...)` quando o CPC lidera o ranking (spec §3).
7. `cacReal(spend, vendasReais)` ou `cplReal(spend, leadsUnicos)` conforme a família — **o número principal** (spec §2.1).

⚠️ **Nenhuma divisão nova no arquivo da rota.** Se precisar de uma, ou ela existe no shared, ou vira issue.

### AC4 — `CAC real` precisa de venda real, e ela não vem da Meta

`cacReal = spend ÷ vendas reais (Loyola)` — regra 7.1: receita, venda e lead **sempre** do Loyola X. `purchasesProxyPixel` do payload é pixel e subconta (R$20k viraram ~R$6–7k reportados, `[deck §7.1]`).

A venda por dia já existe: `SalesDailyPayload.byDay` (`services/sales-daily-sync.ts:67-119`, servida em `routes/public-funnel-sales.ts:181`). Só status pago entra — `isRevenueBucket`, **não** `isRefundBucket`.

⚠️ **`byDay` NÃO tem campo `vendas` — o @po conferiu, e o contorno tem duas armadilhas.** `[validação @po 2026-08-19]`

1. **A contagem por dia chama-se `ingressos.total`** (`sales-daily-sync.ts:364` = `pago + org + semTrack`). O `byDay` só carrega `faturamentoBruto`, `faturamentoLiquido` e `ingressos`. Usar `ingressos.total` como nº de vendas do dia está certo aqui, mas o nome é herdado de outro contexto (18.51/18.52, onde ingresso único ≠ ingresso total) — **comentar no código de onde vem**, ou a próxima leitura assume que é outra coisa.
2. **Venda sem data some do `byDay` e continua no total.** O acumulador é guardado por `if (s.date)` (`:338`), enquanto `totalVendas = sales.size` (`:354`) conta todas. Logo `Σ byDay.ingressos.total ≤ totalVendas`, **silenciosamente**. Com range aplicado, o denominador do `cacReal` vem subcontado e o CAC sai **alto**, sem nada na tela avisando — a mesma classe de falha do imposto ao quadrado, só que para baixo no denominador.

**Entregar:** período sem range usa `totalVendas`; período com range usa a soma do `byDay` **e compara com `totalVendas`**. Diferença > 0 vira diagnóstico declarado no payload (`vendasSemData: N`), nunca correção silenciosa. Regra 7.4 vale aqui igual.

**Entregar:** o payload traz `cacReal` (paga) ou `cplReal` (gratuita) para a etapa, mais a **cobertura de atribuição de venda** (`campaign-attribution.ts` já devolve `coberturaVendas`). A cobertura é **diagnóstico exibido, nunca multiplicador** (spec §2.7).

⚠️ **`cacReal` sai mesmo com cobertura 0%** — ele não depende de atribuição por campanha, só do total da etapa. É a propriedade que motivou a v1.1 inteira. Se ele vier `null` numa etapa com venda, é bug.

### AC5 — ⚠️ A cobertura de lead por dia não existe, e a decisão de escopo é do @po

`calcularTetos` aceita `opts.coberturaDaEtapa: CoberturaDiaria[]` = `{ date, leadsAtribuidos, leadsTotais }` por dia. **Nenhuma função, serviço ou rota produz isso hoje** — as únicas ocorrências no repo são fixtures de teste.

É a mesma classe de buraco que a 44.7 pegou duas vezes (`Teto.fonte` e a própria `coberturaAtipica`): campo que o consumidor precisa, sem nada na entrada que o produza. Registrar cedo é mais barato que descobrir na implementação.

**O que está em jogo:** a guarda só afeta o teto de `convLP` da família **gratuita**. Sem ela, `calcularTetos` roda com `opts` vazio e o teto sai **sem `coberturaJanela`** — comportamento já previsto e testado na 44.7. Mas aí volta o problema que a guarda existe para resolver: a "melhor janela" pode ser a semana em que o **rastreio** funcionou melhor, não a de melhor conversão. Medido: uma etapa que passou de 52% para 95% de cobertura exibiria um salto de **1,8×** sem nada mudar na página.

**✅ DECISÃO @po (2026-08-19): FORA DE ESCOPO — opção (a).** Vira story própria, no backlog com dono.

| opção | custo | consequência |
|---|---|---|
| **(a) fora de escopo** ✅ **decidida** | zero aqui; vira story própria | família gratuita sai sem a guarda **e o payload declara isso** (`guardaDeCobertura: "indisponivel"`) |
| (b) dentro do escopo | ~~estender `computeLeadOriginForStage` com `byDay`~~ | **o custo estimado pelo @sm está baixo — ver abaixo** |

**Por que (a), com evidência que o @sm não tinha:** a opção (b) não é estender um `byDay`.

- **`LeadParaAtribuir` não tem data.** É `{ utmContent, chave }` (`campaign-attribution.ts:39-43`). O produtor de `CoberturaDiaria` precisa de `{ date, leadsAtribuidos, leadsTotais }` — ou seja, **a data tem que entrar no registro de lead e atravessar o chamador inteiro**, não só o agregador.
- **`LeadOriginPayload` não tem quebra por dia nenhuma.** A data é lida (`toIsoDate`, `:264`) e usada **só para min/max do `range`** (`:399-403`). Não existe bucket diário para estender.
- **A dedup por dia é uma decisão nova, não um detalhe.** `coberturaLeads` global usa um `Set` único de chaves (`:198-221`, a correção QA-44-01). Por dia, o mesmo lead que se cadastrou em dois dias conta **uma vez no global e duas na soma dos dias** — a razão de somas do `coberturaDaJanela` (`cadeia-cac.ts:524-536`) exige uma regra explícita de janela de dedup. Isso é design, não implementação.

**Superfície afetada, medida:** a guarda só toca `convLP` da família **gratuita** = `FAMILIA_GRATUITA = ["free", "cpl"]` (`cadeia-cac.ts:159`), e a spec §1 registra que **`cpl` tem zero campanhas vinculadas hoje**. Sobra `free`. Contido.

**Condições inegociáveis da decisão** (regra 7.4 — **o silêncio é proibido**):

1. O payload declara `guardaDeCobertura: "indisponivel"` em **toda** etapa da família gratuita, com motivo legível. Consumidor que não sabe que a guarda estava desligada lê um teto de gratuita como se fosse tão confiável quanto o de paga.
2. Teste obrigatório da AC10 cobrindo essa declaração, **verificado por reversão**.
3. A story do produtor entra no backlog **nesta validação**, não "depois" — e é ela que decide a janela de dedup.

### AC6 — O imposto Meta já está aplicado. Não aplicar de novo

`spend` que vem do agregador de `public-meta.ts:161` **já tem o gross-up de 12,15%**, e o payload declara `spendIncludesMetaTax: true`.

⚠️ **Reaplicar dá imposto ao quadrado** — foi o bug da 29.24, corrigido na 29.27, e voltou a aparecer no Epic 29 porque duas camadas tributavam.

A regra prática, que a story fixa para não ser redescoberta:

| se a série vier de | spend está | ação |
|---|---|---|
| `public-meta.ts` / `accumulate()` | **tributado** | não tocar |
| `services/meta-entity-daily.ts` | **BRUTO** de propósito (doc em `:36-40`) | aplicar `applyMetaTax` |
| `services/meta-db-source.ts` | **BRUTO** | aplicar |

O payload da 44.8 declara `spendIncludesMetaTax: true`, como as irmãs.

### AC7 — Unidades: decimal no cálculo, conversão só na fronteira

Dentro do cálculo, **toda taxa é decimal entre 0 e 1** (spec §2.5). O `Metricas` do shared já é assim (`cadeia-cac.ts:89-99`).

⚠️ O payload público existente devolve **×100** em cinco campos: `ctr` (`:190`), `ctrLink` (`:194`), `connectRate` (`:220`), `lpRate` (`:221`), `checkoutRate` (`:224`). Se a 44.8 consumir qualquer um deles, `dePercentual()` (`cadeia-cac.ts:145`) normaliza **na fronteira, nunca no meio da conta**.

⚠️ **`lpRate` NÃO é o `Conv. LP` da spec.** No payload público significa LP views ÷ link clicks (= Connect Rate); na spec, `Conv. LP` é checkouts ÷ LP views. **O `convLP` não existe naquele payload** — tem que ser derivado de `checkouts` e `landingPageViews`, que estão lá como brutos somáveis. Não reusar o nome (Bloqueio B2).

**✅ DECISÃO @po (2026-08-19): o payload sai em DECIMAL (0–1), com a unidade declarada.**

**Por quê, e por que a divergência com as irmãs não cobra o preço que parece:** esta é uma **rota nova com nomes novos**. Não existe um campo aqui que signifique o mesmo que um campo lá — a própria AC7 prova: o `lpRate` de lá é Connect Rate, o `convLP` **não existe** naquele payload. Ninguém lê os dois esperando a mesma chave. O que a ×100 custaria é concreto: o endpoint converteria na saída e a 44.9 converteria de volta na entrada — duas conversões onde zero bastam, e a §2.5 diz que trocar `2` por `0,02` é **a fonte nº 1 de erro** da spec.

**Entregar:**

- `unidadeDasTaxas: "decimal"` no payload, **desde a v1** — para que uma virada futura seja detectável, e não silenciosa.
- ⚠️ **A declaração cobre taxas, e só.** Dinheiro continua **real absoluto** e o CPM continua na escala ÷1.000 (spec §2.5, a única exceção). Dizer só "decimal" faria o consumidor achar que vale para o dinheiro também.
- Qualquer campo lido do payload público ×100 passa por `dePercentual()` **na fronteira de entrada**, nunca no meio da conta.

### AC8 — Dado ausente vira motivo, nunca zero

Regra 7.4: `semDados` | `baseInsuficiente` | `naoAtribuivel`. Nunca `0`, nunca estimativa, nunca troca silenciosa de fonte.

O módulo já devolve `TetoAusente` com motivo. **A rota propaga, não traduz.** Casos que o payload tem que distinguir com clareza:

- etapa sem campanha vinculada → série vazia com motivo, **não** `days: []` (a distinção que a 44.1 já trava em teste)
- etapa fora da aba (`lyrio`/`comercial`/`debriefing`) → 200 com motivo, não erro
- grupo sem teto elegível → coluna vazia com o motivo; o alvo passa a ser o benchmark (spec §4)
- família gratuita sem a guarda → declarado (AC5)

### AC9 — Bump do `API_CONTRACT_VERSION` para `3`

`packages/shared/src/contract.ts` — a regra do arquivo é explícita: *"quando um PR fizer o web depender de um campo ou rota que a API não tinha, esse mesmo PR incrementa este número."*

A 44.9 vai depender desta rota. O bump entra **aqui**, não lá — senão o painel da 44.9 pede uma rota que a API não tem e o sintoma aparece disfarçado, que é o modo de falha que custou três investigações (18.60, 29.43, 29.45).

⚠️ **Avisar sobre `packages/mcp`**: o servidor stdio que roda na máquina do Inácio **não tem checagem de contrato**. A 44.2 já ensinou isso. Esta rota é nova (não muda nenhuma existente), então o risco é menor — mas o aviso é barato.

⚠️ **A API está sem deploy desde a 44.6.** Com a 44.8 isso deixa de ser inofensivo: é a primeira story do epic com consumidor. O deploy passa a ser pré-requisito da 44.9, não pendência de fundo.

### AC10 — Testes de rota, com o molde que já existe

`__tests__/public-meta-campaigns-daily.test.ts` é o template — **banco mockado, `Fastify()` cru, sem `app.ts`**. A fila de `mockReturnValueOnce` acopla à ordem das queries do handler; se a 44.8 fizer três queries, a fila tem três.

Casos obrigatórios:

- **404 quando a etapa não pertence ao projeto da URL** (o IDOR da AC1) — este é o teste que não pode faltar
- etapa sem campanha → motivo, não `days: []`
- etapa fora da aba → 200 com motivo
- família paga vs gratuita → métrica final diferente (`cacReal` vs `cplReal`)
- **cobertura de venda 0% → `cacReal` ainda sai** (a propriedade da v1.1)
- imposto aplicado **uma única vez**
- ranking sem CPM e CTR (o QA-447-02, agora servido)
- teto com `TetoAusente` → payload traz o motivo
- **família gratuita sem cobertura → o payload declara a guarda como indisponível** (AC5)

⚠️ **Cada teste verificado por reversão.** É o que pegou o teste decorativo da 44.2, o lead-vira-zero da 44.6 e os dois defeitos da 44.7. Teste que não falha com o defeito de volta não protege nada.

### AC11 — ✅ O benchmark de mediana de CPM/CPC sai deste endpoint

**✅ DECISÃO @po (2026-08-19): sim, sai daqui — e o motivo não é o que o @sm alegou.**

O argumento pela regra 7.6 sozinho é mais fraco do que parece: **`mediana()` já está exportada do shared** (`cadeia-cac.ts:480`, no `index.ts:57`). Quem quer que chame, chama a mesma função — a duplicação de fórmula não aconteceria. O que decide são outras duas coisas, conferidas no código:

**(a) O Inácio nunca vê a aba.** `packages/mcp/src/tools.ts` consome `/api/public/meta/v1/...` direto (`:97`, `:124`, `:150`, `:171`, `:195`). A spec §6 sinaliza contra o **alvo vigente — teto quando existe, benchmark quando não existe**. Se o benchmark só existir na aba, então **exatamente no caso para o qual a §5 foi escrita** — grupo sem teto elegível, e o gate 44.5 mediu **2 de 10 grupos vazios e 4 frágeis** — o agente fica **sem alvo nenhum**. Isso é buraco funcional, não risco de divergência.

**(b) "campanhas elegíveis" não é derivável do payload.** `TetosDoGrupo = Record<Metrica, Teto | TetoAusente>` (`cadeia-cac.ts:520`) expõe só a campanha **vencedora** (`campaignId`, `base`, `confianca`). A elegibilidade **por campanha** é calculada dentro de `calcularTetos` e não sai. Para a aba tirar "a mediana das campanhas elegíveis do grupo", ela teria que **re-derivar a régua do gate 44.5** — e aí sim, isso é a violação da 7.6 de verdade.

**Escopo exato — o mínimo que fecha o buraco:**

| entra na 44.8 | fica na 44.9 |
|---|---|
| mediana de **CPM** e **CPC** das campanhas elegíveis do grupo (spec §5, linha 240) | benchmarks **constantes**: CTR ≥2%, Connect >85% |
| o análogo da família gratuita — CPL e Conv. LP→lead, mediana histórica (§5, linha 241) | Conv. LP 4% / 7,5% — **não pode vir para cá**: depende do booleano "LP tem VSL?" e do ticket, que são colunas novas em `funnel_stages` da 44.9 |
| rótulo `mediana histórica` no payload, como a §5 exige | sinalização 🟢🟡🔴 (§6) — apresentação |

⚠️ **Isto adiciona trabalho no `shared`, e eu estou dizendo isso de propósito.** A elegibilidade por campanha existe dentro de `calcularTetos` e precisa ser exposta — seja como saída nova, seja como função irmã (`medianaElegivel(series, metrica)`). É a razão da complexidade subir de 8 para 10 pontos.

**Escape hatch, mesmo padrão da AC2:** se expor a elegibilidade se mostrar mais caro que o previsto — se implicar mexer na assinatura de `calcularTetos` e refazer testes da 44.7 — **parar e trazer ao @po**. Servir sem o benchmark e abrir story própria é decisão aceitável **consciente**, não acidente.

---

## 🚫 Fora de escopo

- **A aba e os campos novos em `funnel_stages`** (booleano "LP tem VSL?", ticket médio manual) → **44.9**
- **Benchmarks constantes** (CTR ≥2%, Connect >85%, Conv. LP 4%/7,5%) **e sinalização 🟢🟡🔴** (spec §5, §6) → **44.9**. ✅ *A mediana de CPM/CPC veio para cá — ver AC11*
- Bloco de criativos → **44.10**
- Migração do `traffic-analytics.ts:452` (`spend ÷ purchases` do pixel) → **44.11**
- **Produtor de `CoberturaDiaria` → story própria (44.12), aberta na validação de 2026-08-19** (ClickUp `86ak2uc8w`, status `backlog`) — ver AC5

⚠️ **Armadilha de renumeração:** a 44.6 manda a aba e os benchmarks "→ 44.8". Ela foi escrita **antes** do deslocamento decidido no QA-446-05 (quando o endpoint era 44.7 e a aba era 44.8). **Toda referência a "44.8" dentro da 44.6 significa a 44.9 de hoje.**

---

## ✅ Decisões do @po (2026-08-19 — nada aqui está aberto)

| # | questão | decisão | onde |
|---|---|---|---|
| 1 | cobertura de lead por dia entra aqui? | **NÃO** — story própria (44.12). O payload declara `guardaDeCobertura: "indisponivel"`, com teste | AC5 |
| 2 | decimal ou ×100? | **Decimal (0–1)**, com `unidadeDasTaxas` declarado. Cobre taxas; dinheiro segue absoluto e CPM na escala ÷1.000 | AC7 |
| 3 | mediana de CPM/CPC sai daqui? | **SIM** — mas pelo motivo (a)+(b) da AC11, não pela 7.6. Constantes e sinalização ficam na 44.9 | AC11 |

**Achado próprio da validação, que não estava na story:** `SalesDailyPayload.byDay` **não tem campo `vendas`**, e descarta silenciosamente venda sem data (`:338` contra `:354`). Com range aplicado, isso subconta o denominador do `cacReal` e o CAC sai alto sem aviso. Virou requisito na AC4 — **é o achado mais perigoso deste lote**, porque erra na direção que ninguém desconfia.

---

## ⚠️ Riscos

| risco | por quê |
|---|---|
| **Recalcular no endpoint** | quebra a regra 7.6 e reintroduz o modo de falha do `connectRate` — 18 a 35 p.p. errados por mais de um ano |
| **Série ad-level crua** | mais de uma linha por data infla a base do piso; o `ultimoDaData` protege a janela, não o piso (QA-447-01) |
| **Faltar o `innerJoin`** | IDOR — a rota vira leitura de etapa de qualquer projeto (é o buraco que a 43.7 abriu) |
| **Reaplicar o imposto** | imposto ao quadrado (29.24 → 29.27), e a conta fica ~13% alta sem nada na tela avisar |
| **`lpRate` como `Conv. LP`** | são conceitos diferentes com o mesmo nome (Bloqueio B2) |
| **Esquecer o bump do contrato** | a 44.9 pede rota que a API não tem, e o sintoma aparece disfarçado (18.60, 29.43, 29.45) |
| **Servir sem deploy da API** | a 44.8 é a primeira story do epic com consumidor; o deploy vira pré-requisito da 44.9 |

---

## 📓 Notas para o @dev

- **Você não vai escrever nenhuma fórmula.** Todas as 24 funções de que precisa existem e estão testadas. Se estiver escrevendo uma divisão, pare e confira se ela já está no shared.
- **Import bare** (`from "@loyola-x/shared"`), nunca subpath. `node -e "import('@loyola-x/shared')"` a partir de `packages/api` é o único gate que pega a armadilha da 19.14 — `tsc`, `vitest` e `next build` passam todos.
- O `campaigns/daily` já devolve `campaignId`, `fonte: "ad-level"` e `days` quase-`DiaBruto`. **A conversão para `SerieDeCampanha` é rasa** — só falta `leadsAtribuidos`, que é opcional.
- **Baseline da suíte da API: 11 falhas.** São **ambientais** — o job `test` do CI passa. Provado no PR #554 comparando a `main` pura com a branch por nomes de teste. Não gaste tempo com elas.
- **Arquivos duplicados `" 2"`**: ~142 no repo, mais 25 que `pnpm dev` gera em `packages/web/.next/types/`. Quebram o typecheck do web com `TS2300` e **não são do seu código**. Limpar antes de investigar.

---

## 📋 Definition of Done

- [x] Rota `GET .../stages/:stageId/cadeia-cac`, pública, com `requireScope`
- [x] `innerJoin` contra IDOR, com teste de 404 para etapa de outro projeto
- [x] Série montada como **uma linha por (campanha, dia)**, via agrupamento reusado
- [x] Agrupamento extraído para serviço, com `campaigns/daily` chamando o mesmo
- [x] Composição chamando o shared, **zero fórmula nova na rota**
- [~] `cacReal`/`cplReal` da etapa, com venda real do Loyola — **feito**; a **cobertura de atribuição fica declarada como indisponível** (ver Nota 1 abaixo — é o único item que não fecha)
- [x] `cacReal` sai mesmo com cobertura 0%
- [x] Guarda de cobertura declarada como `indisponivel` em toda etapa gratuita (AC5), com teste por reversão
- [x] `cacReal` com range: soma do `byDay` comparada a `totalVendas`, e `vendasSemData` declarado (AC4)
- [x] Imposto aplicado uma única vez; `spendIncludesMetaTax` declarado
- [x] Saída em decimal, com `unidadeDasTaxas` declarado; dinheiro absoluto e CPM ÷1.000 preservados (AC7)
- [x] Mediana de CPM/CPC (e o análogo gratuito) servida com rótulo `mediana histórica`, com a elegibilidade vinda do shared (AC11)
- [x] Motivos propagados, nunca zeros
- [x] `API_CONTRACT_VERSION` → `3`
- [x] Testes de rota com o molde existente, cada um verificado por reversão — **incluindo** unidade decimal, benchmark de mediana e guarda indisponível
- [x] `pnpm typecheck`, `pnpm lint` e `pnpm build` limpos; prova de runtime feita; baseline de 11 falhas preservado
- [x] **QA-448-01** — falha de leitura distinguida de ausência de fonte (bloqueante)
- [x] **QA-448-02** — `resolveLeadSource` desempata as duas causas na gratuita
- [x] **QA-448-03** — `SALES_PUBLIC_MAX_AGE_SEC` e `?fresh=1` honrados, com `maxAgeFrom` compartilhado
- [x] **QA-448-04** — `vendasSemData` → `vendasSemDataNoTotal`, com o porquê no código
- [x] **QA-448-05** — teste do ranking guardado contra o caso vazio

---

## 🤖 Dev Agent Record

**Agent Model Used:** claude-opus-5[1m] · **Modo:** YOLO · **Data:** 2026-08-19

### Nota 1 — ⚠️ O único item que NÃO fecha: cobertura de atribuição de venda (AC4)

A AC4 diz *"`campaign-attribution.ts` já devolve `coberturaVendas`"*. A **função** devolve; o que não existe é a **entrada** dela para uma etapa.

Levantado no código:

- `atribuirPorCampanha` (`campaign-attribution.ts:135`) recebe `VendaParaAtribuir[]` = `{ utmContent, valorBruto, status }`.
- **`sales-daily-sync.ts` não mapeia `utm_content`** — zero ocorrências no arquivo. O `Mapping` dele tem `utm_source`, `utm_medium`, `dataVenda` e `status`, e nada mais de UTM. É o mesmo tipo de buraco da 44.7 e da AC5: o consumidor pede um campo que a entrada não produz.
- O único leitor row-level com `utm_content` é **inline no handler** de `routes/public-sales-rows.ts:181-211` — não é serviço, e devolve linha **crua, sem dedup**.

Por que parei aqui em vez de escrever o leitor: `atribuirPorCampanha` conta `totalVendas` sobre o que recebe. Alimentá-lo com a linha crua do `public-sales-rows` produziria um **segundo `totalVendas`**, com dedup diferente do `SalesDailyPayload.totalVendas` que já viaja neste mesmo payload — dois números com o mesmo nome discordando dentro da mesma resposta. É exatamente a classe de defeito que o epic existe para impedir, e a escolha da chave de dedup é decisão de produto, não de implementação.

**O que foi entregue no lugar:** `atribuicao: { coberturaVendas: null, coberturaLeads: null, motivo: "naoAtribuivel", message }` — regra 7.4, com o motivo explícito. **Isto não afeta `cacReal`/`cplReal`**, que dependem do total da etapa (testado: `cacReal SAI mesmo com cobertura de atribuição 0%`).

**Efeito colateral honesto:** sem produtor de `leadsAtribuidos`, a família **gratuita** sai com `convLP: null` (não zero — QA-446-01), e portanto sem teto nem benchmark de `convLP`. Está testado e declarado.

**Para o @po:** isto é uma story própria (o consumidor da 44.3), irmã da 44.12. Ou se decide a chave de dedup aqui e eu escrevo o leitor.

### Nota 2 — O `spend` é arredondado por DIA, não no fim

`services/meta-campaign-daily.ts` arredonda `spend` a 2 casas por dia antes de somar, porque herda o comportamento da rota irmã `campaigns/daily`. Diferença de 3,3e-6 relativo contra somar bruto — **mantida de propósito**: é a mesma série que a 44.1 serve, e o objetivo é a aba e o Inácio verem o mesmo número. Documentado no teste, não escondido.

### Nota 3 — O teste de IDOR seria decorativo, e foi consertado

Com o banco mockado, o 404 cai da fila de respostas: a rota devolveria 404 mesmo com o filtro `funnels.projectId` apagado. Adicionei `vinculoSpy`, que captura o predicado montado pelo drizzle e afirma que o `projectId` da URL entrou nele. **Provado por reversão:** removendo `eq(funnels.projectId, projectId)`, o teste novo falha e o antigo continua passando.

### Nota 4 — Correções do gate FAIL (2ª passada)

O @qa achou o que a minha bateria era estruturalmente incapaz de ver: **nenhum teste exercitava falha de leitura**. Os 5 achados foram corrigidos na mesma passada — inclusive os dois que ele mandou para o backlog, porque são uma linha cada e deixá-los abertos custaria mais em ida e volta do que em código.

| id | conserto |
|---|---|
| **QA-448-01** | `try/catch` no lugar do `.catch(() => null)`; erro vira `motivo: "leituraFalhou"` com a mensagem real. **Não copiei o 502 da rota irmã de propósito:** lá a venda é o payload inteiro, aqui é UM campo — derrubar a resposta jogaria fora teto, ranking e benchmarks, que estão calculados e corretos. Tem teste provando exatamente isso |
| **QA-448-02** | `resolveLeadSource` desempata `syncPendente` (espere) de `semDados` (configure), espelhando `public-leads.ts:47-68`. Uma query, só no ramo de ausência |
| **QA-448-03** | `maxAgeFrom` saiu de `public-funnel-sales.ts` para `utils/cache-freshness.ts`; as duas rotas passam a usar o mesmo, e a nova aceita `?fresh=1` |
| **QA-448-04** | `vendasSemData` → **`vendasSemDataNoTotal`**. O sufixo é o conserto: o diagnóstico não é escopável a range (venda sem data não pertence a janela nenhuma), e o nome antigo deixava o consumidor ler "N vendas faltando NESTE range" |
| **QA-448-05** | `expect(metricas.length).toBeGreaterThan(0)` antes dos `not.toContain` |

**Achado meu durante o conserto:** a rota passou a ler `fastify.config`, que o app de teste não decorava — teria quebrado todos os testes da família paga em runtime. Entrou um `configStub` no `buildApp`.

### Verificação por reversão — 12 defeitos reintroduzidos, 12 pegos

| # | defeito reintroduzido | testes que quebraram |
|---|---|---|
| 1 | elegibilidade do benchmark desligada | 3 |
| 2 | `mediana` virou média | 2 |
| 3 | custo da cadeia sem exigir os 3 elos | 1 |
| 4 | filtro `funnels.projectId` removido (IDOR) | 1 |
| 5 | `vendasSemData` zerado | 1 |
| 6 | guarda de cobertura omitida na gratuita | 1 |
| 7 | imposto Meta reaplicado | 6 (inclusive o da 44.1 — prova que a extração manteve o caminho do imposto compartilhado) |
| 8 | taxas servidas ×100 | 2 |
| 9 | `.catch(() => null)` de volta (QA-448-01) | 1 |
| 10 | `resolveLeadSource` desligado (QA-448-02) | 1 |
| 11 | `maxAgeMs` fixo, config ignorada (QA-448-03) | 2 |
| 12 | `METRICAS_RANQUEAVEIS` esvaziado — prova que a guarda da QA-448-05 pega o caso vazio | 1 |

### Validações

| gate | resultado |
|---|---|
| `turbo typecheck` (api, shared, web) | ✅ limpo |
| `turbo lint` (api, shared) | ✅ 0 erros (1 warning pré-existente em `lp-campaigns.ts:57`) |
| `turbo build` (4 tasks) | ✅ limpo |
| Suíte da API | **1198 passando, 11 falhando** — o baseline exato, nos mesmos 5 arquivos (`funnel-stages-lead-inputs`, `instagram-routes`, `instagram`, `projects`, `schema`). Nenhum arquivo novo entre eles |
| Testes novos | **53 passando** (29 de rota + 10 do `referenciasDoGrupo` + 14 da 44.1 preservados) |
| Gate 19.14 (bare import) | ✅ `node -e "import('@loyola-x/shared')"` de `packages/api` |
| Boot em runtime | ✅ a rota carrega via `tsx` (dev) **e** via `dist/` compilado (produção) |
| Artefatos `" 2"` | 4 arquivos em `packages/web/.next/types/` removidos — eram o `TS2300`, não são código meu |

### File List

**Criados**
- `packages/api/src/utils/cache-freshness.ts` — `maxAgeFrom` compartilhado — **QA-448-03**
- `packages/api/src/utils/meta-insight-agg.ts` — `MetricAgg`/`emptyAgg`/`InsightRow`/`accumulate` (movidos de `public-meta.ts`, sem mudança de comportamento)
- `packages/api/src/services/meta-campaign-daily.ts` — agrupamento (campanha → dia → agregado) — **AC2**
- `packages/api/src/routes/public-cadeia-cac.ts` — a rota — **AC1, AC3–AC8**
- `packages/api/src/__tests__/public-cadeia-cac.test.ts` — 23 testes — **AC10**
- `packages/api/src/__tests__/cadeia-cac-referencias.test.ts` — 10 testes — **AC11**

**Modificados**
- `packages/shared/src/cadeia-cac.ts` — `referenciasDoGrupo` + `ReferenciasDoGrupo` — **AC11**
- `packages/shared/src/index.ts` — export do acima
- `packages/shared/src/contract.ts` — `API_CONTRACT_VERSION` 2 → 3 — **AC9**
- `packages/api/src/routes/public-meta.ts` — helpers migrados; `campaigns/daily` chama o serviço — **AC2**
- `packages/api/src/app.ts` — registro da rota
- `packages/api/src/routes/public-funnel-sales.ts` — passa a importar `maxAgeFrom` do util — **QA-448-03**

### ⚠️ Pré-requisito de deploy (AC9)

`API_CONTRACT_VERSION` foi para `3`. Enquanto a API não for deployada, o banner do painel vai acusar defasagem — **que é o comportamento correto e o motivo de o bump morar aqui**. A 44.9 depende do deploy, não da story. Delegar a @devops.

---

---

## 🧪 QA Results

**Gate:** ❌ **FAIL** · **Reviewer:** @qa (Quinn) · **Data:** 2026-08-19 · **Commit:** `4486d7e9`
**Arquivo do gate:** `docs/qa/gates/44.8-endpoint-do-payload.yml`

### Veredito

10 das 11 ACs passam, e a qualidade da verificação é a mais alta do epic até aqui — 8 reversões executadas e contadas, incluindo o @dev diagnosticar e consertar o **próprio** teste de IDOR, que era decorativo com o banco mockado. A regra 7.6 está respeitada de forma verificável: **não existe uma única divisão no arquivo da rota**.

O que segura o gate é um defeito que a bateria não podia ver, porque **nenhum teste exercita falha de leitura**.

### 🔴 QA-448-01 (high, bloqueante) — falha de leitura vira afirmação falsa

`getFreshSalesDaily(...).catch(() => null)` (`public-cadeia-cac.ts:287`) engole qualquer erro e cai no mesmo ramo de "não há fonte conectada". **Reproduzido:** com a leitura rejeitando `Google Sheets: 403 PERMISSION_DENIED`, a rota devolve

```
200 · principal.motivo "semDados"
     principal.message "A etapa não tem nenhuma fonte de vendas conectada
                        (nem planilha, nem venda manual)."
```

O valor (`cacReal: null`) está certo. A **causa** está errada — e é a causa que determina a ação. "Conectar planilha" e "a leitura falhou, cheque a permissão" são ações opostas.

Isto tem precedente no próprio repo: a **Story 36.9 AC5** separou exatamente estas duas causas em `public-leads.ts:47-68`, porque a mensagem que as fundia fez o chamado de 2026-08-14 pedir *"rodar o sync"* para um problema que rodar o sync não resolvia. E a rota irmã `public-funnel-sales.ts:141-148` trata o mesmo caso com **502 `SALES_COMPUTE_FAILED`**.

**Conserto:** `try/catch` que distingue erro de payload nulo, mais um teste com `mockRejectedValue`. ~6 linhas.

### Demais achados

| id | sev | o quê |
|---|---|---|
| QA-448-02 | medium | A família gratuita **funde de volta** as duas causas que a 36.9 separou. `resolveLeadSource` já existe e é o desempate |
| QA-448-03 | medium | `SALES_PUBLIC_MAX_AGE_SEC` e `?fresh=1` são silenciosamente ignorados — a rota irmã os honra. Mesma classe de divergência que a AC2 combate, só que na política de frescor |
| QA-448-04 | low | `vendasSemData` descreve o total da etapa mas viaja ao lado de um número filtrado por range, sem declarar isso |
| QA-448-05 | low | O teste "ranking sem CPM e CTR" passaria com ranking vazio. Conferi: hoje traz 3 itens, então a asserção é real — mas não está guardada. Uma linha |

### O que NÃO é defeito

**A AC4 parcial está certa.** O @dev parou porque alimentar `atribuirPorCampanha` com a linha crua de `public-sales-rows.ts` criaria um **segundo `totalVendas`**, com dedup diferente do que já viaja no mesmo payload. Isso é decisão de produto, não de implementação, e declarar `naoAtribuivel` é o que a regra 7.4 manda. Verifiquei o efeito colateral: `cacReal`/`cplReal` seguem saindo, testado.

**O arredondamento por dia do `spend`** foi detectado pelo @dev, medido (3,3e-6 relativo), mantido por consistência com a 44.1 e **documentado no teste**. É a decisão certa e o registro certo.

### NFR

| | |
|---|---|
| **Segurança** | ✅ `requireScope` + `innerJoin`; teste de IDOR inspeciona o predicado do drizzle, provado por reversão; sem PII; 404 e não 403 |
| **Performance** | ✅ Medido: 10 camp × 365 d = **32 ms** · 20 × 365 = **49 ms** · 40 × 730 = **187 ms** para tetos + ranking + referências |
| **Confiabilidade** | ⚠️ QA-448-01 e QA-448-03 |
| **Manutenibilidade** | ✅ `public-meta.ts` encolhe 178 linhas e a duplicação some |

**Baseline de testes preservado:** 1192 passando / 11 falhando, os mesmos 5 arquivos ambientais. Nenhum arquivo novo entre as falhas.

### Por que FAIL e não CONCERNS

A AC8 é textual: *"nunca troca silenciosa de fonte"*. O QA-448-01 devolve 200 com uma afirmação falsa sobre a causa, e induz a ação errada — é o chamado de 2026-08-14 se repetindo num endpoint novo, contra um precedente que já está no repo.

FAIL não é bloqueio arbitrário: são ~6 linhas, nada está em produção e o endpoint ainda não tem consumidor. **Depois da 44.9, a mensagem falsa já estará na tela do Inácio.** QA-448-02 e QA-448-03 valem a mesma passada; 04 e 05 podem ir para o backlog.

⚠️ **CodeRabbit:** SKIPPED pelo quarto gate seguido (CLI em WSL, host macOS). É lacuna de cobertura, não aprovação.

## 📝 Change Log

| Data | Autor | Mudança |
|---|---|---|
| 2026-08-19 | @dev (Dex) | **QA fixes aplicados — os 5 achados do gate FAIL, numa passada.** O @qa acertou a causa raiz: **nenhum teste exercitava falha de leitura**, e por isso a bateria não podia ver o QA-448-01. Corrigidos os 3 bloqueantes/medium e também os 2 que ele mandou para o backlog — são uma linha cada. **Uma decisão de desenho, deliberada:** o QA-448-01 NÃO virou 502 como na rota irmã. Lá a venda é o payload inteiro; aqui é um campo, e derrubar a resposta jogaria fora teto, ranking e benchmarks já calculados e corretos. Virou `motivo: "leituraFalhou"` com 200, e tem teste afirmando que os outros blocos sobrevivem à falha. **Achado durante o conserto:** a rota passou a ler `fastify.config` e o app de teste não o decorava — quebraria toda a família paga; entrou `configStub`. **Mais 4 reversões (total 12/12).** Suíte: 1198 passando, 11 falhando — baseline intacto |
| 2026-08-19 | @dev (Dex) | **Implementada. Status Ready → Ready for Review.** 10 das 11 ACs fechadas. **AC2:** o agrupamento saiu de `public-meta.ts` para `services/meta-campaign-daily.ts` e os 4 helpers para `utils/meta-insight-agg.ts`; `campaigns/daily` passou a chamar o serviço e os 14 testes da 44.1 seguem passando sem alteração. **AC11:** `referenciasDoGrupo` no shared, com a elegibilidade — que `TetosDoGrupo` escondia — exposta por métrica; a régua é a mesma do teto (`selo` sobre o agregado da campanha), de propósito, para o benchmark não trocar de população conforme haja teto ou não. **AC4 não fecha inteira:** `sales-daily-sync.ts` não mapeia `utm_content` (zero ocorrências), e o único leitor row-level que mapeia é inline em `public-sales-rows.ts:181-211` e devolve linha crua sem dedup — alimentar `atribuirPorCampanha` com ela criaria um SEGUNDO `totalVendas` discordando do que já viaja no mesmo payload. Parei e declarei `naoAtribuivel` (regra 7.4) em vez de escolher a chave de dedup sozinho; `cacReal`/`cplReal` não são afetados e estão testados. **8 defeitos reintroduzidos por reversão, 8 pegos** — inclusive o teste de IDOR, que era decorativo com o banco mockado e ganhou inspeção do predicado montado pelo drizzle |
| 2026-08-19 | @po (Pax) | **Validada — GO condicional, 9/10.** Status Draft → Ready. **As três questões estão decididas e nenhuma volta para o @dev.** (1) `CoberturaDiaria` **fora** — e o custo da opção (b) estava subestimado: `LeadParaAtribuir` (`campaign-attribution.ts:39`) **não tem data**, `LeadOriginPayload` não tem bucket diário nenhum (a data só alimenta min/max do `range`, `:399-403`), e a dedup por dia contradiz o `Set` global da correção QA-44-01 — é decisão de design, não extensão. Superfície contida: `FAMILIA_GRATUITA` é `["free","cpl"]` e `cpl` tem zero campanhas (spec §1). (2) **Decimal**, porque a rota é nova e não compartilha nome com as irmãs — a própria AC7 prova, `convLP` não existe lá. (3) **Mediana vem para cá**, mas o argumento do @sm pela 7.6 não se sustenta sozinho: `mediana()` **já está exportada** (`cadeia-cac.ts:480`). O que decide é que o Inácio consome a API direto (`packages/mcp/src/tools.ts:97-195`) e ficaria **sem alvo** justamente nos 2 de 10 grupos sem teto (gate 44.5); e que `TetosDoGrupo` (`:520`) só expõe a campanha vencedora, então a aba teria que **re-derivar a régua do gate 44.5** para achar as elegíveis. **Achado meu, o mais perigoso:** `SalesDailyPayload.byDay` **não tem `vendas`** — a contagem é `ingressos.total` (`:364`), e venda sem data cai fora do `byDay` (`:338`) mas fica no `totalVendas` (`:354`); com range, o `cacReal` sai **alto** sem nada avisar. Virou requisito na AC4. Complexidade 8 → 10 pela AC11 |
| 2026-08-19 | @sm (River) | Criada. **Três achados durante o levantamento do terreno:** (a) o `GROUP BY campanha, dia` **já existe**, mas inline no handler de `campaigns/daily` (`public-meta.ts:846-897`) e sem reuso — virou a AC2, porque duas rotas montando a mesma série de dois jeitos é a semente da próxima divergência do `connectRate`; (b) **não existe nenhum produtor de `CoberturaDiaria`** — mesma classe de buraco que a 44.7 pegou duas vezes, e virou a AC5 com decisão explícita do @po em vez de descoberta na implementação; (c) o `convLP` da spec **não existe** no payload público — o `lpRate` de lá é o Connect Rate, e o `Conv. LP` precisa ser derivado de `checkouts`/`landingPageViews`. **Armadilha de renumeração registrada:** as referências a "44.8" dentro da 44.6 significam a 44.9 de hoje, porque a 44.6 é anterior ao deslocamento do QA-446-05. **Três questões abertas para o @po**, sendo a mais estrutural se a mediana de CPM/CPC (spec §5) sai deste endpoint — é cálculo, e cálculo mora no shared pela regra 7.6 |

<!-- clickup:86ak2uc89 -->
