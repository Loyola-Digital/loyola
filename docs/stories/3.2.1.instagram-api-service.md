# Story 3.2.1: Instagram Graph API Service + Metrics Cache

---

## Status

**Ready for Review**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [code_review, security_review]
priority: P0
estimate: L
depends_on: ["3.1.2"]
epic: "EPIC-3-META-INSTAGRAM"
```

---

## Story

**As a** backend service,
**I want** a centralized Instagram Graph API client with intelligent caching,
**so that** the app can fetch Instagram metrics efficiently while respecting Meta's rate limits.

---

## Acceptance Criteria

1. [ ] Service `instagram.ts` com metodo `validateToken(accessToken)` — GET /me valida token, retorna profile data ou erro
2. [ ] Metodo `getProfile(accountId)` — retorna followers_count, follows_count, media_count, biography, profile_picture_url, username
3. [ ] Metodo `getMediaList(accountId, limit?, after?)` — lista posts com paginacao cursor (id, caption, media_type, media_url, thumbnail_url, timestamp, like_count, comments_count)
4. [ ] Metodo `getMediaInsights(mediaId)` — metricas de post: impressions, reach, likes, comments, saves, shares, plays (para reels)
5. [ ] Metodo `getAccountInsights(accountId, period, since, until)` — metricas da conta: impressions, reach, follower_count, profile_views por dia
6. [ ] Metodo `getAudienceDemographics(accountId)` — audience_city, audience_country, audience_gender_age (period=lifetime)
7. [ ] Metodo `getStories(accountId)` — stories ativos com metricas (impressions, reach, replies, exits)
8. [ ] Metodo `getReels(accountId)` — reels com metricas (plays, likes, comments, reach, saves, shares)
9. [ ] Cache layer: verifica `instagram_metrics_cache` antes de chamar API
   - Profile: TTL 5 min
   - Post insights: TTL 15 min
   - Account insights: TTL 30 min
   - Demographics: TTL 1 hora
10. [ ] Cache hit retorna dados do banco; cache miss chama API, salva no banco, retorna
11. [ ] Rate limit tracking: contador interno, max 200 calls/hora por token
12. [ ] Se rate limit atingido: retorna erro claro "Rate limit atingido, tente novamente em X minutos"
13. [ ] Error handling: token expirado retorna erro especifico com mensagem de renovacao
14. [ ] Error handling: Graph API errors mapeados para mensagens uteis
15. [ ] Decripta access_token do banco via encryption service antes de usar
16. [ ] Graph API version: v21.0 (hardcoded como constante)
17. [ ] Testes unitarios com mocks da Graph API (fetch mockado)

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **HTTP Client:** Usar `fetch` nativo do Node.js 18+ (sem axios/got) [Source: architecture/fullstack-architecture-loyola-digital-x.md#5]
- **Fastify Service Pattern:** Decorar Fastify instance com service, similar ao `claude.ts` e `clickup.ts` [Source: packages/api/src/app.ts]
- **Encryption:** Usar `decrypt()` de `services/encryption.ts` (Story 3.1.2)
- **DB:** Consultar `instagram_metrics_cache` via Drizzle antes de fetch externo

### Meta Graph API v21.0 Endpoints

```
Base URL: https://graph.instagram.com/v21.0

GET /me?fields=id,name,username&access_token={token}  — Token validation
GET /{ig-user-id}?fields=id,username,name,biography,followers_count,follows_count,media_count,profile_picture_url  — Profile
GET /{ig-user-id}/media?fields=id,caption,media_type,media_url,thumbnail_url,timestamp,like_count,comments_count&limit={n}&after={cursor}  — Media list
GET /{media-id}/insights?metric=impressions,reach,likes,comments,saved,shares,plays  — Media insights
GET /{ig-user-id}/insights?metric=impressions,reach,follower_count,profile_views&period=day&since={unix}&until={unix}  — Account insights
GET /{ig-user-id}/insights?metric=audience_city,audience_country,audience_gender_age&period=lifetime  — Demographics
GET /{ig-user-id}/stories?fields=id,media_type,media_url,timestamp  — Stories
GET /{story-id}/insights?metric=impressions,reach,replies,exits  — Story insights
```

### Rate Limits

- 200 calls/user/hour (standard Graph API)
- Implementar contador in-memory com reset a cada hora
- Se proximo do limite (>180): logar warning

### Cache TTL Strategy

| Metric Type | TTL | Justificativa |
|------------|-----|---------------|
| profile | 5 min | Muda raramente, mas followers atualiza |
| post_insights | 15 min | Metricas estabilizam apos 24h |
| account_insights | 30 min | Agregados diarios, nao mudam rapido |
| demographics | 1 hora | Lifetime data, atualiza ~1x/dia |
| stories | 5 min | Stories expiram em 24h, dados mudam rapido |
| reels | 15 min | Similar a posts |

### Key Files

| File | Change |
|------|--------|
| `packages/api/src/services/instagram.ts` | **NEW** — Graph API client + cache |
| `packages/api/src/__tests__/instagram.test.ts` | **NEW** — Testes com mocks |

---

## Testing

### Validation Commands

```bash
cd packages/api
pnpm lint
pnpm typecheck
npx vitest run src/__tests__/instagram.test.ts
```

### Test Scenarios

1. validateToken com token valido retorna profile data
2. validateToken com token invalido retorna erro
3. getProfile com cache hit retorna dados do cache (sem fetch)
4. getProfile com cache miss chama API, salva cache, retorna
5. getMediaList retorna lista paginada de posts
6. getAccountInsights retorna metricas com periodo
7. getAudienceDemographics retorna dados lifetime
8. Rate limit atingido retorna erro claro
9. Token expirado retorna erro com mensagem de renovacao
10. Cache expiration: dados expirados forcam novo fetch

---

## Tasks / Subtasks

- [x] **Task 1: Criar Instagram service base** (AC: 16)
  - [x] 1.1 Criar `packages/api/src/services/instagram.ts`
  - [x] 1.2 Definir constante `GRAPH_API_VERSION = 'v21.0'` e `GRAPH_API_BASE`
  - [x] 1.3 Criar helper `graphFetch(path, token)` — fetch wrapper com error handling
  - [x] 1.4 Mapear Graph API errors para mensagens uteis (OAuthException, rate limit, etc.)

- [x] **Task 2: Implementar cache layer** (AC: 9, 10)
  - [x] 2.1 Criar funcao `getCachedMetric(accountId, metricType, periodStart?, periodEnd?)` — query ao banco
  - [x] 2.2 Criar funcao `setCachedMetric(accountId, metricType, data, ttlMinutes, periodStart?, periodEnd?)`
  - [x] 2.3 Criar funcao `invalidateCache(accountId, metricType?)` — para refresh manual

- [x] **Task 3: Implementar rate limiter** (AC: 11, 12)
  - [x] 3.1 Criar rate tracker in-memory: `Map<tokenHash, { count, resetAt }>`
  - [x] 3.2 Incrementar contador antes de cada fetch
  - [x] 3.3 Se count >= 200: lanca erro com tempo restante para reset

- [x] **Task 4: Implementar metodos de API** (AC: 1-8, 13, 14, 15)
  - [x] 4.1 `validateToken(accessToken)` — GET /me, retorna dados ou throw
  - [x] 4.2 `getProfile(accountId)` — decrypt token, check cache, fetch se necessario
  - [x] 4.3 `getMediaList(accountId, limit, after)` — paginacao cursor
  - [x] 4.4 `getMediaInsights(mediaId, accountId)` — metricas de post
  - [x] 4.5 `getAccountInsights(accountId, period, since, until)` — metricas diarias
  - [x] 4.6 `getAudienceDemographics(accountId)` — dados lifetime
  - [x] 4.7 `getStories(accountId)` — stories ativos + metricas
  - [x] 4.8 `getReels(accountId)` — reels + metricas

- [x] **Task 5: Testes** (AC: 17)
  - [x] 5.1 Criar `packages/api/src/__tests__/instagram.test.ts`
  - [x] 5.2 Mock global `fetch` para simular respostas da Graph API
  - [x] 5.3 Testar cada metodo: happy path + error path
  - [x] 5.4 Testar cache hit/miss behavior
  - [x] 5.5 Testar rate limiting
  - [x] 5.6 Executar `npx vitest run` — todos passam (75/75)
  - [x] 5.7 Executar `pnpm lint` e `pnpm typecheck` — zero erros

---

## File List

> Atualizado pelo @dev durante implementacao

| File | Status |
|------|--------|
| `packages/api/src/services/instagram.ts` | **NEW** — Graph API client + cache + rate limiter |
| `packages/api/src/__tests__/instagram.test.ts` | **NEW** — 11 tests with mocked fetch |
| `packages/api/src/app.ts` | Modified — Registered instagramServicePlugin |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log
- Used `onConflictDoUpdate` for cache upsert on composite unique constraint
- Reels filtered client-side from /media endpoint (Graph API has no dedicated reels-only endpoint)
- Rate limiter is in-memory per-process; resets on server restart (acceptable for single-instance deploy)
- Stories insights fetched per-story via Promise.all with error tolerance

### Completion Notes
- 5 tasks complete, all subtasks done
- 75 tests passing (11 new Instagram + 64 existing), 0 regressions
- Lint + typecheck: clean
- Service pattern follows clickup.ts (fastify-plugin, decorate)

### Change Log
- 2026-03-16: Tasks 1-5 implemented — service base, cache layer, rate limiter, API methods, tests

