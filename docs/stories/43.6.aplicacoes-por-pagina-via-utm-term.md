# Story 43.6: Aplicações por página vêm do `utm_term`, não do nome da aba

**Epic:** [43 — Dado que não mente](epics/epic-43-dado-que-nao-mente.md)
**Status:** InReview
**Created:** 2026-08-17 (@sm — River)
**Priority:** 🔴 Alta — é o chamado que **originou** o Epic 43, ainda aberto
**Complexity:** M (5 pontos) — o dado já existe; o que muda é como ele é agrupado
**Depends On:** 43.1 (descoberta de abas, aviso de LP órfã, `application-sheets.ts`)
**Origem:** reporte de 2026-08-17 sobre `dg-pg04` / etapa Vendas

---

## ⚠️ O Epic 43 nasceu deste chamado. Ele continua aberto.

O epic diz, na primeira linha:

> **Origem:** dois chamados independentes de 2026-08-14 — *"a Página C não aparece no gráfico de Aplicações"* e uma lista de 6 itens sobre métricas de vídeo/criativos.
>
> **Status:** Done (5 stories implementadas, revisadas e mergeadas)

**A Página C continua não aparecendo.** A 43.1 trocou o silêncio por um aviso — ganho real, porque antes o gráfico mentia sem avisar. Mas o aviso que aparece hoje **dá o conselho errado**, e a página segue invisível.

Não é regressão da 43.1. É a metade do problema que ela não alcançou: a 43.1 resolveu *"quais abas entram no gráfico"*; este é o problema de *"aba não é página"*.

---

## 💼 O que está na tela hoje, e o que é verdade

Etapa Vendas do `dg-pg04` (`db38a12a-6e71-4ba7-8d24-d504079a793b`):

> LPC, LPD, LPE, LPF, LPG, LPH, LPI estão rodando na Meta mas não têm aba de aplicação. Se essas páginas têm formulário, **crie a aba na planilha do lançamento.**

**O conselho não recupera o dado que já existe.** Medido na planilha real (`[DG-PG04-JUL26] Painel de Controle`, 72 abas) em 2026-08-17:

| Aba | Label no banco | Linhas | Quebra por `utm_term` |
|---|---|---|---|
| `Pesquisa-Aplicacao-Comercial` | **PAGINA A** | 17 | lpa: 2 · **lpc: 1** · sem LP: 14 |
| `Pesquisa-Aplicacao-Comercial-PaginaB` | PAGINA B | 53 | lpb: 44 · sem LP: 9 |

**E não é só o `dg-pg04`.** Levantamento do @po em 2026-08-17 — existem **3 planilhas de aplicação em produção, em 2 funis, e os dois têm aba-base**:

| Funil | Abas | Aba-base | LPs no `lp_links` |
|---|---|---|---|
| `dg-pg04` | 2 | `Pesquisa-Aplicacao-Comercial` (label "PAGINA A") | lpa, lpc, lpd, lpe, lpf, lpg, lph, lpi |
| `dg-pg02` | **1** | `Pesquisa-AplicaçãoComercial` (label `apc`) | lpa, lpb, lpc, lpf |

O `dg-pg02` roda 4 LPs e mostra **uma série só** com tudo somado — a mesma falha, em forma mais completa. Ele não recebe o aviso hoje (o label `apc` não parece nome de página, então a guarda funciona ali), o que torna o caso **pior**: erra em silêncio.

A linha 16 da aba rotulada "PAGINA A":

```
Submitted at = 2026-08-14 09:05:24
utm_term     = Instagram_Stories_dg-pg04-ago-26--vendas-principal-leads
               --2026-08-11--hot--cbo--estaticos-escassez--lpc|01_FD-…
```

**É uma aplicação da Página C, contada como Página A.** Criar a aba `-PaginaC` não a move para lá.

---

## 🔍 Causa raiz: `Pesquisa-Aplicacao-Comercial` não é a Página A

É a **aba-base** — o formulário genérico. Todas as páginas gravam nela, exceto a B, que ganhou aba própria. O rótulo "PAGINA A" foi cadastrado **à mão** em `funnel_spreadsheets.label` e não descreve o conteúdo.

O `lp_links` da Captação Paga confirma o desalinhamento: `lpa, lpc, lpd, lpe, lpf, lpg, lph, lpi` — **sem `lpb`**, justamente a única com aba própria.

### A guarda existia e foi anulada por um dado cadastrado à mão

`application-sheets.ts:99` já antecipava este caso:

> *"A aba-base de um grupo não carrega letra em lugar nenhum. Ela é a 'Página A' só por convenção do time, e cravar isso no código seria inventar semântica."*

Por isso `letrasDasFormas()` devolve `null` quando alguma forma não é identificável — e o chamador silencia o aviso. **Mas `letraDaPagina("PAGINA A")` devolve `"A"`**, porque o label manual parece uma identificação legítima. A guarda não dispara, e o aviso acusa LPC–LPI de órfãs.

O comentário do código chama isso de QA-17 e o trata como resolvido. Não está: a proteção cobre a aba-base **descoberta**, não a **cadastrada com label inventado**.

---

## 📖 Story

**Como** gestor acompanhando quais páginas geram aplicação,
**quero** que o gráfico agrupe pelas páginas que o `utm_term` declara, e não pelo nome da aba,
**para** que uma aplicação da Página C apareça como Página C mesmo caindo no formulário genérico.

---

## ✅ Critérios de aceite

### AC1 — Aba **com** sufixo de página continua declarando a página

`Pesquisa-Aplicacao-Comercial-PaginaB` → todas as suas 53 linhas seguem sendo Página B, inclusive as 9 sem `utm_term` identificável.

**Isto não é omissão, é a regra:** quem criou uma aba por página já declarou a intenção no nome. O `utm_term` não sobrepõe uma declaração explícita — só preenche onde não há nenhuma.

Comportamento atual preservado onde ele já está certo.

### AC2 — Aba **sem** sufixo (aba-base) quebra por `utm_term`

Cada linha vira a página que o `utm_term` declarar. No `dg-pg04`, a aba-base de 17 linhas produz:

| Série | Linhas |
|---|---|
| Página A | 2 |
| **Página C** | **1** ← aparece pela primeira vez |
| Sem página identificada | 14 |

### AC3 — Linhas sem página vão para uma série própria, não somem

14 das 17 linhas da aba-base não têm LP no `utm_term` (aplicação orgânica, tráfego direto, `utm_term` de segmentação como `publico-imersao`). Elas entram como **"Sem página identificada"**.

**Descartar seria trocar um número errado por um número menor e igualmente errado.** O total do gráfico não pode mudar por causa desta story.

### AC4 — ⚠️ A "Página A" vai cair de 17 para 2, e isso precisa ser explicado na tela

É a mudança mais visível, e a que mais chance tem de ser lida como bug novo. O número antigo estava errado; o novo está certo.

Colocar isso ao alcance de quem olhar — tooltip na série, nota no gráfico, o formato fica a critério do @ux. O texto precisa dizer que a série antiga somava páginas diferentes.

> Sem isto, alguém abre a tela na segunda-feira, vê "Página A: 2" e reporta como regressão. Aí a story vira dois chamados em vez de zero.

### AC5 — O aviso de LP órfã muda de critério: sem **aplicação**, não sem **aba**

Uma LP só é órfã quando **nenhuma aplicação** foi atribuída a ela — em nenhuma aba. No `dg-pg04`, LPC sai da lista; LPD–LPI continuam (não têm aplicação nenhuma, e aí o aviso está certo).

O texto muda junto: *"crie a aba"* deixa de ser o conselho, porque a aba não é mais o que determina a atribuição.

**E o que acontece com `letrasDasFormas()`?** Ela existe para silenciar o aviso quando alguma forma não é identificável — proteção que hoje é anulada pelo label manual. Com o AC2, as séries passam a **declarar** a própria letra (vêm do `utm_term`), então a pergunta "esta forma é identificável?" some junto com o problema.

Decidir na implementação se a função morre ou vira outra coisa. O que **não** pode acontecer é ela continuar consultando labels cadastrados à mão — foi isso que produziu o defeito. Verificado pelo @po em 2026-08-17: com os identificadores reais do `dg-pg04` ela devolve `['A','B']`; **sem** o label `PAGINA A`, devolveria `null` e o aviso não teria saído.

### AC6 — Extração da LP: reusar `extractLPName`, mas **ancorada**

`extractLPName` (`lp-campaigns.ts:52`) é `/lp([a-z])/i` — pega o primeiro `lp` + letra **em qualquer posição**.

**Isso é frouxo demais para o `utm_term`.** Nome de campanha é curto e controlado; `utm_term` é uma string longa e livre.

Testado pelo @po em 2026-08-17 (a primeira redação citava exemplos que **não** se confirmam — `help` e `pulp` devolvem `null`, porque o regex exige uma letra **depois** de `lp`):

| Entrada | `extractLPName` | |
|---|---|---|
| `alpha` | **LPH** | ❌ falso positivo |
| `alphabet` | **LPH** | ❌ falso positivo |
| `help` | `null` | ✅ |
| `pulp` | `null` | ✅ |
| `--lpc\|01_FD` | `LPC` | ✅ |
| `utm_term` real do `dg-pg04` | `LPC` | ✅ |

Um caso basta: **`lph` está no `lp_links` do `dg-pg04`**, então "LPH" saído de um `alpha` qualquer seria plausível demais para alguém desconfiar.

O padrão real observado é **delimitado**: `--lpc|`, `--lpa|`. Ancorar na fronteira (`--lpX` seguido de `|`, `--`, fim ou espaço).

Se a versão ancorada divergir da atual em algum caso, **não mudar o `extractLPName` existente nesta story** — ele é consumido por `lpsSemForma` e pela Story 18.44, e alterar semântica compartilhada aqui é escopo que ninguém pediu. Criar a variante ancorada e documentar a diferença.

### AC7 — Coluna de `utm_term`: achar sem quebrar

- A aba-base tem **três** colunas chamadas `utm_term` (índices 23, 47, 48) e uma `Ad Name` (54). **Só a [23] tem dados** — `headers.indexOf("utm_term")` funciona hoje, por sorte. Se um dia preencherem a 47, ele pega a errada.
- A aba B tem uma só (índice 22).
- Planilha **sem** coluna de `utm_term` não pode quebrar o gráfico: cai no comportamento atual (aba = série) e registra o motivo no mesmo canal de avisos do AC4 da 43.1.

Preferir a coluna **preenchida** à primeira homônima.

### AC8 — A porta de entrada da quebra é o `utm_term`, **não** o nome da aba

⚠️ **Corrigido pelo @po em 2026-08-17.** A primeira redação reusava `ehNomeDePagina` (a porta de entrada do *aviso*, da 43.1) como porta de entrada da *quebra*. **São perguntas diferentes**, e o critério errado exclui justamente quem mais se beneficia.

| Pergunta | Critério certo |
|---|---|
| Este funil deve receber o **aviso** de LP órfã? | os nomes das abas/labels seguem nomenclatura de página (`ehNomeDePagina`) — regra da 43.1, **inalterada** |
| Esta aba deve ser **quebrada** por página? | as linhas dela têm LP no `utm_term` |

**Prova de que o critério antigo estava errado — o `dg-pg02`:**

| | `dg-pg02` |
|---|---|
| Abas de aplicação | **1**, `Pesquisa-AplicaçãoComercial` (base) |
| Label no banco | `apc` |
| `ehNomeDePagina("apc")` | **false** |
| `lp_links` da Captação Paga | **`lpa, lpb, lpc, lpf`** |

Pelo AC8 original, o `dg-pg02` seria **excluído** da quebra — porque o label é `apc` e não "PAGINA A". Mas ele roda 4 LPs, e hoje mostra **uma única série** com tudo somado: exatamente o defeito que esta story existe para corrigir, na sua forma mais completa.

Funil que de fato não fala a língua de páginas continua intocado — mas quem decide isso é a **ausência de LP nas linhas**, não a grafia do label.

### AC9 — Funil sem nenhuma LP no `utm_term` não muda

Se nenhuma linha de uma aba tem LP identificável, ela continua sendo uma série só, com o label atual. Nada de série "Sem página identificada" solitária substituindo um gráfico que funcionava.

---

## 🚫 Fora de escopo

- **Renomear o label "PAGINA A"** da aba-base no banco — depois do AC2 ele deixa de decidir qualquer coisa. Vale como limpeza, não como parte do fix.
- **Corrigir `extractLPName` para os outros consumidores** — ver AC6.
- **Criar as abas `-PaginaC` … `-PaginaI`** — deixa de ser necessário; é justamente o conselho que esta story remove.
- **Investigar por que LPB não está no `lp_links`** enquanto tem aba própria. Anomalia real, outra conversa.
- **As 14 linhas sem LP** — descobrir de onde vieram é análise de tracking, não código.
- Mudar o gráfico de leads, criativos ou qualquer outra tela que agrupe por LP.

---

## ⚠️ Riscos

| Risco | Mitigação |
|---|---|
| **"Página A: 2"** ser lido como regressão | AC4 é obrigatório, não cosmético |
| `alpha` → LPH e outros falsos positivos | AC6 (extração ancorada) |
| Mudar `extractLPName` e quebrar 18.44 / `lpsSemForma` | AC6 proíbe explicitamente |
| Coluna `utm_term` duplicada mudar de posição | AC7 (preferir a preenchida) |
| Total do gráfico mudar | AC3 (série "Sem página identificada") |
| Aba B perder as 9 linhas sem `utm_term` | AC1 (sufixo declara a página) |
| Ganho parecer maior do que é | **No `dg-pg04` a story recupera 1 aplicação e separa 2.** O valor está no modelo ficar correto, não no volume. Dizer isso na hora de apresentar |

---

## 📓 Notas para o @dev

- **`application-sheets.ts` é o lugar.** É o módulo que a 43.1 criou para "decidir o que entra no gráfico", e é puro — testável sem Google e sem banco. A quebra por `utm_term` pertence a ele; a rota só orquestra.
- **`contar()` (`stage-applications.ts:150`) hoje devolve uma forma por aba.** Com o AC2 uma aba-base pode gerar N formas. É a mudança estrutural da story — o resto decorre dela.
- O alinhamento em D-day (`alignForms`) já lida com séries de tamanhos diferentes; séries novas entram sem tratamento especial.
- **Medir antes e depois**, por série, no `dg-pg04`. O AC4 depende de você saber os dois números.
- Planilha real para testar: `1mZxquxN9ALYRf6Y2vOjWTdtUyKnzLH41LPBDChYJhbo`, abas `Pesquisa-Aplicacao-Comercial` e `...-PaginaB`.
- Rodar código contra Google/banco: `.ts` em `packages/api/src/scripts/` + `npx tsx`. **O `dotenv` não carrega sozinho** — `dotenv.config({ path: new URL("../../.env", import.meta.url).pathname })` no topo, senão dá "GOOGLE_SERVICE_ACCOUNT_KEY nao configurado".

---

## 📋 Definition of Done

- [ ] AC1–AC9 atendidos
- [ ] Testes em `application-sheets.ts`: aba com sufixo (AC1), aba-base quebrada (AC2), linhas sem LP (AC3), **`alpha` não virando LPH** (AC6), planilha sem coluna `utm_term` (AC7), aba com label não-página mas com LP no `utm_term` (AC8), aba sem nenhuma LP (AC9)
- [ ] Antes/depois por série **nos dois funis** (`dg-pg04` **e** `dg-pg02`) registrado no Dev Agent Record
- [ ] `dg-pg02` quebrado por página apesar do label `apc` (AC8) — a prova de que a porta de entrada é o `utm_term`
- [ ] **Página C visível no gráfico** — o chamado que originou o Epic 43
- [ ] Total do gráfico inalterado
- [ ] `tsc --noEmit` limpo (com o `shared` compilado)
- [ ] Validação visual na etapa real — **não pular**
- [ ] File List preenchida · ClickUp atualizado

---

---

## 🤖 Dev Agent Record

**Status:** Ready → InProgress → InReview
**Agent:** @dev (Dex) · 2026-08-17 · Branch `feature/43.6-aplicacoes-por-utm-term` (de `origin/main`)

### Medição antes/depois — executada contra as planilhas de produção

| Funil / aba | Antes | Depois | Total |
|---|---|---|---|
| `dg-pg04` / `Pesquisa-Aplicacao-Comercial` | 1 série "PAGINA A" = **17** | PAGINA A **2** · **PAGINA C 1** · Sem página **14** | preservado ✅ |
| `dg-pg04` / `…-PaginaB` | 1 série "PAGINA B" = **53** | PAGINA B **53** (AC1, inalterada) | preservado ✅ |
| `dg-pg02` / `Pesquisa-AplicaçãoComercial` | 1 série "apc" = **49** | apc **49** (AC9, inalterada) | preservado ✅ |

**A Página C aparece.** É o chamado que originou o Epic 43, fechado.

### ⚠️ A premissa do @po sobre o `dg-pg02` não se confirmou

O AC8 previa que o `dg-pg02` se beneficiaria — tem `lp_links` com 4 LPs e uma aba-base só. **Ele não quebra**, e o motivo está no dado: os `utm_term` dele não trazem LP.

```
dg-pg04: …--hot--cbo--estaticos-escassez--lpc|01_FD-ST…     ← tem --lpX
dg-pg02: …--vendas-principal--2026-05-11—hot--cbo--estaticos ← não tem
```

Ter LP cadastrada em `lp_links` **não implica** que os anúncios carreguem a LP no `utm_term`. São coisas independentes, e só a segunda permite atribuir.

**Isto não invalida o AC8** — o critério corrigido (presença de LP na linha, não grafia do label) continua certo, e é justamente ele que faz o `dg-pg02` cair no AC9 em vez de virar uma série "Sem página identificada" solitária. O que não se confirma é o exemplo usado para justificar a correção.

**Consequência boa:** o alcance real é **um funil só**. O `dg-pg02` não muda nada.

### Decisões de implementação

1. **`letraDaLpNoUtmTerm` é função nova, não um patch no `extractLPName`** (AC6). Ancorada em `(?:^|[-_|\s])lp([a-z])(?=$|[-_|\s])`. Confirmado por teste: `alpha` e `alphabet` **não** viram LPH. O `extractLPName` segue intocado, como o AC6 exige.

2. **O aviso passou a ler as séries do gráfico, não os labels do banco.** `lpsSemForma` recebe `letrasComForma` — as letras das séries com `ehPagina: true`. Era o label "PAGINA A" cadastrado à mão que anulava a guarda da 43.1; ler a série resolve na origem. `letrasDasFormas` continua exportada e testada, sem alteração de semântica.

3. **`RawForm.ehPagina`** é o que sustenta o AC5. A série "Sem página identificada" e a aba-base não quebrada valem `false` — não podem servir de prova de que a página X tem aplicação.

4. **O aviso ganhou ressalva em vez de sumir.** Com 14 aplicações sem página no `dg-pg04`, dizer "LPD não teve aplicação" seria afirmar mais do que se sabe — alguma das 14 pode ser dela. O payload leva `aplicacoesSemPagina` e a tela diz: *"Há 14 aplicações sem página identificada — algumas podem ser destas páginas."*

5. **`aggregateForms` recebeu `ehPagina: false`** — o agregado é a soma de todas as páginas, então não é página nenhuma e não entra no cálculo do aviso.

### Validações

| Verificação | Resultado |
|---|---|
| `vitest` — `application-sheets.test.ts` | ✅ **43/43** (18 novos) |
| `vitest` — suite da API | 1035 passam · 11 falhas pré-existentes (mesmos 5 arquivos de sempre) |
| `tsc --noEmit` (api + web) | ✅ limpo |
| `eslint` (5 arquivos) | ✅ limpo |
| Medição contra planilhas reais | ✅ 3 abas, total preservado nas 3 |

### File List

**Modificados**
- `packages/api/src/services/application-sheets.ts` — `letraDaLpNoUtmTerm`, `acharColunaUtmTerm`, `agruparPorPagina`, `SEM_PAGINA`
- `packages/api/src/routes/stage-applications.ts` — `contar()` devolve `RawForm[]`; `ehPagina`; `letrasComForma`/`semPagina`; `lpsSemForma` lê séries
- `packages/api/src/__tests__/application-sheets.test.ts` — 18 testes novos
- `packages/web/lib/hooks/use-stage-applications.ts` — `aplicacoesSemPagina`
- `packages/web/components/funnels/applications-daily-chart.tsx` — novo texto do aviso (AC5), nota do AC4, subtítulo

### ⚠️ Validação visual não executada

Exige login Clerk. **Não marcada como feita.** O efeito está medido contra as planilhas reais (tabela acima), mas que a série "PAGINA C" desenhe na tela não foi confirmado.

---

## 📝 Change Log

| Data | Autor | Mudança |
|---|---|---|
| 2026-08-17 | @dev (Dex) | **Implementada.** Status `Ready → InProgress → InReview`. Medido contra as 3 abas de produção, total preservado nas 3: no `dg-pg04` a aba-base 17 → PAGINA A 2 + **PAGINA C 1** + Sem página 14, e a `-PaginaB` intacta em 53. **A premissa do @po sobre o `dg-pg02` não se confirmou:** ele tem 4 LPs em `lp_links` mas os `utm_term` dele não trazem LP nenhuma, então cai no AC9 e não muda — ter LP cadastrada não implica LP no anúncio. O AC8 continua certo; só o exemplo que o justificava não se sustenta. Alcance real: **um funil**. 43/43 testes, `tsc` e `eslint` limpos. Validação visual NÃO executada |
| 2026-08-17 | @po (Pax) | **Validada — GO condicional, 8/10.** Status `Draft → Ready` após 4 correções. **(1) AC8 reescrito** — usava `ehNomeDePagina` (porta de entrada do *aviso*) como porta de entrada da *quebra*; critério errado que **excluía o `dg-pg02`**, o funil que mais se beneficia (4 LPs numa série só, errando em silêncio). **(2) Escopo ampliado**: são 2 funis afetados, não 1 — levantamento das 3 planilhas de aplicação de produção. **(3) AC9 criado**: funil sem LP no `utm_term` não muda. **(4) AC6 corrigido**: `help` e `pulp` NÃO são falsos positivos (testado); `alpha`/`alphabet` → LPH são, e um basta. Causa raiz confirmada por execução: `letrasDasFormas` devolve `['A','B']` com o label manual e `null` sem ele |
| 2026-08-17 | @sm (River) | Criada a partir do reporte sobre `dg-pg04`. Tudo medido na planilha de produção em 2026-08-17: as 72 abas, a inexistência de `-PaginaC`, a quebra por `utm_term` (lpa:2 · lpc:1 · 14 sem LP na aba-base; lpb:44 · 9 sem LP na B) e a linha 16 com `--lpc\|` datada de 2026-08-14. Solução escolhida pelo time entre duas alternativas: quebrar por `utm_term` dentro da aba, em vez de exigir uma aba por página. Registrado que este é o chamado que **originou** o Epic 43 e que o epic foi fechado como Done com o sintoma vivo |

<!-- clickup:86ak1hyvp -->
