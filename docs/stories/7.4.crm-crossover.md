# Story 7.4: CRM Crossover — Leads por UTM + CPL Real

---

## Status

**Draft**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation]
priority: P0
estimate: L
depends_on: ["7.1", "7.2", "7.3"]
epic: "EPIC-7-TRAFFIC-ANALYTICS"
```

---

## Story

**As a** gestor de trafego da Loyola Digital,
**I want** the dashboard to cross-reference Meta Ads campaign data with real leads from the Google Sheets CRM via UTM parameters,
**so that** I can see real conversions, CPL (cost per lead), and identify which campaigns, ad sets, and ads are actually generating leads — not just clicks.

---

## Acceptance Criteria

1. [ ] Service `traffic-analytics.ts`: cruza dados Meta Ads + Google Sheets leads via UTM matching
2. [ ] Match logic: UTM Campaign ↔ campaign name/id, UTM Medium ↔ adset name/id, UTM Content ↔ ad name/id
3. [ ] Match flexivel: case-insensitive, trim whitespace, ignore leading/trailing spaces
4. [ ] `GET /api/traffic/analytics/:projectId/overview?days=30` — resumo: total spend, total leads, CPL medio, total campanhas
5. [ ] `GET /api/traffic/analytics/:projectId/campaigns?days=30` — campanhas com metricas de midia + leads + CPL
6. [ ] `GET /api/traffic/analytics/:projectId/adsets?campaignId=X&days=30` — ad sets com leads + CPL
7. [ ] `GET /api/traffic/analytics/:projectId/ads?adsetId=X&days=30` — ads com leads + CPL
8. [ ] Resposta de cada rota inclui: nome | spend | impressions | clicks | CTR | CPC | CPM | leads | CPL
9. [ ] CPL calculado: spend / leads (retorna null se leads = 0)
10. [ ] Dashboard `/traffic` atualizado: tabela de campanhas agora mostra colunas Leads e CPL quando projeto tem Sheets conectado
11. [ ] Indicador visual no dashboard: badge "CRM Conectado" (verde) ou "Apenas Midia" (cinza) por projeto
12. [ ] Leads sem UTM atribuivel mostrados como "Sem atribuicao" em linha separada na tabela
13. [ ] Cache de dados cruzados: TTL 15min (invalidar ao re-mapear abas no settings)
14. [ ] Drill-down de campanhas agora inclui leads/CPL em cada nivel (campanha → ad set → ad)
15. [ ] `pnpm typecheck` e `pnpm lint` passam sem erros

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **Meta Ads service:** `packages/api/src/services/meta-ads.ts` — ja tem `fetchCampaigns()`, `fetchInsights()`, `fetchAdSets()`, `fetchAds()` (apos 7.2)
- **Google Sheets service:** `packages/api/src/services/google-sheets.ts` — ja tem `getTabData()` (apos 7.1)
- **DB schema:** `googleSheetsConnections` + `googleSheetsTabMappings` com `columnMapping` JSONB
- **Dashboard:** `packages/web/app/(app)/traffic/page.tsx` — charts + tabela de campanhas (apos 7.2)

### UTM Matching Logic

```typescript
// O match entre Meta Ads e Google Sheets e feito assim:
//
// 1. Buscar campanhas do Meta Ads (com metricas)
// 2. Buscar dados da aba "leads" do Sheets (com column mapping)
// 3. Para cada lead:
//    - Extrair UTM Campaign, UTM Medium, UTM Content do lead
//    - Normalizar: lowercase, trim, remove whitespace extra
// 4. Agrupar leads por UTM Campaign:
//    - Contar quantos leads cada campanha gerou
//    - Match com campanha Meta Ads por nome (case-insensitive) ou ID
// 5. Calcular CPL: campaign.spend / leads_count
//
// Exemplo de dados da planilha (aba Leads):
// | Email           | UTM Campaign  | UTM Medium     | UTM Content    |
// | joao@mail.com   | campanha_1    | publico_frio   | criativo_video |
// | maria@mail.com  | campanha_1    | publico_quente | criativo_img   |
// | pedro@mail.com  | campanha_2    | publico_frio   | criativo_video |
//
// Resultado do cruzamento:
// campanha_1: spend R$500, 2 leads, CPL R$250
// campanha_2: spend R$300, 1 lead, CPL R$300
```

### Service Pattern (traffic-analytics.ts)

```typescript
// Novo service: packages/api/src/services/traffic-analytics.ts

interface CampaignAnalytics {
  campaignId: string;
  campaignName: string;
  status: string;
  // Metricas de midia
  spend: number;
  impressions: number;
  clicks: number;
  ctr: number;
  cpc: number;
  cpm: number;
  // CRM data (null se Sheets nao conectado)
  leads: number | null;
  cpl: number | null;
}

export async function getCampaignAnalytics(
  projectId: string,
  metaAccountId: string,
  accessToken: string,
  sheetsConnection: SheetsConnection | null, // null se nao tem Sheets
  days: number
): Promise<CampaignAnalytics[]> {
  // 1. Fetch campaign insights from Meta Ads
  // 2. If sheetsConnection exists, fetch leads from Sheets tab
  // 3. Group leads by UTM Campaign
  // 4. Match leads to campaigns
  // 5. Calculate CPL
  // 6. Return merged array
}
```

### Normalizacao de UTMs

```typescript
function normalizeUtm(value: string | undefined | null): string {
  if (!value) return "";
  return value.trim().toLowerCase().replace(/\s+/g, " ");
}

function matchUtmToCampaign(
  utmValue: string,
  campaignName: string,
  campaignId: string
): boolean {
  const normalized = normalizeUtm(utmValue);
  return (
    normalized === normalizeUtm(campaignName) ||
    normalized === normalizeUtm(campaignId)
  );
}
```

### Cache Strategy

```typescript
// Cache in-memory per-project com TTL 15min
// Key: `analytics:${projectId}:${days}`
// Invalidar quando:
// - Tab mappings sao atualizados (PUT /api/google-sheets/connections/:id/tabs)
// - Nova conta Meta Ads e vinculada ao projeto

const ANALYTICS_CACHE_TTL = 15 * 60 * 1000; // 15 min
```

### API Routes

```typescript
// Novo arquivo: packages/api/src/routes/traffic-analytics.ts

export default fp(async function trafficAnalyticsRoutes(fastify) {
  // GET /api/traffic/analytics/:projectId/overview?days=30
  // GET /api/traffic/analytics/:projectId/campaigns?days=30
  // GET /api/traffic/analytics/:projectId/adsets?campaignId=X&days=30
  // GET /api/traffic/analytics/:projectId/ads?adsetId=X&days=30
});
```

### Dashboard Updates

```
CampaignTable (atualizada)
├── Coluna: Nome | Status | Spend | Impressions | Clicks | CTR | CPC | CPM | [Leads] | [CPL]
│   ↑ Colunas Leads e CPL so aparecem quando projeto tem Sheets conectado
├── Badge "CRM Conectado" (verde) no topo quando Sheets esta ativo
├── Linha "Sem atribuicao" no final com leads que nao matcharam nenhuma campanha
└── Drill-down inclui Leads + CPL em cada nivel
```

### Key Files

| File | Change |
|------|--------|
| `packages/api/src/services/traffic-analytics.ts` | **Novo** — servico de cruzamento Ads × CRM |
| `packages/api/src/routes/traffic-analytics.ts` | **Novo** — rotas de analytics por projeto |
| `packages/api/src/app.ts` | Registrar plugin traffic-analytics routes |
| `packages/web/app/(app)/traffic/page.tsx` | Atualizar: colunas Leads/CPL, badge CRM, linha "sem atribuicao" |
| `packages/web/lib/hooks/use-traffic-analytics.ts` | **Novo** — hooks para analytics cruzado |

### Previous Story Insights (7.1, 7.2, 7.3)

- Google Sheets service ja fornece `getTabData()` com column mapping aplicado
- Meta Ads service ja fornece `fetchCampaignInsights()` com metricas por campanha
- Dashboard ja tem tabela expandivel com drill-down
- Column mapping JSONB tem campos UTM mapeados (utmCampaign, utmMedium, utmContent)

---

## Tasks

- [ ] **Task 1 (AC: 1, 2, 3):** Criar `packages/api/src/services/traffic-analytics.ts` — funcoes `getCampaignAnalytics()`, `getAdSetAnalytics()`, `getAdAnalytics()` com UTM matching logic (case-insensitive, trim)
- [ ] **Task 2 (AC: 9, 12):** Implementar logica de agrupamento de leads por UTM + calculo de CPL + bucket "sem atribuicao"
- [ ] **Task 3 (AC: 13):** Implementar cache in-memory com TTL 15min + funcao de invalidacao
- [ ] **Task 4 (AC: 4):** Implementar rota `GET /api/traffic/analytics/:projectId/overview` — resumo com spend total, leads total, CPL medio
- [ ] **Task 5 (AC: 5, 8):** Implementar rota `GET /api/traffic/analytics/:projectId/campaigns` — campanhas com metricas + leads + CPL
- [ ] **Task 6 (AC: 6):** Implementar rota `GET /api/traffic/analytics/:projectId/adsets?campaignId=X`
- [ ] **Task 7 (AC: 7):** Implementar rota `GET /api/traffic/analytics/:projectId/ads?adsetId=X`
- [ ] **Task 8:** Registrar plugin em `packages/api/src/app.ts`
- [ ] **Task 9:** Criar `packages/web/lib/hooks/use-traffic-analytics.ts` — hooks `useTrafficOverview`, `useTrafficCampaigns`, `useTrafficAdSets`, `useTrafficAds`
- [ ] **Task 10 (AC: 10, 11, 14):** Atualizar dashboard `/traffic/page.tsx` — adicionar colunas Leads/CPL na tabela, badge "CRM Conectado", linha "sem atribuicao", drill-down com leads/CPL
- [ ] **Task 11 (AC: 15):** Executar `pnpm typecheck` e `pnpm lint`

---

## Testing

- Cruzamento funciona: leads da planilha matcham com campanhas por UTM
- Match case-insensitive: "Campanha_1" == "campanha_1"
- Match com trim: " campanha_1 " == "campanha_1"
- CPL calculado corretamente: spend / leads
- CPL null quando leads = 0
- Leads sem UTM aparecem como "sem atribuicao"
- Cache funciona: segunda chamada e rapida (dentro do TTL)
- Dashboard mostra colunas Leads/CPL apenas quando Sheets conectado
- Badge "CRM Conectado" aparece quando projeto tem Sheets
- Badge "Apenas Midia" aparece quando nao tem Sheets
- Drill-down mostra leads/CPL em todos os niveis
- typecheck e lint passam

---

## Dev Agent Record

### File List

| File | Status |
|------|--------|
| | |

### Debug Log

_(empty)_

### Completion Notes

_(empty)_

### Change Log

_(empty)_
