# Story 36.9: LEADS POPUP da Captação Paga — elegibilidade do `lead-origin-sync`

**Epic:** [36 — Meta Ads Creative Intelligence API + MCP](epics/epic-36-meta-ads-creative-intelligence.md)
**Status:** Ready
**Created:** 2026-08-14 (@sm — River)
**Priority:** 🟠 Média — item 4 do chamado de 2026-08-14
**Complexity:** M (3 pontos)
**Depends On:** — (36.7 já entregou o cache e o endpoint)
**Relacionada:** 36.7 (Buraco 2 — leads por origem), 18.52 (ingressos únicos ≠ leads na Paga)

---

## ⚠️ O chamado pede "rodar o sync". Rodar o sync não resolve.

O item 4 diz: *"Rodar o sync dos LEADS POPUP da etapa Captação Paga (`get_stage_leads_summary` = semDados)"*.

**A etapa não é elegível ao sync.** `syncLeadOrigin` (`lead-origin-sync.ts:329-353`) monta a lista de etapas a processar a partir de duas fontes, e só delas:

1. `stage_lead_scoring_schemas.survey_id` não nulo
2. `funnel_surveys.stage_id` não nulo

E `computeLeadOriginForStage` (`:138-173`) lê os leads **exclusivamente de uma planilha de pesquisa** (`funnel_surveys`): sem survey, `return null` → `stagesSkipped++` → nada é gravado no cache → o endpoint devolve `semDados`.

**Os LEADS POPUP não vivem em `funnel_surveys`.** Vivem em `funnel_spreadsheets` com `type = 'leads'` (`schema.ts:1025-1037`) — tabela que o sync não consulta em nenhum ponto. O próprio código registra a distinção em `useCrossReferenceLeads.ts:110`: *"na Paga é a de `sales` (n8n-kiwify-captação, a de leads é popup)"*.

Rodar o sync hoje produz **exatamente o mesmo `semDados`**, sem erro nenhum no log — a etapa simplesmente não aparece na lista. É por isso que isto é uma story e não uma tarefa de operação.

> Mesmo padrão dos três itens que a investigação do Epic 43 derrubou: o pedido descreve o sintoma corretamente e a causa incorretamente.

---

## 💼 Valor de negócio

Destravar `get_stage_leads_summary` na Captação Paga entrega o split **Pago / Orgânico / Sem Track** e os **leads únicos** — e, com eles, o CPL único (pago e geral) para quem consome o feed.

Hoje a etapa devolve `semDados`, que no payload atual é indistinguível de "o sync ainda não rodou".

---

## 🔍 O que existe hoje (verificado na `main` `82c65d3e`)

| Peça | Onde | Estado |
|---|---|---|
| Endpoint | `routes/public-leads.ts:23` | lê `public_metrics_cache`, scope `LEAD_ORIGIN_SCOPE` |
| Resposta sem cache | `public-leads.ts:47-54` | `semDados: true` |
| Elegibilidade | `lead-origin-sync.ts:329-353` | Lead Scoring ∪ `funnel_surveys` |
| Fonte dos leads | `lead-origin-sync.ts:146-173` | só `funnel_surveys` → `readSheetData` |
| Colunas | `lead-origin-sync.ts:183-192` | por `ALIASES` sobre os headers, **ignora `column_mapping`** |
| Classificação | `utils/lead-origin.ts` | origem + temperatura — **reusar, não reescrever** |
| Agendamento | `plugins/meta-perf-scheduler.ts:61` | ciclo diário (`META_PERF_SYNC_HOUR`, default 4h) |
| Backfill manual | `scripts/backfill-lead-origin.ts` | primeira carga / debug |

### A planilha de leads tem UTM — o split é viável

Confirmado em `schema.ts:1081-1086`: `funnel_spreadsheets.column_mapping` carrega `utm_source`, `utm_medium`, `utm_campaign`, `utm_content` e `utm_term`, além de `email`, `phone` e `date`.

Ou seja, **tudo que `utils/lead-origin.ts` precisa para classificar origem e deduplicar já está mapeado.** O que falta é o sync chegar até lá.

---

## 📖 Story

**Como** quem consome o feed público de leads,
**quero** que etapas com planilha de LEADS conectada entrem no `lead-origin-sync`,
**para** que a Captação Paga pare de responder `semDados` e entregue o split por origem e os únicos.

---

## ✅ Critérios de aceite

### AC1 — Planilha de leads torna a etapa elegível

`syncLeadOrigin` passa a incluir também as etapas com `funnel_spreadsheets.stage_id` não nulo e `type = 'leads'`, unidas às duas fontes atuais no mesmo `Map` por `stageId` (`lead-origin-sync.ts:355-358`).

### AC2 — A planilha de leads é a fonte de TODO o payload

Quando a etapa tem planilha `type = 'leads'`, é **ela** a fonte do `leads-summary` inteiro: `totalLeads`, `uniqueLeads` e o split por origem **e por temperatura**.

**Não há cruzamento com a pesquisa** — os três eixos derivam de UTM e contato, não de respostas:

| Eixo | De onde sai |
|---|---|
| Origem | `classifyOrigem(utm_source)` — `lead-origin.ts:26` |
| Temperatura | `classifyTemperatura(utm_term)` — `lead-origin.ts:72`, usado em `lead-origin-sync.ts:232` |
| Únicos | e-mail + `phoneTail(phone)` — `lead-origin.ts:81` |

Os três campos estão no `column_mapping` de `funnel_spreadsheets` (`schema.ts:1081-1086`). A pesquisa segue sendo fonte de **faixa** (Lead Scoring), que é outro endpoint.

> Implementar isto como cruzamento leads × pesquisa por e-mail seria trabalho que o código dispensa — e estouraria a estimativa M.

Contar leads pela planilha de pesquisa é contar **quem respondeu a pesquisa** — subconjunto de quem virou lead. Um proxy que subestima e não se anuncia, que é a classe de defeito que o Epic 43 fechou.

**Aprovado pelo @po em 2026-08-14**, condicionado ao AC3 (fonte declarada) e ao AC6 (delta medido) — porque muda número em etapa que hoje já produz cache.

### AC3 — O payload declara de onde veio o número

A resposta passa a carregar a fonte usada:

```json
{ "fonte": "planilha_leads" | "pesquisa" | "lead_scoring" }
```

Sem isso, dois números com significados diferentes chegam com a mesma forma a quem consome — e ninguém sabe se está lendo leads ou respondentes.

### AC4 — Colunas vêm do `column_mapping` configurado, com alias como fallback

Hoje as colunas são descobertas por `ALIASES` sobre os headers (`lead-origin-sync.ts:183-192`). Para a planilha de leads, usar o `column_mapping` **explícito** e cair nos aliases só quando o mapeamento não tiver o campo.

O mapeamento foi configurado por alguém que sabia qual coluna era qual; heurística sobre header é o palpite que se usa na ausência dele.

### AC5 — `semDados` para de juntar duas causas opostas

A mensagem atual (`public-leads.ts:52`) diz *"sync ainda não rodou ou stage sem survey"* — duas causas com ações opostas: uma pede esperar, a outra pede configurar.

Separar:

| Situação | Resposta |
|---|---|
| Etapa elegível, cache ainda não computado | `semDados: true, motivo: "sync_pendente"` |
| Etapa sem nenhuma planilha de leads/pesquisa | `semDados: true, motivo: "etapa_sem_fonte"` |

Foi essa ambiguidade que fez o chamado pedir "rodar o sync" para um problema que rodar o sync não resolve.

### AC6 — Etapa que já funciona não muda em silêncio

Se o AC2 alterar o número de alguma etapa que hoje já produz cache, **medir o delta antes e depois** e registrar no Dev Agent Record.

Número que sobe porque a fonte passou a ser a correta **não é regressão** — mas se algum relatório já foi apresentado com o número antigo, ele mudou. Mesma advertência que a 43.2 deixou registrada no Epic 43.

### AC7 — Uma etapa quebrada não derruba as outras

Preservar o `try/catch` por etapa de `syncLeadOrigin` (`:361-374`): planilha ilegível vira `errors[]` e o laço continua.

---

## 🚫 Fora de escopo

- Calcular CPL único no endpoint — é derivado (leads únicos ÷ investimento) por quem consome
- Mudar `utils/lead-origin.ts` — a classificação está correta e tem uso além desta story
- Ingressos únicos vs leads na Captação Paga (18.51/18.52) — outra leitura, outro consumidor
- Criar tela de configuração de planilha de leads — já existe
- Backfill histórico automático — roda pelo script existente

---

## ⚠️ Riscos

| Risco | Mitigação |
|---|---|
| AC2 mudar número de etapa que já funciona | AC6 (delta medido) + AC3 (fonte declarada) |
| Planilha de leads sem UTM mapeada → tudo "Sem Track" | Tratar como ausência declarada, não como orgânico |
| Etapa com pesquisa **e** planilha de leads | AC2 define a precedência; AC3 a torna visível |
| Dedupe divergir do resto do app | Reusar `utils/lead-origin.ts`, não reimplementar |
| Mais etapas no laço → mais leituras do Sheets | Elegibilidade cresce por dezenas, não milhares; sync é diário |

---

## 📓 Notas para o @dev

- **Primeiro confirme o diagnóstico no ambiente real:** a Captação Paga tem `funnel_surveys` vinculada? Se tiver, o caminho muda (vira AC2 puro, sem AC1). Se não tiver — hipótese desta story — é elegibilidade mesmo. Um `SELECT` resolve, e a story inteira depende disso.
- A união de fontes em `:355-358` já é um `Map` por `stageId`. Acrescentar a terceira fonte é acrescentar um laço, não redesenhar.
- `computeLeadOriginForStage` já retorna `null` para "sem fonte" e o chamador conta como `stagesSkipped`. O AC5 precisa distinguir *skip por falta de fonte* de *ainda não rodou* — hoje os dois chegam iguais ao endpoint.
- `readSheetData(spreadsheetId, sheetName)` é o mesmo para os dois tipos de planilha.

---

## 📋 Definition of Done

- [ ] **PRIMEIRO:** diagnóstico da Captação Paga confirmado e registrado — tem `funnel_surveys` vinculada? tem planilha `type='leads'`? **Se a etapa já tiver pesquisa vinculada, a premissa do AC1 cai: pare e volte ao @sm** em vez de implementar elegibilidade que não era o problema
- [ ] AC1–AC7 atendidos
- [ ] Teste: etapa só com planilha de leads entra no sync; precedência do AC2; os dois motivos do AC5
- [ ] Delta medido nas etapas que já tinham cache (AC6)
- [ ] `tsc --noEmit` limpo
- [ ] File List preenchida
- [ ] ClickUp atualizado
- [ ] **Pós-merge (@devops):** rodar `backfill-lead-origin.ts` e conferir que `get_stage_leads_summary` da Captação Paga sai de `semDados` — é o pedido literal do chamado, e só vale depois do fix

---

## 📝 Change Log

| Data | Autor | Mudança |
|---|---|---|
| 2026-08-14 | @sm (River) | Criada. Investigação mostrou que "rodar o sync" **não resolve**: a etapa não é elegível — `syncLeadOrigin` só olha Lead Scoring e `funnel_surveys`, e os LEADS POPUP vivem em `funnel_spreadsheets.type='leads'`. Confirmado que essa tabela tem UTM mapeada, o que viabiliza o split |
| 2026-08-14 | @po (Pax) | F1 verificada como resolvida — AC2 sem cruzamento e com os três eixos rastreados ao código. **`Draft → Ready`**, ClickUp → `ready for dev` |
| 2026-08-14 | @sm (River) | AC2 corrigido conforme F1 do @po: a temperatura vem de `utm_term`, não da pesquisa — a planilha de leads passa a ser fonte de **todo** o payload, com a tabela dos três eixos e aviso explícito contra construir cruzamento por e-mail. Diagnóstico da premissa promovido a primeiro item do DoD |
| 2026-08-14 | @po (Pax) | Validação: **GO CONDICIONAL 9/10** — o AC2 afirma que a pesquisa é fonte de temperatura, mas ela vem de `utm_term` (`lead-origin.ts:72`); seguir o AC como escrito faria o @dev construir um cruzamento por e-mail que o código dispensa, estourando o M. Precedência do AC2 e numeração no Epic 36 **aprovadas**. Correção em `docs/qa/validations/36.8-36.9-po-validation.md`. Segue `Draft` |

<!-- clickup:86ak16qqb -->
