# Story 4.1: Projects DB Schema + Migration

---

## Status

**Ready for Review**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [code_review, migration_validation]
priority: P0
estimate: S
depends_on: ["3.1.2"]
epic: "EPIC-4-MULTI-PROJECT-STRUCTURE"
```

---

## Story

**As a** Loyola Digital admin,
**I want** a `projects` table in the database linking all client resources together,
**so that** Instagram accounts and conversations can be scoped to a specific client project.

---

## Acceptance Criteria

1. [ ] Tabela `projects` criada com campos: `id` (UUID PK), `name` (VARCHAR 100 NOT NULL), `client_name` (VARCHAR 100 NOT NULL), `description` (TEXT nullable), `color` (VARCHAR 7 nullable — hex color), `created_by` (FK → users.id), `is_active` (BOOLEAN DEFAULT true), `created_at` (TIMESTAMPTZ), `updated_at` (TIMESTAMPTZ)
2. [ ] Índice `idx_projects_created_by` em `projects(created_by)`
3. [ ] Coluna `project_id` adicionada em `instagram_accounts`: `UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE` — nullable na migration para não quebrar dados existentes
4. [ ] Índice `idx_ig_accounts_project` em `instagram_accounts(project_id)`
5. [ ] Coluna `project_id` adicionada em `conversations`: `UUID NULLABLE REFERENCES projects(id) ON DELETE SET NULL`
6. [ ] Índice `idx_conversations_project` em `conversations(project_id)`
7. [ ] Drizzle schema em `db/schema.ts` atualizado com todas as alterações
8. [ ] Migration gerada via `npx drizzle-kit generate` e aplicável via `npx drizzle-kit push`
9. [ ] Dados existentes em `instagram_accounts` e `conversations` preservados após migration (project_id = null)
10. [ ] `pnpm typecheck` e `pnpm lint` passam sem erros

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **ORM:** Drizzle ORM — schema definido em `packages/api/src/db/schema.ts` [Source: architecture/fullstack-architecture-loyola-digital-x.md#ADR-003]
- **DB:** PostgreSQL 16 (Railway managed) [Source: architecture/fullstack-architecture-loyola-digital-x.md#ADR-007]
- **Schema atual:** `packages/api/src/db/schema.ts` — tabelas existentes: `users`, `conversations`, `messages`, `delegated_tasks`, `instagram_accounts`, `instagram_metrics_cache`
- **UUID PKs:** Todas as tabelas usam `uuid()` com `.defaultRandom()` no Drizzle
- **Timestamps:** `timestamp({ withTimezone: true })` em todas as datas
- **Drizzle imports:** `pgTable`, `uuid`, `text`, `varchar`, `boolean`, `timestamp`, `index`, `uniqueIndex` — já usados no schema existente

### Key Files

| File | Change |
|------|--------|
| `packages/api/src/db/schema.ts` | Adicionar tabela `projects` + colunas `project_id` nas tabelas existentes |
| `packages/api/drizzle/` | Migration files gerados pelo drizzle-kit |

### Schema Drizzle Reference

```typescript
// Nova tabela projects
export const projects = pgTable(
  "projects",
  {
    id: uuid("id").defaultRandom().primaryKey(),
    name: varchar("name", { length: 100 }).notNull(),
    clientName: varchar("client_name", { length: 100 }).notNull(),
    description: text("description"),
    color: varchar("color", { length: 7 }),           // ex: "#d4a843"
    createdBy: uuid("created_by")
      .notNull()
      .references(() => users.id, { onDelete: "restrict" }),
    isActive: boolean("is_active").notNull().default(true),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
    updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    index("idx_projects_created_by").on(table.createdBy),
  ]
);

// Alteração em instagramAccounts: adicionar project_id
// project_id: uuid("project_id").references(() => projects.id, { onDelete: "cascade" })
// index("idx_ig_accounts_project").on(table.projectId)

// Alteração em conversations: adicionar project_id
// projectId: uuid("project_id").references(() => projects.id, { onDelete: "set null" })
// index("idx_conversations_project").on(table.projectId)

// delegatedTasks: sem alteração nesta epic (tasks não são exibidas por projeto)
```

### SQL Reference

```sql
CREATE TABLE projects (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name        VARCHAR(100) NOT NULL,
  client_name VARCHAR(100) NOT NULL,
  description TEXT,
  color       VARCHAR(7),
  created_by  UUID NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  is_active   BOOLEAN NOT NULL DEFAULT true,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_projects_created_by ON projects(created_by);

-- Alterações nas tabelas existentes
ALTER TABLE instagram_accounts ADD COLUMN project_id UUID REFERENCES projects(id) ON DELETE CASCADE;
CREATE INDEX idx_ig_accounts_project ON instagram_accounts(project_id);

ALTER TABLE conversations ADD COLUMN project_id UUID REFERENCES projects(id) ON DELETE SET NULL;
CREATE INDEX idx_conversations_project ON conversations(project_id);

-- delegated_tasks: sem alteração nesta epic
```

### Notas Importantes

- `instagram_accounts.project_id` é **nullable na migration** (dados existentes não têm projeto ainda). A obrigatoriedade será enforçada na camada de API (Story 4.2), não no banco por ora — isso permite que os dados existentes continuem funcionando.
- `conversations.project_id` é **sempre nullable** (conversas globais de admins não precisam de projeto).
- Não há `enum` novo nesta story — o `user_role` enum com `guest` fica para Story 5.1.
- `created_by` em `projects` usa `ON DELETE RESTRICT` propositalmente — não se deve deletar um usuário que tem projetos.

---

## Testing

### Validation Commands

```bash
cd packages/api
pnpm lint
pnpm typecheck
npx drizzle-kit generate
npx drizzle-kit push   # requer DB connection (Railway)
```

### Test Scenarios

1. Migration aplicada sem erros em DB limpo
2. Migration aplicada sem perda de dados em DB com dados existentes (`instagram_accounts`, `conversations`, `delegated_tasks` mantêm todos os registros, `project_id = null`)
3. `projects` aceita insert com todos os campos obrigatórios
4. FK `created_by` → `users.id` enforçada: insert com user inexistente falha
5. FK `instagram_accounts.project_id` → `projects.id` ON DELETE CASCADE: deletar projeto deleta contas vinculadas
6. FK `conversations.project_id` → `projects.id` ON DELETE SET NULL: deletar projeto seta `project_id = null` nas conversas
7. Drizzle schema compila sem erros de tipo (`pnpm typecheck`)
8. `delegated_tasks` não foi alterada — dados existentes preservados sem modificação

---

## Tasks / Subtasks

- [x] **Task 1: Adicionar tabela `projects` ao schema Drizzle** (AC: 1, 2)
  - [x] 1.1 Em `packages/api/src/db/schema.ts`, importar tipos necessários (já disponíveis: `pgTable`, `uuid`, `varchar`, `text`, `boolean`, `timestamp`, `index`)
  - [x] 1.2 Definir e exportar `export const projects = pgTable("projects", { ... })` com todos os campos e índice
  - [x] 1.3 Verificar que `users` está importado/disponível para a FK reference

- [x] **Task 2: Adicionar `project_id` em `instagram_accounts`** (AC: 3, 4)
  - [x] 2.1 Adicionar campo `projectId: uuid("project_id").references(() => projects.id, { onDelete: "cascade" })` na tabela `instagramAccounts`
  - [x] 2.2 Adicionar `index("idx_ig_accounts_project").on(table.projectId)` no array de índices da tabela
  - [x] 2.3 Verificar que a referência circular (projects → users, instagramAccounts → projects) está resolvida na ordem de declaração

- [x] **Task 3: Adicionar `project_id` em `conversations`** (AC: 5, 6)
  - [x] 3.1 Adicionar campo `projectId` (nullable) em `conversations` com FK → `projects.id` ON DELETE SET NULL
  - [x] 3.2 Adicionar índice `idx_conversations_project` em `conversations`

- [x] **Task 4: Gerar e validar migration** (AC: 7, 8, 9)
  - [x] 4.1 Executar `npx drizzle-kit generate` — migration criada em `packages/api/src/db/migrations/`
  - [x] 4.2 Inspecionar o SQL gerado — confirmado: CREATE TABLE projects + ALTER TABLE conversations/instagram_accounts + FKs + índices
  - [x] 4.3 Executar `npx drizzle-kit push --force` com DB Railway — Changes applied
  - [x] 4.4 Confirmar que dados existentes foram preservados (project_id nullable → NULL nas linhas existentes)

- [x] **Task 5: Validações finais** (AC: 10)
  - [x] 5.1 Executar `pnpm lint` — zero erros
  - [x] 5.2 Executar `pnpm typecheck` — zero erros
  - [x] 5.3 Atualizar File List desta story

---

## File List

> Atualizado pelo @dev durante implementacao

| File | Status |
|------|--------|
| `packages/api/src/db/schema.ts` | Modified |
| `packages/api/src/db/migrations/0000_black_purifiers.sql` | Created |
| `packages/api/src/__tests__/chat-tools-instagram.test.ts` | Modified (lint fix pré-existente: remove unused `beforeEach` import) |

---

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log

- `drizzle-kit push` requer TTY para confirmação interativa — resolvido com flag `--force`
- Lint pré-existente em `chat-tools-instagram.test.ts`: `beforeEach` importado mas não usado — corrigido

### Completion Notes

- Tabela `projects` criada com 9 campos + índice `idx_projects_created_by`
- `conversations.project_id` adicionado como nullable FK → `projects.id` ON DELETE SET NULL
- `instagram_accounts.project_id` adicionado como nullable FK → `projects.id` ON DELETE CASCADE
- `delegated_tasks` não alterada (tasks sem aba de projeto nesta epic)
- Migration `0000_black_purifiers.sql` gerada e aplicada no banco Railway com `--force`
- Ordem de declaração no schema: `users` → `projects` → `conversations` → ... → `instagramAccounts` (sem referências circulares)

### Change Log

- `packages/api/src/db/schema.ts`: adicionada tabela `projects` + campos `projectId` em `conversations` e `instagramAccounts`
- `packages/api/src/db/migrations/0000_black_purifiers.sql`: migration gerada pelo drizzle-kit
- `packages/api/src/__tests__/chat-tools-instagram.test.ts`: removido import `beforeEach` não utilizado

