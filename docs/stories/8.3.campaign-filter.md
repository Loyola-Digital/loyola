# Story 8.3: Filtro por Campanha

---

## Status

**Draft**

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [code_review, ui_validation]
priority: P0
estimate: M
depends_on: ["EPIC-7"]
epic: "EPIC-8-CREATIVE-INTELLIGENCE"
wave: 1
moscow: MUST
```

---

## Story

**As a** gestor de trafego da Loyola Digital,
**I want** to filter the entire dashboard by a specific campaign,
**so that** I can analyze a single campaign's performance (adsets, ads, funnel, daily chart) without noise from other campaigns.

---

## Acceptance Criteria

1. [ ] Novo dropdown "Campanha" ao lado do filtro de AdSet existente — lista todas campanhas ativas
2. [ ] Selecionar campanha filtra: SummaryCards (totais apenas daquela campanha), tabela (mostra apenas aquela campanha expandida com adsets/ads)
3. [ ] DailyChart filtrado: quando campanha selecionada, grafico mostra spend/clicks apenas daquela campanha
4. [ ] Nova rota `GET /api/traffic/analytics/:projectId/campaign-daily?campaignId=X&days=30` — daily insights filtrados por campanha
5. [ ] Hook `useCampaignDailyInsights(projectId, campaignId, days)` — habilitado apenas quando campanha selecionada
6. [ ] FunnelChart recalculado para campanha selecionada (nao totais gerais)
7. [ ] TopPerformers filtrado: quando campanha selecionada, mostra top ads APENAS daquela campanha
8. [ ] Filtro de AdSet continua funcionando — pode combinar: campanha X + adset Y
9. [ ] Reset: botao "Limpar filtros" ou selecionar "Todas as campanhas"
10. [ ] URL state: filtros persistidos na URL via query params (`?campaign=X&adset=Y`) para compartilhamento
11. [ ] `pnpm typecheck` e `pnpm lint` passam

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context

- **Dashboard:** `packages/web/app/(app)/traffic/page.tsx` — ja tem filtro de AdSet (`AdSetFilterDropdown`) e filtro de periodo
- **Campaign data:** `useTrafficCampaigns()` ja retorna lista de campanhas — reusar para popular o dropdown
- **DailyChart:** Atualmente usa `useMetaAdsDailyInsights(accountId, days)` — nivel conta, nao campanha
- **Meta API:** `fetchCampaignInsights()` ja filtra por campanha, `fetchDailyInsights()` precisa de novo endpoint com filtro

### Filter Cascade Logic

```
┌─────────────────────────────────────────────┐
│ Conta Meta Ads ▼ │ Projeto ▼ │ Período ▼    │
│ [Campanha ▼]     │ [AdSet ▼]  │ [Limpar]    │
└─────────────────────────────────────────────┘

Campanha selecionada → filtra:
  ├── SummaryCards: recalcula totais para 1 campanha
  ├── FunnelChart: recalcula funil para 1 campanha
  ├── DailyChart: mostra daily da campanha (nova rota)
  ├── TopPerformers: top ads daquela campanha
  ├── CampaignTable: mostra apenas 1 campanha, auto-expandida
  └── AdSet dropdown: lista apenas adsets daquela campanha
```

### Campaign Daily Insights (Nova Rota)

```typescript
// No meta-ads.ts — nova funcao
export async function fetchCampaignDailyInsights(
  metaAccountId: string,
  accessToken: string,
  campaignId: string,
  days: number
): Promise<MetaDailyInsight[]> {
  const filtering = encodeURIComponent(
    JSON.stringify([{ field: "campaign_id", operator: "EQUAL", value: campaignId }])
  );
  // /act_{id}/insights?time_increment=1&level=campaign&filtering=[{campaign_id}]
}
```

### URL State

```typescript
// useSearchParams para persistir filtros
const searchParams = useSearchParams();
const campaignFilter = searchParams.get("campaign");
const adsetFilter = searchParams.get("adset");

// Atualizar URL sem reload
router.replace(`/traffic?campaign=${id}&adset=${adsetId}`, { scroll: false });
```

### Key Files

| File | Change |
|------|--------|
| `packages/api/src/services/meta-ads.ts` | +`fetchCampaignDailyInsights()` |
| `packages/api/src/routes/traffic-analytics.ts` | +rota `GET .../campaign-daily` |
| `packages/web/lib/hooks/use-traffic-analytics.ts` | +`useCampaignDailyInsights()` hook |
| `packages/web/app/(app)/traffic/page.tsx` | +CampaignFilterDropdown, filter cascade logic, URL state |

---

## Tasks

- [ ] **Task 1 (AC: 4):** `meta-ads.ts` — `fetchCampaignDailyInsights()` + rota API
- [ ] **Task 2 (AC: 5):** Hook `useCampaignDailyInsights(projectId, campaignId, days)`
- [ ] **Task 3 (AC: 1, 9):** UI — CampaignFilterDropdown com opcao "Todas as campanhas" + botao Limpar
- [ ] **Task 4 (AC: 2, 6):** Filter cascade — SummaryCards e FunnelChart recalculados por campanha
- [ ] **Task 5 (AC: 3):** DailyChart — usar daily insights filtrados por campanha quando selecionada
- [ ] **Task 6 (AC: 7):** TopPerformers — filtrar por campaignId quando campanha selecionada
- [ ] **Task 7 (AC: 8):** Combinacao de filtros — campanha + adset funcionando junto
- [ ] **Task 8 (AC: 10):** URL state — query params persistidos
- [ ] **Task 9 (AC: 11):** `pnpm typecheck` e `pnpm lint` passando

---

## Testing

- Dropdown lista todas campanhas ativas do projeto
- Selecionar campanha filtra SummaryCards, FunnelChart, DailyChart, TopPerformers e tabela
- DailyChart mostra dados especificos da campanha selecionada
- FunnelChart recalcula funil para campanha unica
- TopPerformers mostra apenas ads da campanha selecionada
- Filtro campanha + adset combinam corretamente
- "Limpar filtros" reseta para visao geral
- URL persiste filtros — reload mantem estado
- typecheck e lint passam

---

## Dev Agent Record

### File List

| File | Status |
|------|--------|
| — | — |

### Debug Log

_(empty)_

### Change Log

- 2026-03-20: Story 8.3 criada por @sm (River) a partir do EPIC-8
