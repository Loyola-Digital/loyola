# Story 18.51c: Captação Paga — Cards de Resumo, Ingressos (Leads Popup) e Venda Ingressos únicos/totais

**Epic:** 18 — Dashboard Improvements
**Status:** Done
**Created:** 2026-07-13 (@po — Pax, split de [18.51](18.51.story.md))
**Priority:** Medium
**Parent:** [18.51](18.51.story.md)
**Depends On:** **18.51a** (consome os campos do backend). Independente de 18.51b (pode ser paralela).

<!-- clickup:task_id -->

---

## 📖 Escopo desta fatia

Os **cards** do dashboard da etapa Paga (`launch-dashboard.tsx`): card resumo de faturamento (único/total), cards de ingressos únicos/totais ao lado de "Leads Popup", e o card "Venda ingressos" mostrando vendas únicas/totais.

Ler a **story-pai [18.51](18.51.story.md)** para glossário. Lembrar: **Vendas únicas ≡ Ingressos únicos**, **Vendas totais ≡ Ingressos totais** (mesmos valores, reuso).

---

## ✅ Acceptance Criteria

**AC1a.1** — Card resumo com 2 linhas: (1, destaque) **Faturamento do produto da captação (único)**; (2, abaixo) **Faturamento total dos produtos** (todos, sem dedup).

**AC1a.2** — Card só renderiza com planilha de vendas conectada e com dados (sem zeros enganosos).

**AC1f.1** — À direita do card **"Leads Popup"** (`launch-dashboard.tsx:365-381`), card(s) com **Ingressos únicos** e, logo abaixo, **Ingressos totais**.

**AC1f.2** — Ambos com tooltip: únicos = definição dedup; totais = por produto (mesmo detalhamento de 18.51b AC1b.3).

**AC1f.3** — Cards só na etapa Paga com planilha conectada.

**AC1g.1** — Card **"Venda ingressos"** (`launch-dashboard.tsx:382-389`) passa a mostrar **Vendas únicas** (valor principal) e **Vendas totais** (subValue/2ª linha).

**AC1g.2** — Reusar **exatamente** os valores de ingressos únicos/totais (AC1f) — não recalcular, para não divergir da tabela (18.51b).

**AC1g.3** — Tooltip explica ambos. Preservar ou reorganizar de forma legível o `subValue` de "visitas checkout" existente (alinhar com @ux).

---

## 🧪 Definition of Done

- [x] Card resumo faturamento único/total renderiza só com planilha conectada (`showFaturamento`).
- [x] Cards Ingressos únicos/totais à direita de Leads Popup, com tooltips (`showIngressos`).
- [x] Card "Venda ingressos" mostra únicas + totais, reusando os mesmos valores.
- [x] Coerência garantida por construção: cards somam os mesmos `*ByDay` que o footer da tabela 18.51b (`sumAllOrigem`/`sumAllNum`).
- [x] `tsc --noEmit` verde (0 erros) + `next build` verde (27/27 páginas) ([[feedback_verify_build_before_merge]]).

---

## 🛠️ Dev Notes

`launch-dashboard.tsx`: card "Leads Popup" 365-381 (label 368), card "Venda ingressos" 382-389 (usa `metrics.totalVendas`). `salesData` buscado só p/ paid (126-143). Card resumo 1a: padrão `StatCard`/card com tooltip — referência `manual-pix-sales-section.tsx:458-491`. Expor novos campos via `use-stage-sales-data.ts`. Ver pai para detalhes.

---

## Dev Agent Record

### File List
- `packages/web/components/funnels/launch-dashboard.tsx` (modificado)

### Notas de implementação
- **AC1a:** card "Faturamento" agora mostra **Faturamento único** (produto captação, dedup e-mail) como valor de destaque e **"Total: R$ X"** (todos os produtos, sem dedup) na 1ª linha do `subValue`, preservando o breakdown por origem (Pago/Org/Sem Track). Tooltip nativo (`title`) explica único vs total.
- **AC1f:** novo card **"Ingressos"** (ícone `UserCheck`) inserido imediatamente à direita de "Leads Popup" — valor = ingressos únicos, `subValue` = "Totais: X" com tooltip por produto. Só renderiza na etapa Paga com planilha conectada (`showIngressos = showFaturamento`).
- **AC1g:** card **"Venda ingressos"** passa a mostrar **vendas únicas** (= ingressos únicos) como valor e **"Totais: X"** + "N visitas checkout" no `subValue`.
- **Coerência (AC1g.2 + DoD):** todos os valores dos cards vêm da **soma dos `*ByDay`** (`ingressosUnicosByDay`, `ingressosTotaisByDay`, `faturamentoUnicoByDay`, `faturamentoTotalByDay`) — as mesmas bases do footer da tabela 18.51b (`sumAllOrigem`/`sumAllNum`), garantindo que card == tabela == gráfico mesmo com venda sem `dataVenda`.
- Tooltip "por produto" reusa o formato de `ingressosPorProduto` da 18.51b.

## QA Results

**Gate:** CONCERNS (aprovado com observações) — @qa (Quinn), 2026-07-13
**Status:** InReview → Done

### 7 Checks
1. **Code review:** OK — helpers `sumOrigem`/`sumAllOrigem`/`sumAllNum` espelham exatamente o footer da tabela 18.51b; comentários explicam o porquê da soma dos `*ByDay`.
2. **Testes:** N/A (frontend puro, sem lógica testável isolada nova); validado via build.
3. **AC compliance:** AC1a/1f/1g atendidos.
4. **No regressions:** card "Venda ingressos" mantém fallback (`metrics.totalVendas`) quando `!showIngressos`; demais cards intactos; `colCount`/`gridClass` OK (máx 12).
5. **Performance:** somas O(dias) triviais no render; sem impacto.
6. **Security:** N/A (sem input/query nova).
7. **Docs:** story Dev Agent Record + tooltips explicativos nos cards.

### Issues
- **[low/code] Divergência visual no card Faturamento:** `"Total"` usa soma dos `faturamentoTotalByDay`, breakdown por origem usa `faturamentoBruto` (escalar). Diverge só quando há venda sem `dataVenda`. Decisão consciente: priorizar coerência card↔tabela (AC1g.2). Mitigado por tooltip. **Recommendation:** monitorar; se incomodar, alinhar as duas bases numa story futura.
- **[info/ux] Redundância Ingressos (AC1f) × Venda ingressos (AC1g):** mesmos números por design (requisito Danilo). **Recommendation:** validação visual do Danilo.

### Gates
- `tsc --noEmit`: **verde** (0 erros).
- `next build`: **verde** (compilou + type-check + 27/27 páginas). ETIMEDOUT do 1º run foi I/O transitório (2º run limpo).

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-07-13 | @po (Pax) | Criada Ready (split de 18.51). |
| 2026-07-13 | @dev (Dex) | Ready → InProgress. Implementados AC1a/1f/1g em launch-dashboard.tsx (cards reusam soma dos *ByDay p/ coerência com tabela 18.51b). |
| 2026-07-13 | @dev (Dex) | InProgress → InReview. DoD completa; tsc verde + next build verde (27/27). |
| 2026-07-13 | @qa (Quinn) | InReview → Done. Gate **CONCERNS**: 1 low (divergência visual card Faturamento em venda sem dataVenda) + 1 info UX (redundância Ingressos×Venda ingressos). Coerência card↔tabela confirmada. |
