# Story 44.12: O produtor de `CoberturaDiaria` — ligar a guarda de rastreio

**Epic:** 44 — Aba "Inácio"
**Status:** Merged (`578f60bf`, PR #562) — **aguardando deploy da API.** A guarda só liga em produção depois do deploy + backfill; hoje ela está desligada nas 15 etapas.
**Created:** 2026-08-19 (@sm — River)
**Priority:** 🟡 Média — nada quebra sem ela, mas **toda etapa gratuita exibe hoje o aviso de guarda indisponível**
**Complexity:** M (8 pontos) — o laço já lê tudo, mas a Decisão 1 do @po exige `projectId` (que `computeLeadOriginForStage` não tem) e duas passadas sobre as linhas
**Depends On:** 44.6 (`coberturaAtipica`, `Done`) · 44.7 (`calcularTetos`, `Done`) · 44.9 (o serviço que vai consumir, `Done`)
**Origem:** Story 44.8 AC5 (decisão do @po de 2026-08-19) · spec §4

> **Fonte única:** `docs/stories/epics/epic-44-aba-inacio.md` **v1.1**.

---

## 💼 Por que esta story existe

`calcularTetos` aceita `opts.coberturaDaEtapa`, e **nada no repo produz esse dado**. A guarda de rastreio (`coberturaAtipica`) nunca roda.

Consequência hoje, visível em produção: toda etapa da família gratuita mostra

> *"A guarda de cobertura de rastreio NÃO rodou: não existe produtor de cobertura de lead por dia. O teto de convLP desta família não está protegido contra janela escolhida por rastreio melhor, e não por conversão melhor."*

**O que a guarda impede:** a "melhor janela" de `Conv. LP` pode ser a semana em que o **rastreio** funcionou melhor, não a de melhor conversão. Medido na 44.6: uma etapa que passou de 52% para 95% de cobertura exibiria um salto de **1,8×** sem nada mudar na página. O selo de confiança não pega isso — ele conta volume, não qualidade de atribuição.

---

## 🔄 Correção de uma estimativa anterior

⚠️ Na validação da 44.8 o **@po escreveu que esta story seria cara**, porque `LeadParaAtribuir` (`campaign-attribution.ts:39`) não tem data e `LeadOriginPayload` não tem quebra diária.

**Isso estava certo sobre o arquivo errado.** O lugar certo não é `campaign-attribution.ts` — é `lead-origin-sync.ts`, e lá os três campos necessários **já estão no mesmo laço, na mesma linha**:

```
lead-origin-sync.ts:374-403   for (const row of sheet.rows) {
  :379-381   const key = email || phone   ← a chave de dedup
  :394       cell(row, idx.utmContent)    ← o que decide "atribuído"
  :399       toIsoDate(cell(row, idx.date)) ← a data, já normalizada
```

O que falta é **um `Map<data, {atribuídos, totais}>`** dentro de um laço que já lê tudo. São ~15 linhas.

**Então o custo desta story não é código. São as três decisões abaixo** — e é por isso que ela existe como story, em vez de virar um commit.

> **✅ @po (2026-08-19): a correção do @sm procede, e é minha.** Eu apontei `campaign-attribution.ts` quando o caminho barato era `lead-origin-sync.ts`. Conferi as três linhas: `:379`, `:394` e `:399`, mesma iteração. Registrar isso é mais útil que defender a estimativa.
>
> **Uma parte do que eu disse continua valendo:** a dedup por dia é decisão de design, não extensão mecânica (virou a Decisão 2). E a Decisão 1, medida abaixo, é mais cara do que este draft estima — o que reforça que a leitura "não é embrulho mecânico" estava certa pelo motivo errado.

---

## 🗺️ O terreno (levantado em 2026-08-19)

| o que | onde | estado |
|---|---|---|
| O contrato a satisfazer | `shared/cadeia-cac.ts` — `CoberturaDiaria { date, leadsAtribuidos, leadsTotais }` | ⚠️ **contagens, não frações** |
| Quem consome | `coberturaDaJanela` (`:524-536`) — **razão de somas** | dividir por dia e tirar média daria outro número |
| Onde a guarda roda | `calcularTetos` (`:585`) — só `familia === "gratuita" && metrica === "convLP"` | pronto |
| Onde produzir | `computeLeadOriginForStage` (`lead-origin-sync.ts:288`) | data + chave + `utmContent` no mesmo laço |
| Dedup atual | `globalKeys` — **um `Set` para o período inteiro** (`:300`, `:384`) | ver Decisão 2 |
| Onde guardar | `publicMetricsCache`, scope `lead-origin`, key = stageId | ⚠️ caches existentes **não têm** o campo novo |
| Job e backfill | `syncLeadOrigin` (`admin.ts:54`) · `scripts/backfill-lead-origin.ts` | prontos |
| Quem vai ligar | `services/cadeia-cac-payload.ts:280` — `calcularTetos(series, familia)` **sem opts** | ver AC4 |
| O aviso a desligar | mesmo arquivo, `:281` | ver AC5 |

---

## ✅ Critérios de aceite

### AC1 — 🛑 Decisão 1: o que conta como "atribuído"?

O comentário do `shared` diz: *"quantos carregavam um `utm_content` que resolve para algum anúncio"*. Isso admite duas leituras, e elas dão números diferentes:

**✅ DECISÃO @po (2026-08-19): (b) — `utm_content` que RESOLVE para um anúncio conhecido.**

| opção | regra | efeito |
|---|---|---|
| (a) `utm_content` preenchido | a célula não está vazia | cobertura mais alta; conta id que não resolve |
| **(b) `utm_content` que RESOLVE** ✅ **decidida** | o id aparece em `meta_ad_insights_daily` do projeto | cobertura mais baixa e comparável |

**O argumento decisivo não é o do @sm — é mais forte que ele.**

Cobertura é uma **razão**, e razão só significa algo se numerador e denominador vierem da mesma população. Foi assim que eu decidi a 44.13, e vale aqui igual.

O numerador da métrica que a guarda protege é `leadsAtribuidos` do `DiaBruto` — leads atribuídos **à campanha**, e essa atribuição usa `mapearAdsParaCampanhas`, que conta **só id que resolve**. Se a cobertura contasse id não resolvido, a guarda compararia uma população contra outra: mediria "quantos leads têm UTM" enquanto o `convLP` mede "quantos leads resolveram". A guarda barraria (ou deixaria passar) janelas pelo motivo errado.

#### ⚠️ E o custo é maior do que este draft estimou

O draft diz "1 query + o mapa". Conferi:

- **`mapearAdsParaCampanhas(db, projectId, adIds)` exige `projectId`** — e `computeLeadOriginForStage(db, stageId)` **só tem `stageId`** (`lead-origin-sync.ts:288-291`). Precisa resolver o projeto (uma query pelo join etapa→funil) ou mudar a assinatura e o chamador (`:528`).
- **O laço passa a ter duas passadas.** Para saber quais ids resolvem é preciso ter a lista ANTES de agrupar por dia. Primeira passada acumula `(data, chave, utmContent)`; a query resolve o mapa; a segunda agrupa. Não cabe em 15 linhas.
- **A lista do `inArray` pode ser grande** — uma etapa com milhares de `utm_content` distintos gera um `IN` grande. Deduplicar os ids antes da query, não passar um por linha.

Por isso a complexidade sobe de **5 para 8 pontos**. A decisão continua sendo (b) — o custo é real, mas servir uma razão de populações diferentes seria pior que não servir nada.

### AC2 — 🛑 Decisão 2: a dedup por dia contradiz a dedup global, e isso é esperado

`globalKeys` é **um `Set` para o período inteiro** (`:300`). Por dia, é preciso `Map<data, Set<chave>>`. Consequência aritmética inevitável:

```
lead que se cadastrou em 03/08 e de novo em 11/08
  → uniqueLeads (global) ......... conta 1
  → Σ leadsTotais (por dia) ...... conta 2
```

**✅ DECISÃO @po (2026-08-19): confirmado — `Σ leadsTotais ≠ uniqueLeads` é o comportamento correto, e o teste que trava isso é obrigatório.**

A cobertura de uma janela pergunta *"neste período, quanto do rastreio funcionou?"* — e o lead que voltou é uma oportunidade de rastreio a mais. Deduplicar entre dias responderia outra pergunta.

⚠️ **O teste não é opcional nem "nice to have".** Sem ele isto é uma bomba de relógio: parece exatamente o defeito que o QA-44-01 corrigiu (soma de conjuntos sobrepostos passando de 100%), e a pessoa que "consertar" vai desligar a guarda sem saber. O comentário no código tem que estar **no ponto onde o `Map` é criado**, não no topo do arquivo.

⚠️ **Mas alguém vai achar que é bug.** Já aconteceu neste epic: o QA-44-01 corrigiu justamente uma soma que passava de 100% por somar conjuntos sobrepostos.

**Entregar:** a divergência **documentada no código, no ponto onde ela nasce**, e um teste que a trava — com um lead em dois dias, afirmando que o global conta 1 e a série conta 2. Sem esse teste, a próxima pessoa "conserta" e quebra a guarda.

⚠️ E há um segundo efeito, na direção oposta: **linha sem data legível fica fora da série diária** (o `if (d)` de `:400`), mas continua no `uniqueLeads`. É o mesmo padrão do `vendasSemDataNoTotal` da 44.8 — **declarar, nunca corrigir em silêncio**.

### AC3 — Produzir `byDay` no payload de lead

Estender `LeadOriginPayload` com:

```ts
/** Story 44.12: cobertura de rastreio por dia. Contagens, não frações. */
coberturaDiaria: { date: string; leadsAtribuidos: number; leadsTotais: number }[];
/** Linhas sem data legível — ficam fora da série (AC2). Nunca corrigido em silêncio. */
leadsSemData: number;
```

⚠️ **Contagens, não frações.** `coberturaDaJanela` soma numerador e denominador da janela e divide **depois**. Entregar a fração pronta por dia daria outro número — é o mesmo erro que a regra da §2.6 proíbe.

### AC4 — Ligar no `calcularTetos`

`services/cadeia-cac-payload.ts:280` chama `calcularTetos(series, familia)` **sem opts**.

⚠️ **Há uma reordenação envolvida:** o cache de lead é lido hoje **depois** dos tetos (`:372`), dentro do ramo da família gratuita. A cobertura precisa estar em mãos **antes** de `calcularTetos`. Ler uma vez e reusar nos dois pontos — não fazer duas queries.

### AC5 — O aviso muda de estado, e os estados são três

`guardaDeCobertura` hoje é binário (`indisponivel` | `naoSeAplica`). Passa a distinguir:

| estado | quando | o que a aba diz |
|---|---|---|
| `aplicada` | a guarda rodou | quantas janelas foram barradas, se alguma |
| `semLeadNoPeriodo` | há série de cobertura, mas nenhuma janela tem lead | a guarda não pôde julgar — **não é o mesmo que não ter rodado** |
| `indisponivel` | a etapa não tem cache com `coberturaDiaria` | ⚠️ **continua existindo** — ver AC6 |
| `naoSeAplica` | família paga | como hoje |

⚠️ O `motivo: "coberturaAtipica"` de `TetoAusente` (`cadeia-cac.ts:645`) passa a ser alcançável pela primeira vez: significa *"toda janela elegível foi barrada pela guarda"*. A aba precisa de texto para ele — hoje há um genérico.

### AC6 — ⚠️ O cache existente não tem o campo, e a guarda fica desligada até o sync rodar

`publicMetricsCache` guarda o `LeadOriginPayload` serializado. Os registros já gravados **não têm `coberturaDiaria`**.

Ou seja: **mergear e deployar não liga a guarda.** Ela liga quando o sync reprocessar cada etapa — diariamente, ou via `scripts/backfill-lead-origin.ts`.

**✅ DECISÃO @po (2026-08-19): o backfill está DENTRO do escopo.**

E a razão está no próprio DoD desta story: *"validação visual: uma etapa gratuita **sem** o aviso de indisponível"*. Isso é **impossível** sem o backfill — o código pode estar perfeito em produção e toda etapa já cacheada continuaria mostrando o aviso.

Sem backfill, esta story entrega zero comportamento observável. Com ele, entrega o que se propôs.

**Entregar:** ausência do campo em cache velho cai em `indisponivel` **sem quebrar** (não `undefined.length`), e o backfill roda como parte da story, com o resultado registrado (quantas etapas passaram a ter cobertura).

### AC7 — Testes

- lead em dois dias → global 1, série 2 (a AC2)
- linha sem data → fora da série, contada em `leadsSemData`
- `utm_content` que não resolve → **conta ou não conta, conforme a Decisão 1**, com teste que trava a escolha
- `coberturaDaJanela` sobre a série produzida bate com a razão de somas calculada à mão
- cache velho sem o campo → `indisponivel`, sem exceção
- uma janela com cobertura 20 p.p. abaixo da mediana da etapa → barrada (o caminho ponta a ponta)

⚠️ **Cada teste verificado por reversão.** Padrão do epic — 25/25 na 44.9.

---

## 🚫 Fora de escopo

- Cobertura de **venda** por campanha → **44.13** (que precisa resolver dedup **e** filtro de status)
- Bloco de criativos → **44.10** · Migração do `traffic-analytics` → **44.11**
- Tipar o payload da cadeia no `shared` → QA-449-03, backlog

---

## ✅ Decisões do @po (2026-08-19 — nada aqui está aberto)

| # | questão | decisão | onde |
|---|---|---|---|
| 1 | "atribuído" = UTM preenchido ou UTM que resolve? | **Resolve.** Não pelo motivo do @sm — porque cobertura é razão, e o numerador do `convLP` conta só id resolvido. Populações diferentes tornariam a guarda incomparável | AC1 |
| 2 | `Σ leadsTotais ≠ uniqueLeads` é correto? | **Sim**, e o teste que trava isso é **obrigatório**, com o comentário no ponto onde o `Map` nasce | AC2 |
| 3 | backfill dentro ou fora? | **Dentro.** Sem ele o DoD desta story é insatisfazível — a validação visual pede uma gratuita sem o aviso | AC6 |

**Sobre a correção que o @sm me fez:** ela procede e eu registrei na seção acima. Aponte-se o erro onde ele está — eu indiquei `campaign-attribution.ts` quando o caminho barato era `lead-origin-sync.ts`.

**Mas o levantamento subestimou a Decisão 1**, e por isso a complexidade sobe de 5 para 8: `mapearAdsParaCampanhas` exige `projectId`, que `computeLeadOriginForStage` não tem, e resolver id antes de agrupar por dia obriga a **duas passadas** sobre as linhas.

---

## ⚠️ Riscos

| risco | por quê |
|---|---|
| **Entregar fração por dia** | `coberturaDaJanela` é razão de somas; média de frações diárias dá outro número (§2.6) |
| **"Consertar" a soma dos dias** | `Σ leadsTotais > uniqueLeads` é correto e parece bug — foi o QA-44-01 ao contrário |
| **Achar que o deploy liga a guarda** | não liga; só o sync/backfill reprocessando é que popula o campo |
| **Duas queries do cache de lead** | a AC4 exige reordenação, e é fácil resolver duplicando a leitura |
| **Escolher a Decisão 1 sozinho** | muda o valor da cobertura, e portanto quais janelas são barradas |

---

## 📓 Notas para o @dev

- **Você não vai escrever fórmula nenhuma.** `coberturaAtipica`, `coberturaDaJanela` e `mediana` já existem e estão testados. Esta story **produz a entrada**.
- O `toIsoDate` de `:264` já resolve `DD/MM/YYYY` → ISO. Não reimplementar.
- **Baseline da suíte da API: 11 falhas ambientais.** O CI passa.
- **Arquivos `" 2"`**: limpar antes de investigar `TS2300` — não são do seu código.

---

## 📋 Definition of Done

- [x] `utm_content` só conta como atribuído se **resolver** (Decisão 1), com teste travando
- [x] `projectId` resolvido para o `mapearAdsParaCampanhas`, e os ids deduplicados antes do `inArray`
- [x] `coberturaDiaria` e `leadsSemData` em `LeadOriginPayload`, em **contagens**
- [x] Divergência `Σ dias ≠ global` documentada no código e travada em teste
- [x] `calcularTetos` recebendo `coberturaDaEtapa`, com **uma** leitura de cache
- [x] `guardaDeCobertura` com os quatro estados, e texto para `coberturaAtipica`
- [x] Cache velho sem o campo → `indisponivel`, sem exceção
- [x] **Backfill executado** — de 1 para **13 de 15** etapas com cobertura; as **7 gratuitas** com a guarda `aplicada`
- [x] Testes verificados por reversão (Sessão 1: 2 válidas · Sessão 2: 3 de 4)
- [x] `turbo typecheck lint build` 12 tasks limpos · API **1264/11** (baseline de 11 preservado) · Web 480/480
- [x] Validação visual (usuário, 2026-08-19, local sobre banco de produção):
  - `fz-l2-jun-26 / Grat`, `pps1 / Aquisição` e `dg-pg02 / Downsell` **sem** o aviso de indisponível
  - contraprova `bbe-pr1-mar-26 / Grat`: a mensagem nova aparece, citando os **213 leads** com data ilegível

⚠️ **Armadilha operacional encontrada na validação:** rodar `turbo build` com o
`next dev` no ar sobrescreve `packages/web/.next` e o dev passa a servir chunk
inconsistente — a caixa da guarda sumiu da tela por três rodadas seguidas de
conferência, sem nada errado no código. `rm -rf packages/web/.next` + restart do
dev resolveu. Não confundir com defeito de render.

---

---

## 🤖 Dev Agent Record — ⏸️ PARADA EM ANDAMENTO (2026-08-19)

**Branch:** `feature/44.12-cobertura-diaria` · **4 commits, NÃO pushados, sem PR**

```
6b9f9937  feat(api): coluna mapeável por ÍNDICE, para cabeçalho vazio
7771bf02  feat(api): produtor de CoberturaDiaria — a guarda de rastreio liga
d8023bed  docs(story): validada — GO 9/10
44bd12e6  docs(story): draft
```

### O que está PRONTO

| AC | estado |
|---|---|
| AC1 · "atribuído" = resolve | ✅ `comoAdId` exportada; query de `projectId` **preguiçosa** (etapa sem `utm_content` não paga query) |
| AC2 · `Σ dias ≠ global` | ✅ documentado no ponto onde o `Map` nasce, travado em teste |
| AC3 · `coberturaDiaria` + `leadsSemData` | ✅ contagens, não frações |
| AC4 · ligar no `calcularTetos` | ✅ cache lido **antes**, reusado no `cplReal`; teste conta as queries |
| AC5 · 4 estados da guarda | ✅ `aplicada` · `semLeadNoPeriodo` · `indisponivel` · `naoSeAplica`, no back e na aba |
| AC6 · cache velho | ✅ cai em `indisponivel` sem quebrar · backfill do **BBE executado** |
| AC7 · testes | ✅ 15 no produtor + 6 na guarda; **5 reversões** (R29 e R31 válidas; a R30 foi mal construída e **não conta**) |

**Validações:** `turbo typecheck lint build` 12 tasks limpos · API **1260/11** (baseline) · Web **480/480**.

### ⛔ O que FALTAVA (superado na Sessão 2 — ver acima)

**Configurar `columnMapping` das planilhas com cabeçalho vazio.** O código suporta `{"date": "2"}`, mas ninguém apontou a coluna ainda.

Medido no backfill do BBE: **9 das 15 etapas em cache não têm data resolvida**. Enquanto o mapeamento não existir, essas etapas seguem em `indisponivel` — que é o comportamento correto e declarado (decisão (c) do usuário), mas a guarda não liga nelas.

⚠️ **É escrita em `funnel_surveys.columnMapping` no banco de PRODUÇÃO.** Duas ressalvas registradas antes da parada:

1. O índice `2` vale para o Tally do BBE. **Cada uma das 9 precisa ser conferida** — não dá para assumir o mesmo índice.
2. Se existir tela de configuração de pesquisa no painel, fazer por lá é melhor que escrever direto no banco.

**Próximo passo proposto e não executado:** levantar as 9 etapas e o índice que cada uma precisaria. É **só leitura**, e foi onde a sessão parou.

### ✅ Sessão 2 (2026-08-19) — o levantamento das 9, e o que ele revelou

**O plano combinado não teria funcionado.** Escrever `{"date": "<índice>"}` em
`funnel_surveys.column_mapping` não produziria efeito nenhum: `resolveLeadSource`
devolvia `columnMapping: null` nos ramos `pesquisa` (`:186`) e `lead_scoring`
(`:175`) — **o sync nunca leu o mapeamento da pesquisa** — e o campo lá se chama
`timestamp`, enquanto `resolveColIdx` procura `date`. As 9 etapas são todas
`pesquisa` ou `lead_scoring`.

E o inverso do problema: **7 das 9 já tinham a coluna certa apontada no painel.**
O dado existia no banco desde sempre; faltava lê-lo.

#### O levantamento (15 etapas em cache, medido em produção)

| grupo | etapas | causa | coluna |
|---|---|---|---|
| **A** só faltava backfill | dg-pg02/Cap.Paga · dg-pg04/Vendas · fz-l2-jun/Cap.Paga · fz-l3-jul/Captação | cache velho; alias já achava a data | idx 26, 8, 2, 2 |
| **B** alias achava coluna **errada** | fz-m2-jul26/Grat · fz-m1-mai26/Grat | `Data Conversão` com **0/343** e 237/268 células | real: idx 2 `Submitted at`, 343/343 e 268/268 |
| **C** data nomeada fora dos aliases | bbe-web-mai/Grat · dg-pg02/Downsell · dg-pg04/Cap.Paga · dg-pg04/Comercial · fz-l2-jun/Grat · pps1/Aquisição | `Submitted at` não está em `ALIASES.date` | idx 2, 100% das linhas |
| **D** cabeçalho vazio | bbe-pr1/Grat | o caso do enunciado | idx 2 (214/214) ou idx 23 `Conversão Lead` (209/214) |
| — | bbe-pr1/Cap.Paga | 2 linhas, **nenhuma** coluna com data | nada a mapear |

⚠️ O grupo B é o mais grave e não estava previsto: ali o alias não falhava em
achar — ele **achava a coluna errada em silêncio**. Rodar o backfill sem
corrigir isso teria produzido `leadsSemData=343` numa etapa cuja data está 100%
preenchida.

⚠️ E um achado que muda o DoD: as 4 etapas do grupo A são todas `paid`/`sales`.
A guarda só roda em família **gratuita**. Sem a correção abaixo, o backfill não
ligaria a guarda em **nenhuma** etapa gratuita — a validação visual exigida
seria insatisfazível.

#### A correção (commit `2ef170a0`)

`resolveLeadSource` passa a propagar `{ date: survey.column_mapping.timestamp }`
nos dois ramos de pesquisa. **Só a data.** `email`, `phone` e `utm_*` seguem no
alias de propósito: promovê-los trocaria o identificador de dedup de caches já
publicados — a mesma fronteira que a precedência desta função defende desde a
36.9.

4 testes novos; **3 verificados por reversão** (o quarto trava o comportamento
anterior e passa revertido de propósito — está dito no teste).

#### Backfill executado — resultado

**De 1 para 11 etapas com cobertura diária, de 15.** Cinco delas gratuitas
(`dg-pg02/Downsell` 14 dias · `fz-l2-jun/Grat` 17 · `fz-m1-mai26` 18 ·
`fz-m2-jul26` 8 · `pps1/Aquisição` 26), o que torna a validação visual possível.

Permanecem em `indisponivel`, corretamente:

| etapa | por quê | ação |
|---|---|---|
| bbe-pr1-mar-26 / Captação gratuita | header vazio; o painel filtra colunas sem nome (`survey-mapping-dialog.tsx:71`) | **decisão do usuário: mapear `Conversão Lead` pelo painel** (209/214) — evita escrita direta no banco |
| bbe-web-mai-26 / Captação Gratuita | survey com mapping vazio | mapear `Submitted at` pelo painel (248/248) |
| bbe-pr1-mar-26 / Captação paga | 2 linhas, nenhuma data | nada a fazer |
| fz-l2-jun-26 / Captação Paga | ver abaixo | fora de escopo |

#### ⚠️ Achado HERDADO, não causado por esta story

`fz-l2-jun-26 / Captação Paga` saiu com `dias=0` **e** `leadsSemData=0` sobre 128
leads. A data lê perfeitamente — o que falta é **identificador**: as colunas
`email` e `telefone` que o alias resolve estão **0/128 preenchidas**, enquanto os
dados reais moram em `Digite o e-mail que você usou para se cadastrar:` e
`Agora seu WhatsApp:` (93/128 cada). O `uniqueLeads` dessa etapa já era **0**
antes desta story — é exatamente o que `identifiersFilled` foi criado para
explicar, na 36.9.

Linha sem identificador não entra na cobertura de propósito (não há como
deduplicar, e `uniqueLeads` já a ignora). **Não corrigido aqui:** é etapa `paid`,
onde a guarda não roda, e mexer na resolução de `email` trocaria o dedup de
caches publicados. Curiosidade que reforça o cuidado: a survey dessa etapa mapeia
`email: "email"` — a coluna **vazia**. Candidato a story própria.

#### ✅ Validação visual (2026-08-19, pelo usuário, ambiente local sobre banco de produção)

`FZ / fz-l2-jun-26 / Captação Gratuita` — **o aviso de guarda indisponível NÃO
aparece mais.** É o DoD desta story, atendido.

⚠️ **Mas `Conv. LP` continua sem alvo, e o motivo NÃO é a guarda.** Verificado
nas 5 gratuitas: `guarda=aplicada` em todas, e `convLP` com
`motivo: "baseInsuficiente"` — houve janela, nenhuma atingiu o piso de base.
`coberturaAtipica` só é emitido quando a guarda barra alguma janela
(`cadeia-cac.ts:642-646`), e para barrar é preciso haver candidata.

A base do `convLP` são **leads por campanha**, que a Story 44.3 ainda não produz
— é o segundo aviso que a aba exibe na mesma tela. Então: esta story entregou o
produtor e armou a guarda; o efeito de barrar janela só será observável quando
a 44.3 ligar. O caminho `coberturaAtipica` segue coberto por teste, não por
dado de produção.

#### ⚠️ O que a validação visual das OUTRAS etapas revelou — `utm_content` também não era lido

Conferindo `pps1 / Aquisição` contra `dg-pg02 / Downsell`: a segunda tinha 92,7%
de cobertura, a primeira **0,0%** — nenhum lead atribuído em 26 dias, numa etapa
com R$ 15 mil investidos.

Não era rastreio ruim. A `pps1` guarda o Ad ID numa coluna chamada **`co=`**,
com **70 de 77** preenchidas, e nenhum alias acha esse nome. O mapeamento da
survey aponta `utm_content: "co="` — e continuava não sendo lido, porque a
primeira correção promoveu só a data.

Entregar a cobertura com 0% falso mina exatamente a guarda que ela alimenta.
**Decisão do usuário: promover `utm_content` junto com a data** (commit
`5a1189be`). Alcance medido antes de decidir: **1 etapa das 15** — nenhuma outra
tem `utm_content` mapeado divergente do alias.

Depois do rebackfill: **`pps1` foi de 0,0% para 88,2%** (67 de 76).

⚠️ **`utm_source`, `utm_medium`, `utm_term`, `email` e `phone` continuam no
alias.** Há teste travando **duas** delas (`utm_source` e `email`) — QA-4412-06:
`phone`, `utm_medium` e `utm_term` não têm reversão que as pegue. `classifyOrigem`/`classifyCanal`/
`classifyTemperatura` os consomem — promovê-los reclassificaria Pago/Orgânico e
canal em caches já publicados, mudando número que outras abas já mostram. Efeito
colateral conhecido e aceito: a `pps1` segue com os leads pagos **não**
classificados como pagos, porque `utm_source` mora em `s=`. Candidato a story
própria, com o impacto medido antes.

#### 📊 Quadro final (2026-08-19, depois do mapeamento das duas etapas do BBE pelo painel)

**13 de 15 etapas com cobertura diária** — era **1** no início da sessão. As
**7 etapas gratuitas** estão com a guarda `aplicada`:

| etapa | dias | cobertura | guarda |
|---|---|---|---|
| BBE / bbe-pr1-mar-26 / Gratuita | 20 | 94,1% | aplicada |
| BBE / bbe-web-mai-26 / Gratuita | 9 | 93,1% | aplicada |
| DG / dg-pg02 / Downsell | 14 | 92,7% | aplicada |
| FZ / fz-l2-jun-26 / Gratuita | 17 | 58,9% | aplicada |
| FZ / fz-m1-mai26 / Gratuita | 18 | 53,2% | aplicada |
| FZ / fz-m2-jul26 / Gratuita | 8 | 92,6% | aplicada |
| PP / pps1 / Aquisição | 26 | 88,2% | aplicada |

As duas etapas sem cobertura são as que devem mesmo ficar sem: `bbe-pr1 /
Captação paga` (2 linhas, nenhuma coluna com data) e `fz-l2-jun-26 / Captação
Paga` (o `uniqueLeads=0` herdado). **Nenhuma escrita direta em
`column_mapping`** — as duas do BBE foram mapeadas pelo painel, como previsto.

⚠️ Observação para o backlog, fora do alcance desta story: três etapas `paid`
saem com cobertura baixa ou zero (`dg-pg02/Cap.Paga` 0,0% com 51 sem data,
`fz-l3-jul` 0,0%, `dg-pg04/Cap.Paga` 18,7%). A guarda não roda em família paga,
então nada depende disso hoje — mas a 44.13 (cobertura de venda) vai pisar
nesses números.

#### 🛡️ Resposta ao gate CONCERNS (2026-08-19)

Os **3 itens obrigatórios** do @qa, resolvidos:

| # | o que era | resolução |
|---|---|---|
| **QA-4412-05** | 8 artefatos `" 2"` commitados em `5a1189be` | `git rm` — entraram por um `git add -A packages/api` meu. Erro de higiene, não de projeto |
| **QA-4412-01** | cortar o 3º argumento de `calcularTetos` **não quebrava nada** | spy passthrough prova que a série do cache chega ao argumento; a reversão derruba |
| **QA-4412-03** | caso MISTO truncava a série em silêncio | `leadsSemData` sobe ao topo do payload, como `vendasSemDataNoTotal`; o front declara |

⚠️ **Sobre o QA-4412-01, o achado mais importante do gate.** O teste espia a
CHAMADA, não o teto — e a razão é estrutural: `leadsAtribuidos` fica ausente da
série até a 44.3 existir, então `convLP` nunca tem teto na família gratuita e a
guarda nunca tem janela para barrar. **Nenhum comportamento observável muda se o
fio for cortado**, e foi exatamente por isso que o buraco sobreviveu a 15
reversões. Quando a 44.3 ligar, este teste deve virar asserção sobre o teto.

**Não resolvidos nesta rodada, por decisão:**

- `QA-4412-04` (medium) — a mensagem genérica mente quando o que falta é
  **identificador**, não data (`fz-l2-jun-26/Cap.Paga`, 128 leads com
  `email`/`telefone` 0/128). Etapa `paid`: o ramo não renderiza hoje. Vai para o
  backlog junto com o achado herdado que a própria story já documenta.
- `QA-4412-06` (low) — a fronteira das 5 chaves está travada em 2 (`utm_source`
  e `email`); `phone`/`utm_medium`/`utm_term` não têm teste. Corrigir o texto da
  story, que afirmava as cinco.
- `QA-4412-08` (low) — o `findColIdx` do script de diagnóstico diverge do
  serviço (falta o `includes` invertido e o guarda de tamanho), então
  **sub-reporta** a resolução por alias. O levantamento das 9 saiu dele — na
  direção conservadora, mas divergência é dívida.
- `QA-4412-02` já estava corrigido em `d5ac3865`, durante o próprio gate.

**Baseline após as correções:** API **1271/11** (as 11 pré-existentes) · Web
480/480 · `turbo typecheck lint build` 12/12.

### O achado que a implementação expôs

O backfill do BBE saiu com `dias=0` e `leadsSemData=213`. A causa **não era o código novo**: o export do Tally tem as três primeiras colunas de cabeçalho vazio e a data mora na terceira.

```
headers  ["", "", "", "Qual é seu sexo?", "name", "email", ...]
linha    ["Ar7OOAo", "Zjz88go", "29/03/2026", "Feminino", ...]
                                 ↑ índice 2
```

Esse dado **já não era lido antes** — o `range.from/to` dessas etapas já vinha `null`. A 44.12 só tornou o problema visível, porque foi a primeira a precisar da data. E o código fez a coisa certa: declarou `leadsSemData: 213` em vez de inventar.

**Decisão do usuário: (b) mapeamento por índice + (c) cobertura parcial declarada.** Ambas implementadas. Fallback por conteúdo foi recusado — procurar "uma coluna que pareça data" é como se lê a coluna errada em silêncio.

### File List

**Modificados**
- `packages/api/src/services/lead-origin-sync.ts` — `coberturaDiaria`, `leadsSemData`, `resolveColIdx` por índice, `dataMapeadaNaSurvey` (Sessão 2)
- `packages/api/src/services/campaign-attribution.ts` — `comoAdId` exportada
- `packages/api/src/services/cadeia-cac-payload.ts` — reordenação + 4 estados da guarda
- `packages/web/lib/hooks/use-cadeia-cac.ts` · `packages/web/components/funnels/cadeia-cac-stage-tab.tsx`

**Criados**
- `packages/api/src/__tests__/lead-origin-cobertura-diaria.test.ts` (21 testes — 15 + 6 da Sessão 2)
- `packages/api/src/scripts/diag-cobertura-colunas.ts` — diagnóstico read-only que produziu o levantamento das 9

## 🧪 QA Results

**Gate:** ⚠️ **CONCERNS** · **Reviewer:** @qa (Quinn) · **Data:** 2026-08-19
**Arquivo:** `docs/qa/gates/44.12-produtor-de-cobertura-diaria.yml` · **13 commits** (`44bd12e6` → `d5ac3865`), nada pushado

### Veredito

A story faz o que se propôs e a evidência de produção sustenta: de 1 para **11 de 15** etapas com cobertura diária, 5 gratuitas, e a aba sem o aviso de indisponível. A Sessão 2 é o melhor trabalho do lote — **o plano combinado teria produzido efeito zero**, e descobrir isso antes de escrever em produção vale mais que a story inteira.

O que segura o gate é **uma coisa só, e é grande**: o fio que dá título à story não tem um teste.

### 🔴 QA-4412-01 (high) — cortar o fio da guarda não quebra NADA

A ligação é uma linha (`cadeia-cac-payload.ts:310`). Reproduzido — trocando por `calcularTetos(series, familia, {})`, isto é, **a guarda nunca roda**:

```
suíte INTEIRA da API   1267 passando / 11 falhando
baseline               1267 passando / 11 falhando
typecheck              limpo
```

Byte por byte o mesmo. E o pior não é a lacuna: `guardaDeCobertura` é derivado de `lead?.coberturaDiaria` (`:309`), **independente de esse valor ter chegado ao `calcularTetos`**. Com o fio cortado o payload continua devolvendo `estado: "aplicada"` e a aba continua dizendo "Guarda de rastreio ativa". Não há fonte única entre *a guarda rodou* e *o payload diz que rodou*.

Por que os 21 testes novos não pegam: o único teste "ponta a ponta" (`:225-254`) chama `calcularTetos` **direto**, com `cobertura` montada à mão (`:234-238`) — nunca a saída do produtor, nunca pela rota. Ele testa o `shared`, que a 44.6 e a 44.7 já testaram.

### 🟡 QA-4412-03 (medium) — no caso MISTO a série é truncada em silêncio

Sonda pela rota, com 10 dias de cobertura e `leadsSemData: 213`:

```
guardaDeCobertura        { estado: "aplicada", motivo: null, message: null, dias: 10 }
payload menciona "213"?  false
```

`leadsSemData` só é dito quando `comLead === 0`. E não é chave de topo do payload — mas **`vendasSemDataNoTotal` é**. A AC2 desta story diz literalmente que `leadsSemData` segue "o mesmo padrão do `vendasSemDataNoTotal` da 44.8". O padrão citado é o que não foi seguido.

Não é cosmético: a mediana de referência da guarda sai da série **sobrevivente**. Metade dos leads sem data legível → mediana de meia amostra, e a aba diz "protegido" sem ressalva.

### 🟡 QA-4412-04 (medium) — a mensagem genérica mente quando o problema é IDENTIFICADOR

Derivado do produtor: uma linha só entra em `linhasDeCobertura` se tiver `key` (`:537`), e `leadsSemData` só sobe se `key && !date` (`:538`). Logo `coberturaDiaria === []` **e** `leadsSemData === 0` significa *nenhuma linha tem e-mail nem telefone* — e a frase servida é "nenhum dia do período tem lead". É o caso da `fz-l2-jun-26/Cap.Paga` que a própria story documenta (128 leads, `email`/`telefone` 0/128). Mesma classe que o `09583f69` corrigiu no ramo vizinho. Hoje não renderiza porque é `paid`; nada impede uma gratuita. `identifiersFilled` já está no mesmo objeto (`:341`).

### 🟡 QA-4412-05 (medium) — 8 artefatos `" 2"` foram COMMITADOS

O `5a1189be` declara 1.214 inserções; **~1.120 não são da story**. Risco operacional é nulo por sorte (vitest não casa `*.test 2.ts`; o `_journal.json` tem 0 referências a `users_listed`), mas `campaign-attribution 2.ts` está dentro de `src`, o tsconfig inclui `["src"]`, e ele vai para `dist/` — a cópia **pré-44.12**, com `comoAdId` sem `export`. A ironia: é o arquivo cujo comentário na cópia viva diz que duas definições dariam populações diferentes.

### ✅ QA-4412-02 — achado por mim, corrigido pelo @dev durante a revisão (`d5ac3865`)

A frase afirmava "**e descartou janelas de cobertura atípica**" sempre que `estado === "aplicada"`. Sonda com cobertura uniforme:

```
guarda        { estado: "aplicada", dias: 3 }
tetos.convLP  { valor: null, motivo: "baseInsuficiente" }
```

E `baseInsuficiente` é emitido (`shared:641-646`) exatamente quando `houveBarradaPelaGuarda === false` — **zero janelas barradas**. Era o estado das cinco gratuitas na validação visual. A frase falsa estava na tela e passou. Corrigido para uma afirmação sobre a **regra**, verdadeira sempre. Residual: a AC5 pede a contagem de janelas barradas, que segue não entregue (débito registrado no código, endereçado à 44.3).

### 15 reversões executadas por mim — 13 diferenciais, 2 lacunas

| # | reversão | falhas | veredito |
|---|---|---|---|
| R1 | promoção da survey inteira OFF | 4 | diferencial |
| R2 | só `utm_content` deixa de ser promovido | 1 | diferencial |
| R3 | promover `utm_source` | 1 | diferencial |
| R4 | promover `email` | 1 | diferencial |
| **R5** | **promover `phone` + `utm_medium` + `utm_term`** | **0** | **LACUNA (QA-4412-06)** |
| R6 | `comoAdId` removido | 1 | diferencial |
| R7 | mapeamento por índice desligado | 1 | diferencial |
| R8 | dedup por dia vira global | 2 | diferencial |
| R9 | mensagem de data ilegível → genérica | 1 | diferencial |
| R10 | volta a ler o cache 2× | 14 | diferencial |
| R11 | `aplicada`/`semLeadNoPeriodo` colapsam | 2 | diferencial |
| R12 | índice vence o nome | 1 | diferencial |
| R13 | `leadsSemData` para de contar | 4 | diferencial |
| R14 | linha sem identificador entra na cobertura | 1 | diferencial |
| **R15** | **`calcularTetos` deixa de receber a cobertura** | **0** | **LACUNA CRÍTICA (QA-4412-01)** |

⚠️ Sobre a fronteira do `dataMapeadaNaSurvey`: a story afirma (linha 392-393) que as cinco chaves têm "teste travando isso". São **duas de cinco** — `utm_source` e `email`. `utm_medium` alimenta `classifyCanal`, `utm_term` alimenta `classifyTemperatura`, e ambos são publicados no endpoint público.

### O que eu verifiquei por mim

- **Sem PII.** Os dois campos novos são contagens; as chaves de dedup vivem só em memória e nunca são serializadas.
- **A query nova é escopada** por `projectId` resolvido pelo `innerJoin` etapa→funil, não por valor do request. E é **preguiçosa** — etapa sem `utm_content` não paga nada.
- **O `diag-cobertura-colunas.ts` é estritamente read-only** (só `SELECT` e `readSheetData`; zero INSERT/UPDATE/DELETE). **Veredito: manter versionado** — segue convenção da mesma pasta e 2 etapas ainda dependem dele. Mas o `findColIdx` dele **diverge** do serviço (falta `na.includes(h)` e o guarda `h.length > 2`), e o levantamento das 9 saiu dessa ferramenta.
- **Números já publicados MUDARAM** no `GET /api/public/v1/.../leads-origin`, que serve o payload inteiro: `range.from/to` (grupo B), `topUtm.content` e `columnsResolved.utmContent` (`pps1`). Todos para melhor — mas mudaram, e a story diz que nada publicado mudou.

### Por que CONCERNS e não FAIL

Considerei FAIL pelo QA-4412-01, e por mais tempo que nos gates anteriores: ele é pior que o QA-449-01 numa dimensão que importa — lá o campo sem cobertura era acessório, aqui é a entrega que dá **título** à story.

Três coisas seguraram: **(1)** a ligação está correta — li o código, não só rodei o teste, e a validação visual mostrou as cinco gratuitas saindo de `indisponivel` para `aplicada`; o defeito é latente, e a régua que a 44.9 fixou ("reprovar por risco futuro seria mudar a régua no meio do epic") se aplica. **(2)** O único defeito **presente** no comportamento entregue — a frase que afirmava ter descartado janelas — já foi corrigido no `d5ac3865`. Era o perfil que reprovaria, e deixou de ser. **(3)** QA-4412-03 e -04 são omissões de declaração, não números errados.

**Antes do merge:** QA-4412-05 (mecânico e obrigatório), QA-4412-01 (um teste, não um redesenho), QA-4412-03.
**Backlog:** QA-4412-02 residual, -04, -06, -07, -08, -09, -10.

⚠️ **Para o @devops:** o backfill JÁ rodou contra produção com o código da branch. Enquanto a API deployada não tiver este código, **o sync diário vai reescrever os caches sem `coberturaDiaria` e desligar a guarda de novo nas 11 etapas.**

⚠️ CodeRabbit SKIPPED pelo sexto gate seguido (CLI em WSL, host macOS).

**Baseline:** API 1267/11 (as 11 são pré-existentes e nenhuma toca a 44.12) · Web 480/480 · `turbo typecheck lint build` 12/12, confirmado também com `--force`. O DoD registra "1264/11" — número de antes dos 3 testes do `5a1189be`.

## 📝 Change Log

| Data | Autor | Mudança |
|---|---|---|
| 2026-08-20 | @devops (Gage) | **Merge feito — e a guarda caiu, como a própria story previu.** PR #562 squash → `578f60bf` na main; CI verde nos 5 checks, sem stack de PR. **Antes do merge, rodei o `diag-cobertura-colunas.ts` (read-only) contra produção: 15 de 15 etapas com `coberturaDiaria` AUSENTE** — não 13, não 11. O sync diário rodou com a API deployada (que não tem o produtor) e reescreveu **todos** os caches, inclusive as 13 que o backfill da sessão anterior havia preenchido. É exatamente o aviso que o @dev deixou para o @devops no gate, agora medido. **A ordem é obrigatória:** deploy da API em `578f60bf` → `backfill-lead-origin.ts` → reconferir com o diag. Backfill antes do deploy é trabalho perdido: o próximo sync limpa. ClickUp `86ak2uc8w` → `ready to ship` (não `done`: o merge não é o efeito, o deploy é). |
| 2026-08-19 | @dev (Dex) | **Sessão 2 — o levantamento das 9 e a correção que ele exigiu.** O plano de escrever `column_mapping` em produção não teria efeito: o sync nunca leu `funnel_surveys.column_mapping` (`:175`, `:186` devolviam `null`) e o campo lá se chama `timestamp`, não `date`. **7 das 9 etapas já tinham a coluna certa mapeada no painel** — faltava lê-la. Em 2 delas o alias ainda escolhia coluna PIOR que a mapeada (`fz-m2-jul26`: `Data Conversão` com 0 de 343 células, contra `Submitted at` com 343/343). Commit `2ef170a0` propaga **só a data**. Backfill: de 1 para **11 de 15** etapas com cobertura, 5 gratuitas — sem nenhuma escrita em `column_mapping`. Restam 2 etapas do BBE, a mapear **pelo painel**. Achado herdado declarado: `fz-l2-jun-26/Cap.Paga` tem `uniqueLeads=0` desde antes desta story (colunas `email`/`telefone` 0/128 vazias) |
| 2026-08-19 | @po (Pax) | **Validada — GO, 9/10.** Status Draft → Ready. **A correção que o @sm me fez procede e está registrada na story:** eu apontei `campaign-attribution.ts` na validação da 44.8 quando o caminho barato era `lead-origin-sync.ts`, e conferi as três linhas (`:379`, `:394`, `:399`, mesma iteração). **Decisão 1: resolve** — mas não pelo argumento do @sm. O decisivo é que cobertura é **razão**, e o numerador do `convLP` (`leadsAtribuidos` do `DiaBruto`) conta **só id que resolve**, via `mapearAdsParaCampanhas`. Contar id não resolvido na cobertura compararia populações diferentes, e a guarda barraria janelas pelo motivo errado. **E o draft subestimou esse custo:** `mapearAdsParaCampanhas` exige `projectId`, que `computeLeadOriginForStage` **não tem** (`:288-291`), e resolver antes de agrupar obriga a **duas passadas** sobre as linhas — complexidade 5 → 8. **Decisão 2: confirmada**, com o teste obrigatório e o comentário no ponto onde o `Map` nasce. **Decisão 3: backfill DENTRO**, porque o DoD desta story pede uma gratuita sem o aviso, e isso é insatisfazível sem reprocessar o cache |
| 2026-08-19 | @sm (River) | Criada. **O levantamento corrigiu uma estimativa que o próprio @po deu na validação da 44.8:** lá ficou registrado que esta story seria cara porque `LeadParaAtribuir` não tem data. Está certo sobre o arquivo errado — o lugar não é `campaign-attribution.ts`, é `lead-origin-sync.ts`, e lá **data, chave de dedup e `utm_content` já estão no mesmo laço, na mesma linha** (`:379`, `:394`, `:399`). O código é ~15 linhas. **O custo real são três decisões**, e é por isso que isto é story e não commit: (1) "atribuído" é UTM preenchido ou UTM que resolve — muda o número; (2) a dedup por dia faz `Σ leadsTotais ≠ uniqueLeads`, o que é correto e parece bug; (3) o cache existente não tem o campo, então **deploy não liga a guarda** — só o backfill. |

<!-- clickup:86ak2uc8w -->
