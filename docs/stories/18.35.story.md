# Story 18.35: Agrupar Content/Ad por Name Resolvido

**Epic:** 18  
**Status:** Draft  
**Assignee:** @dev  
**Branch:** feature/18.35-content-grouping-by-name

---

## Story

Story 18.32 AC2-3 deveria agrupar dados por **name resolvido** (vários IDs com mesmo name consolidam em uma linha). Atualmente, quando um Ad ID não é resolvido pela Meta API, aparece como número (`120251580121030273`) em vez de:
1. Um name (se resolvido)
2. Um identificador inteligente (se não resolvido)

**Problema:** Backend retorna IDs em vez de names na chave da resposta (`content` field).

**Objetivo:** Garantir agrupamento correto por name — vários IDs da mesma campanha/criativos consolidam em uma única linha.

## Acceptance Criteria

### AC1: Investigar Agrupamento Current
- [ ] Verificar se `contentByName.get()` está funcionando corretamente
- [ ] Confirmar que `adName` está sendo resolvido antes do agrupamento
- [ ] Identificar por que alguns IDs não estão sendo agrupados

### AC2: Implementar Fallback Smart para Unresolved IDs
- [ ] Se um Ad ID não resolver via `resolveEntityNames`, usar padrão: `"[Unresolved] 120251580121030273"`
- [ ] IMPORTANTE: Não usar "Ad #ID" — isso ainda aparece como ID. Usar label descritivo.
- [ ] Todos os IDs com mesmo name consolidam, mesmo que não resolvidos

### AC3: Validar Grouping
- [ ] Cada linha na tabela = uma unique name (resolvida ou fallback)
- [ ] Vendas/Bruto agregados corretamente por name
- [ ] Nenhum ID aparece como "número nu"
- [ ] Sorting por vendas funciona

### AC4: Testes
- [ ] Tabela "Por Content (Ad)" mostra nomes agrupados (não números)
- [ ] Múltiplos IDs com mesmo name = uma linha
- [ ] Dados agregados corretos
- [ ] Lint ✅, Typecheck ✅

## Dev Notes

**Current Implementation (18.32 AC3):**
```typescript
const contentByName = new Map<string, ContentGroup>();
for (const [contentId, metrics] of utmContentMap.entries()) {
  const adName = (contentNames.get(contentId) ?? contentId).trim() || "(sem nome)";
  // ... grouping ...
}
const porUtmContentGrouped = Array.from(contentByName.values())
  .map(({ name, vendas, bruto, liquido }) => ({ content: name, name, vendas, bruto, liquido }))
```

**Issue:** Quando `contentNames.get(contentId)` retorna `undefined`, usa `contentId` como fallback. Depois agrupa por isso.

**Fix:**
1. Se `contentNames.get(contentId)` é undefined E contentId é numérico, usar label: `"[Unresolved] ID"`
2. Isso garante que todos os IDs falhados com padrão similar agrupem juntos

**Files to Modify:**
- `/packages/api/src/routes/stage-sales-data.ts` (linhas 612-614, contentByName grouping)

**Reference:**
- Story 18.32 AC3: implementação original
- Story 18.33: fallback inteligente (mas não é suficiente)

**Testing Checklist:**
- [ ] Tabela mostra nomes resolvidos (não IDs)
- [ ] IDs não resolvidos agrupam com label inteligente
- [ ] Vários IDs mesma campanha = 1 linha
- [ ] Agregação correta (vendas somados)

## File List

- `packages/api/src/routes/stage-sales-data.ts` (modified)

## Change Log

- Created: 2026-06-08
- Status: Draft

---

## Dev Agent Record

### Checkpoint 1: Investigation
- [ ] TODO: Verificar por que `contentByName` está usando IDs em vez de names
- [ ] TODO: Confirmar que `resolveEntityNames` está sendo chamado corretamente

### Checkpoint 2: Implement Smart Fallback
- [ ] TODO: Usar label "[Unresolved] ID" para IDs não resolvidos
- [ ] TODO: Garantir agrupamento por name (ou padrão de fallback)

### Checkpoint 3: Validation
- [ ] TODO: Testar com dados que têm IDs não resolvidos
- [ ] TODO: Verificar agregação de métricas
- [ ] TODO: CodeRabbit review passed

### Debug Log
- Issue: Tabela mostra "120251580121030273" em vez de nome resolvido
- Root Cause: Fallback em grouping não está usando pattern inteligente; cada ID fica como própria chave

### Completion Notes
*Pending implementation*
