# Story 18.58: Testes de LPs — Tooltip de cálculo em todas as colunas

**Epic:** 18 — Dashboard Improvements
**Status:** Done
**Created:** 2026-07-16 (@sm — River)
**Priority:** Low
**Depends On:** 18.44/18.46 (tabela Testes de LPs e fórmulas), 18.50 (Vendas/Faturamento por LP via co=), 18.56 (célula do nome — não conflitar com o lápis)

<!-- clickup:86ajjh3gq -->

---

## 📖 Contexto

A tabela **Desempenho de Testes de LPs** (`lp-performance-table.tsx`) exibe métricas calculadas (`lp-metrics-calculator.ts`), mas os headers não explicam **como** cada número nasce — o gestor precisa confiar de memória (ou perguntar) o que é Connect Rate, de onde vem o Investimento (com ou sem imposto?), qual denominador da Tx Conv. etc.

O usuário pediu (2026-07-16): **tooltip em todas as colunas** da tabela indicando como o cálculo foi feito.

As fórmulas reais (fonte: `lp-metrics-calculator.ts` + `useLpPerformanceData.ts`):

| Coluna | Cálculo/Fonte (código) |
|--------|------------------------|
| LP | Derivada do Campaign Name da campanha Meta (18.46); sem `lpX` no nome → "LPA" |
| Investimento (R$) | Soma do spend Meta da LP **+ imposto 12,15%** (`applyMetaAdsTax`, `useLpPerformanceData.ts:110`) |
| Leads (free) | Planilha de leads cruzada por LP (`utm_term`/`utm_content` contém `lpX`), respeitando filtro Hot/Cold |
| CPL (free) | Investimento ÷ Leads (`calculateCPL`) |
| Vendas (paid) | Vendas da planilha atribuídas via `co=` da venda → campanha da LP (18.50), dedupadas |
| CPV (paid) | Investimento ÷ Vendas (`calculateCPV`) |
| CPM | (Investimento ÷ Impressões) × 1000 (`calculateCPM`) |
| CPC | Investimento ÷ Cliques (`calculateCPC`) |
| CTR (%) | (Cliques ÷ Impressões) × 100 (`calculateCTR`) |
| LP View | `landing_page_view` real da Meta API, somado por LP (18.46 AC4) |
| Connect Rate (%) | (LP Views ÷ Cliques no link) × 100 (`calculateConnectRate`; pode passar de 100% por particularidade de tracking) |
| Tx Conv. (%) | (Resultado ÷ Cliques no link) × 100 — Resultado = Leads na Gratuita, Vendas na Paga (`calculateTxConv`) |
| Faturamento (R$) (paid) | Soma do bruto das vendas atribuídas via `co=` → campanha da LP (18.50) |
| ROAS (paid) | Faturamento ÷ Investimento (`calculateROAS`) |

> ⚠️ **Nota do PO (anti-alucinação):** o comentário de cabeçalho de `lp-metrics-calculator.ts` (linhas 8-9) está **desatualizado** (fórmulas pré-18.46: "Connect Rate: Conversões ÷ Cliques" e "Tx Conv.: Conversões ÷ LP Views"). A tabela acima reflete as **implementações reais** das funções (JSDoc individual de cada função está correto). @dev: usar as funções como fonte dos textos, NÃO o cabeçalho do arquivo; aproveitar para corrigir o cabeçalho stale junto com a nota da R1.

---

## ✅ Acceptance Criteria

**AC1 — Tooltip em todos os headers**
- **Given** a tabela Desempenho de Testes de LPs (Paga OU Gratuita), **when** o usuário passa o mouse sobre **qualquer** header de coluna, **then** um tooltip explica a fórmula/fonte daquela coluna, com os textos da tabela do Contexto (adaptar redação, não a semântica).
- Colunas condicionais respeitam o stage: free mostra tooltips de Leads/CPL; paid de Vendas/CPV/Faturamento/ROAS.

**AC2 — Conteúdo fiel ao código**
- Os textos refletem EXATAMENTE o cálculo implementado (fonte: `lp-metrics-calculator.ts` e `useLpPerformanceData.ts`) — incluindo: Investimento com imposto 12,15%; Connect Rate podendo exceder 100%; Tx Conv. com denominador de cliques no link; Vendas/Faturamento atribuídos via `co=` → campanha (podem divergir dos cards, que contam todas as vendas).

**AC3 — Mecanismo consistente com o repo**
- Usar o padrão de tooltip já usado nas tabelas do dashboard (ex.: headers da tabela de Criativos usam `title` nativo — Story 18.55; existe também `components/ui/tooltip.tsx`). Escolher UM padrão e aplicar em todas as colunas; se `title` nativo, indicar affordance discreta (ex.: cursor-help / underline pontilhado no header).
- Não conflitar com o clique de ordenação (se houver) nem com o lápis/link da célula LP (18.56) — tooltip é só no header.

**AC4 — Regressão zero**
- Nenhuma mudança em valores, colunas, ordenação, filtros ou layout além do tooltip. Paga e Gratuita.

---

## 📊 Complexidade & Valor

- **Estimativa:** XS — textos + atributo/wrapper de tooltip nos headers de 1 componente.
- **Valor de negócio:** autoexplicação da tabela — qualquer pessoa do time entende o número sem perguntar; reduz desconfiança quando tabela × cards divergem (atribuição via co=).

## ⚠️ Riscos

- **R1 (baixo)** — Texto de tooltip desatualizar quando uma fórmula mudar em story futura. **Mitigação:** manter os textos ao lado das colunas no próprio componente (constante única `COLUMN_TOOLTIPS`) e nota no calculator apontando para ela.

## 📦 Escopo

**IN:**
- `packages/web/lib/components/funnels/lp-performance-table.tsx` — tooltips nos headers (constante de textos + render).

**OUT:**
- Tooltips em outras tabelas (Criativos, Dados Diários) — Criativos já tem os essenciais (18.55).
- Mudança de fórmulas ou dados.

---

## 🧪 Definition of Done

- [x] Todas as colunas (free e paid) com tooltip explicando cálculo/fonte (Tx Conv. com texto por stage).
- [x] Textos conferidos contra `lp-metrics-calculator.ts`/`useLpPerformanceData.ts` (imposto 12,15%, co=, denominadores por cliques no link, Connect Rate >100%).
- [x] Sem regressão visual/funcional na tabela (só affordance cursor-help + underline pontilhado nos headers).
- [x] `tsc --noEmit` web verde + `next build` verde.

---

## 📁 File List

| Arquivo | Mudança |
|---------|---------|
| `packages/web/lib/components/funnels/lp-performance-table.tsx` | Constante `COLUMN_TOOLTIPS` (14 textos, fiéis às implementações) + componente `HeadWithTooltip` (`title` nativo, cursor-help, underline pontilhado) aplicado a todos os headers; Tx Conv. com texto por stage (paid=Vendas, free=Leads) |
| `packages/web/lib/utils/lp-metrics-calculator.ts` | Cabeçalho stale corrigido (Connect Rate/Tx Conv. pós-18.46) + nota apontando para `COLUMN_TOOLTIPS` (mitigação R1) |

---

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-07-16 | @sm (River) | Story criada (4 ACs) a partir de pedido direto do usuário: tooltip de cálculo em todas as colunas da tabela Testes de LPs. Fórmulas extraídas do código (lp-metrics-calculator.ts / useLpPerformanceData.ts) para garantir fidelidade (Artigo IV). |
| 2026-07-16 | @po (Pax) | Validação GO — score 10/10 (checklist de 10 pontos). Anti-alucinação: as 8 fórmulas da tabela do Contexto conferidas função a função em `lp-metrics-calculator.ts` (todas EXATAS, incl. Connect Rate >100% e Tx Conv por cliques no link); `applyMetaAdsTax` confirmado em `useLpPerformanceData.ts:110`; leads por LP via `utm_term`/`utm_content` com filtro hot/cold e vendas/faturamento via `co=` (18.50) confirmados; headers `TableHead` (:198-230) e `LpNameCell` (18.56, :65) presentes; `components/ui/tooltip.tsx` existe; padrão `title` nativo da 18.55 confirmado. Achado: cabeçalho do calculator com fórmulas stale — nota adicionada no Contexto. Status: Draft → Ready. |
| 2026-07-16 | @dev (Dex) | Status Ready → **InProgress** (mesma branch da 18.57, implementada após a reordenação conforme recomendação do PO). |
| 2026-07-16 | @dev (Dex) | Implementação completa (2 arquivos). Mecanismo escolhido (AC3): `title` nativo (padrão 18.55) com affordance cursor-help + underline pontilhado discreto. Textos usados das implementações reais (não do cabeçalho stale, que foi corrigido junto — nota do PO atendida). Tooltip do Tx Conv. muda por stage. Gates: tsc web + next build verdes. Status InProgress → **InReview**. |
| 2026-07-16 | @qa (Quinn) | QA gate **PASS** — 4/4 ACs; os 15 textos de `COLUMN_TOOLTIPS` conferidos contra as implementações reais (calculadora, `applyMetaAdsTax` 12,15%, leads via `utm_term`/`utm_content`+Hot/Cold, vendas/faturamento via `co=` com dedup no backend); cabeçalho stale do calculator corrigido (nota do PO atendida); zero chamada Meta API; tsc web verde. Concern low: TEST-001 (débito de infra herdado). Gate: `docs/qa/gates/18.58-lp-table-tooltips-calculo.yml`. Status InReview → **Done**. |

---

## QA Results

### Review Date: 2026-07-16

### Reviewed By: Quinn (Test Architect)

**Escopo revisado:** `git diff 625ac44..da5eb29` — `lp-performance-table.tsx` (+66/-11) e `lp-metrics-calculator.ts` (+7/-3).

#### 7 Quality Checks

| # | Check | Resultado | Evidência |
|---|-------|-----------|-----------|
| 1 | Code review | ✅ PASS | Constante única `COLUMN_TOOLTIPS as const` (mitigação R1) + componente `HeadWithTooltip` pequeno e tipado; `className` repassado preserva alinhamento `text-right` |
| 2 | Testes | ✅ PASS | Sem runner no web (TEST-001, low, herdado). **Fidelidade verificada texto a texto (15 textos):** `lp` (backend `lpAndTempFromCampaignName` :104 — lpX no campaign name, sem match → LPA ✓); `investimento` (`applyMetaAdsTax` em `useLpPerformanceData.ts:110`, gross-up 12,15% 2026+ ✓); `leads` (`useCrossReferenceLeads` :199-205 — `utm_term`/`utm_content` contém lpX, quebra hot/cold, filtro no hook ✓); `vendas`/`faturamento` (backend :352-400 — `co=` → campanha, dedup por transactionId/email, vendas sem `co=` ficam fora, valor bruto preferido ✓); `cpm`/`cpc`/`ctr`/`cpl`/`cpv`/`roas` idênticos às funções ✓; `lpView` (`landingPageViews` da Meta API somado por LP ✓); `connectRate` (LP Views ÷ Cliques, JSDoc confirma >100% possível ✓); `txConvPaid`=Vendas÷Cliques / `txConvFree`=Leads÷Cliques com switch `isPaid` no header ✓ |
| 3 | ACs | ✅ PASS | 4/4 — todos os 14 headers (free e paid) com tooltip; condicionais respeitam o stage; mecanismo único (`title` nativo, padrão 18.55) com affordance cursor-help + underline pontilhado |
| 4 | Regressões | ✅ PASS | Diff só troca `TableHead` → `HeadWithTooltip` (que rende `TableHead` por dentro); body/valores/filtros/colunas intocados; tooltip no `span` do header — zero conflito com `LpNameCell`/lápis (18.56, célula intocada); tabela não tem ordenação por clique |
| 5 | Performance | ✅ PASS | `title` nativo = zero JS/estado/listener; nenhum fetch ou render extra |
| 6 | Security | ✅ PASS | Texto estático hardcoded em atributo `title` (React escapa); nenhum input de usuário |
| 7 | Documentação | ✅ PASS | File List, Change Log, DoD 4/4; **cabeçalho stale do calculator corrigido** (fórmulas pós-18.46) + nota cruzada para `COLUMN_TOOLTIPS` — nota do PO atendida ✓ |

#### Gates técnicos

- `npx tsc --noEmit` (packages/web): ✅ exit 0 (rodado nesta review)
- `next build`: SKIPPED — validado pelo @dev, sem suspeita concreta
- **Meta API:** zero chamada nova (zero diff em `packages/api`) — diretriz de rate limit respeitada ✅

#### Issues

- **TEST-001 (low):** sem teste automatizado para presença/conteúdo dos tooltips (vitest não instalado no web — débito herdado).

#### Nota de fidelidade (informacional)

O tooltip de Faturamento diz "valor bruto" — a implementação prefere o bruto e só cai para o líquido quando bruto ≤ 0 (edge case). Mesma semântica do Contexto da story e da 18.50; sem ação.

### Gate Status

Gate: PASS → docs/qa/gates/18.58-lp-table-tooltips-calculo.yml
