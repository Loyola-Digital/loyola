# Story 19.8 — Sellers Breakdown by Lead Profile

**Epic:** 19 (Stage Sales)
**Status:** Ready for Review
**Points:** 5
**Depende de:** 19.5 (planilha de vendas), 18.17 (lead scoring com faixa pré-calculada)

## User Story

Como gestor da Loyola, quero ver no dashboard de Stage Sales **quem vendeu pra quem** — cruzando o `utm_source` da venda (que carrega o nome do vendedor) com o **perfil/banda** do lead que comprou, identificado pelo email — para entender qual vendedor converte melhor cada faixa de lead e onde alocar esforço.

## Acceptance Criteria

- [x] **AC1:** Nova rota `GET /api/projects/:projectId/funnels/:funnelId/stages/:stageId/sellers-breakdown?startDate&endDate` que retorna:
  ```ts
  {
    sellers: Array<{
      utmSource: string;          // nome do vendedor (bruto)
      totalSales: number;
      totalRevenue: number;
      avgTicket: number;
      dominantBand: "A" | "B" | "C" | "D" | "no_profile";
      bands: { A: number; B: number; C: number; D: number; no_profile: number };
      bandsPct: { A: number; B: number; C: number; D: number; no_profile: number };
    }>;
    coverage: { matched: number; total: number; pct: number };
    semDados: boolean;
  }
  ```
- [x] **AC2:** Match lead↔venda por **email normalizado** (lowercase + trim).
- [x] **AC3:** Banda do lead resolve via Story 18.17 (`precomputed_band_column` / `survey.columnMapping.faixa`) quando configurado; senão recalcula via `computeBands`/`scoring_model`.
- [x] **AC4:** Vendas sem lead matched vão pro bucket `no_profile`. Cobertura exibida no topo (ex: "85% das vendas matched com lead").
- [x] **AC5:** Frontend renderiza grid de cards (1 card por vendedor) no Stage Sales dashboard, mostrando: total vendas, R$ total, ticket médio, banda dominante destacada, distribuição percentual de bandas (mini-pizza ou barras compactas).
- [x] **AC6:** Cards ordenados por R$ total desc.
- [x] **AC7:** Respeita filtro de período (startDate/endDate) do dashboard.
- [x] **AC8:** Typecheck shared/api/web zero erros.

## Tasks

- [x] 1. Backend: `packages/api/src/routes/stage-sales-data.ts` (ou novo `sellers-breakdown.ts`)
  - Lê planilha de vendas via existing `readSheetData` + `columnMapping`
  - Lê planilha de survey via `funnelSurveys` + `readSheetData`
  - Resolve banda por lead (reusa helpers de `lead-scoring.ts` — `findQuestionColumn`, `findAnswerMatch`, `computeBands`)
  - Match por email normalized
  - Agrega por `utm_source`
- [x] 2. Frontend: hook `useSellersBreakdown(projectId, funnelId, stageId, customRange)` + componente `SellersBreakdownGrid` no `stage-sales-section.tsx`
- [x] 3. Typecheck.

## Dev Notes

### Por que reusar lead-scoring helpers?

`findQuestionColumn`, `findAnswerMatch` e `computeBands` já implementam toda a normalização (acentos, NBSP, contains bidirecional — fixes commits `bcfb132`, `116ddce`, `9e396e8`). Reescrever seria duplicação.

### Email matching

Não fazer fuzzy aqui. Email é identificador — match exact após `.toLowerCase().trim()` e remover whitespace interno. Diferenças em domínios (`gmail.com` vs `gmaill.com`) devem aparecer como sem match.

### Banda dominante

Se 2 bandas empatam, ordem A > B > C > D > no_profile (banda melhor vence).

### Performance

Cache em memória 5min (mesma estratégia de `traffic-analytics.ts`) pra evitar re-fetch da planilha em cada interação do dashboard.

## File List

- `packages/api/src/routes/stage-sales-data.ts` *(modified — adiciona endpoint)*
- `packages/web/lib/hooks/use-sellers-breakdown.ts` *(new)*
- `packages/web/components/funnels/sellers-breakdown-grid.tsx` *(new)*
- `packages/web/components/funnels/stage-sales-section.tsx` *(modified — mounta o grid)*

## Change Log

| Data | Quem | Mudança |
|---|---|---|
| 2026-05-28 | @dev Dex | Story criada e iniciada (request direto do Lucas) |
