storyId: 29.21
title: "Detalhamento: status Meta, resize por arraste, 'Por Campanha' e rótulos no gráfico Margem"
reviewer: "@qa Quinn"
date: 2026-07-09
branch: feat/perpetual-detalhamento-status-drag-margin-29.21
verdict: CONCERNS
summary: >
  Escopo entregue (AC1–AC8) confere com o código. Implementação sólida, reusa
  padrões existentes, typecheck + build OK, sem regressão nas features 29.19/29.20.
  Aprovado com 3 observações LOW/advisory — nenhuma bloqueia o ship.

quality_checks:
  code_review: PASS       # reusa statusColors, molde SpendPointLabel, sem duplicação
  unit_tests: CONCERNS    # sem testes novos (padrão do repo p/ dashboards); chart-granularity intacto
  acceptance_criteria: PASS  # AC1–AC8 atendidas conforme reescritas
  regressions: PASS       # detailRows/sortedRows/overlay não tocados; dimWidth preservado
  performance: PASS       # useCampaignPicker reusa queryKey (sem request extra)
  security: PASS          # sem input não-sanitizado, sem secrets, status de fonte confiável
  documentation: PASS     # story, Change Log e Dev Agent Record completos

issues:
  - id: Q1
    severity: low
    category: code
    description: >
      `startDimResize` adiciona listeners de mousemove/mouseup no `document` e seta
      `document.body.style.userSelect='none'` sem cleanup no unmount. Se o componente
      desmontar durante um drag ativo, os listeners e o userSelect ficam órfãos até o
      próximo mouseup.
    likelihood: very_low   # exige arrastar E desmontar simultaneamente
    recommendation: >
      Opcional — mover o drag para um useEffect com cleanup, ou guardar refs dos
      handlers e removê-los no unmount. Não bloqueia.
  - id: Q2
    severity: low
    category: code
    description: >
      `fmtCurrencyCompact(Math.ceil(num))` — em valores grandes o formato compacto
      (ex.: "R$ 1,2 mil") mascara o efeito do Math.ceil; o arredondamento pra cima só
      é perceptível em valores < 1000.
    recommendation: >
      By-design (atende ao pedido literal). Nota de expectativa apenas.
  - id: Q3
    severity: low
    category: ux
    description: >
      Rótulos renderizados em TODAS as barras (pedido literal). No modo Diário com
      range longo podem ficar apertados/sobrepor. Já documentado pelo @dev.
    recommendation: >
      Se incomodar, aplicar a regra "1 a cada N" do SpendPointLabel (ajuste rápido).

follow_ups:
  - "effective_status (estado real) + status nos níveis Público/Criativo — diferido por custo de Graph API. Abrir story se houver uso recorrente."

decision: >
  CONCERNS — aprovado. Pode seguir para push/PR (@github-devops). Os 3 itens são
  LOW e advisory; Q1 é o único code-smell real e tem probabilidade muito baixa.
