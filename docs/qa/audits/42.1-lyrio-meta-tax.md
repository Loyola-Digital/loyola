# Auditoria — imposto de 12,15% do Meta Ads na etapa Lyrio

**Story:** 42.1 · **Agente:** @dev (Dex) · **Data:** 2026-08-12
**Base auditada:** `main` em `15bdd25` (inclui `df57b84`, que adicionou Google Ads à etapa)
**Veredito geral:** ✅ **Todos os pontos aplicam o imposto exatamente uma vez.** Nenhuma correção de código foi necessária.

---

## Regra sob auditoria

O `spend` da API Meta é **líquido**. O custo real é `spend ÷ (1 − 0,1215)`, aplicado por linha diária cuja `date_start >= 2026-01-01`. A parcela do imposto é a diferença.

Fonte única: `packages/web/lib/utils/funnel-metrics.ts:29-36` (`applyMetaAdsTax`), alíquota em `:17`.

O risco que esta auditoria endereça não é a fórmula — é **onde** ela é aplicada. O imposto é aplicado no frontend em alguns fluxos e no backend em outros; aplicar nos dois lados produz imposto ao quadrado, e o resultado é um número plausível, apenas errado. O histórico do repositório tem dois incidentes desse tipo.

---

## AC1 + AC2 — Inventário dos pontos de spend

| # | Ponto exibido | Arquivo:linha | Origem do spend | Imposto aplicado? | Veredito |
|---|---|---|---|---|---|
| 1 | Resumo · **Investimento (Meta)** | `lyrio-stage-view.tsx:155`, exibido em `:545` | `useCampaignDailyInsightsBulk` → `/campaign-daily` | Sim — frontend, `sumMetaInsights` → `applyMetaAdsTax` por linha | ✅ 1× |
| 2 | Resumo · **Investimento (Google)** | `lyrio-stage-view.tsx:163-165`, exibido em `:547` | `useGoogleAdsCampaigns` → `/google-ads/analytics/:id/campaigns` | Não — **correto**, o tributo é da plataforma Meta | ✅ N/A |
| 3 | Resumo · **ROAS** | `:168` (soma), `:178-179` (razão), exibido em `:550` | `metaSpend28 + googleSpend28` | Herdado: Meta 1×, Google 0× | ✅ 1× |
| 4 | Meta Ads · **Spend Meta** | `sales-meta-kpis.tsx:104` → `:128` | mesmo `/campaign-daily` | Sim — frontend, `sumMetaInsights` | ✅ 1× |
| 5 | Meta Ads · **CPM** | `sales-meta-kpis.tsx:134` | derivado de `spend` (#4) | Herdado | ✅ 1× |
| 6 | Meta Ads · **ROAS** | `sales-meta-kpis.tsx:161` | derivado de `spend` (#4) | Herdado | ✅ 1× |
| 7 | Meta Ads · **CPV** | `sales-meta-kpis.tsx:162` | derivado de `spend` (#4) | Herdado | ✅ 1× |
| 8 | Meta Ads · **Margem** (hint do ROAS) | `sales-meta-kpis.tsx:163`, `:185` | derivado de `spend` (#4) | Herdado | ✅ 1× |
| 9 | Google Ads · **Investimento** | `lyrio-stage-view.tsx:950-952`, exibido em `:967` | `useGoogleAdsCampaigns` | Não — correto | ✅ N/A |
| 10 | Google Ads · **CPA** | `lyrio-stage-view.tsx:962` | derivado de #9 | Não — correto | ✅ N/A |

**Nenhum ponto com 0× indevido ou 2×.** Os dois caminhos de spend Meta da tela (`metaSpend28` e o de `SalesMetaKpis`) convergem em `sumMetaInsights`, que é o único lugar onde o gross-up acontece no frontend.

---

## AC3 — Os endpoints consumidos não pré-tributam

| Endpoint | Consumido por | Pré-tributa? | Evidência |
|---|---|---|---|
| `/api/traffic/analytics/:projectId/campaign-daily` | #1, #4 | **Não** | `grep applyMetaTax` no bloco da rota (`traffic-analytics.ts:553-640`): **0 ocorrências**. Convenção registrada em `:859` |
| `/api/google-ads/analytics/:accountId/campaigns` | #2, #9 | **Não** (nem deveria) | `spend` vem de `cost_micros ÷ 1e6` (`services/google-ads.ts:170-172`) |

O helper `packages/api/src/utils/meta-tax.ts` existe e é usado por **outros** fluxos (traffic-analytics agregado, lead-scoring). Nenhum deles alimenta a etapa Lyrio.

---

## AC4 — Borda da data

Coberta por teste (`funnel-metrics-meta-tax.test.ts`): 2025 não tributa, 2026 tributa, período que cruza a virada tributa só as linhas de 2026, `date_start` vazio não tributa, spend zero não tributa.

A decisão vale por **linha diária**, não pela data de hoje — verificado em `funnel-metrics.ts:94` (`sumMetaInsights` passa `row.date_start`) e no novo `sumMetaSpendWithTax`.

---

## AC5 + AC6 — Helpers e travamento

Adicionados a `funnel-metrics.ts`, derivando de `applyMetaAdsTax` (alíquota e regra de data em um lugar só):

- `metaTaxAmount(spendValue, dateStr)` — a parcela do imposto
- `sumMetaSpendWithTax(allInsights: CampaignDailyInsight[][])` — `{ spendLiquido, imposto, spendComImposto }`

15 testes em `packages/web/lib/utils/__tests__/funnel-metrics-meta-tax.test.ts`, incluindo as duas invariantes (`spendComImposto = spendLiquido + imposto` e igualdade com `sumMetaInsights`), com `toBeCloseTo` — as comparações são de ponto flutuante e `a + (b − a) === b` não vale em IEEE-754.

**Aritmética de referência dos testes:** 878,50 é o líquido cujo bruto é exatamente 1.000,00. O imposto é 121,50 — 12,15% **do bruto**. Um teste também verifica explicitamente que o resultado **não** é `spend × 1,1215` (985,24), a conta rejeitada na decisão de junho/2026.

---

## Pendência aberta — moeda do Google Ads (Story 42.3 AC5)

Não é sobre imposto, mas apareceu na auditoria e afeta os pontos #2, #3, #9 e #10.

`spend` do Google vem de `cost_micros ÷ 1.000.000`, e o Google reporta `cost_micros` **na moeda da conta**. Verificado por busca: **`currency_code` não é lido em nenhum ponto** de `packages/api/src/services/google-ads.ts` nem das rotas. A moeda é assumida como BRL, não verificada.

Enquanto a conta vinculada operar em BRL, os números estão certos. Se algum dia uma conta em outra moeda for vinculada, o card do Resumo somará moedas diferentes — erro invisível, de magnitude ~5×.

**Não corrigido aqui:** converter moeda é decisão de produto (qual cotação, de qual data), e a Story 42.3 instrui a escalar. Registrado como risco conhecido.

---

## Conclusão

A regra estava correta antes desta story. O que mudou:

1. a correção agora está **provada** por teste, não por inspeção;
2. existe um helper (`sumMetaSpendWithTax`) que permite exibir líquido e imposto separados **sem** reimplementar a alíquota — que é o que a Story 42.4 consome para o memorial da Margem de Contribuição;
3. o inventário acima documenta os 10 pontos, servindo de referência para quem for mexer nesta tela depois.
